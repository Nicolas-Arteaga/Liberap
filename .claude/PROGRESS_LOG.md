# Progress Log — Verge

Changelog corto y fechado de fixes/features/decisiones por sesión. Ver `~/.claude/CLAUDE.md` para cómo se usa esto.

## 2026-09-24 (54) — Migraciones de Trail-1 APLICADAS a producción (pedido explícito del usuario)
- `dotnet ef database update` ejecutado: aplicó `AddTrailStopCanaryFields` y `AddUseTrailStopToStrategyProfile` contra la base Postgres real (localhost:5433/Verge).
- Verificado post-aplicación: 5 columnas nuevas creadas con los tipos/defaults diseñados (`TrailLevelApplied` int default 0, `TrailStopCanary` bool nullable, `OriginalSlPrice` numeric nullable, `TrailAuditJson` text nullable en `SimulatedTrades`; `UseTrailStop` bool default false en `StrategyProfiles`). Cero filas afectadas (3,364 trades / 33 perfiles intactos), sistema operando con normalidad (28 trades abiertos al momento de verificar).
- **Comportamiento sin cambios**: `TrailStop:Enabled` sigue en `false` y ningún perfil tiene `UseTrailStop=true` — las columnas existen pero Trail-1 sigue completamente inactivo hasta activación explícita futura (interruptor global + toggle por perfil).

## 2026-09-24 (53) — ROUND 42: corrección sobre migraciones "pendientes" + Trail-1 como flag de StrategyProfile (flujo normal de Verge)
- **Corrección a R41**: `AddStrategyProfileBroadcastToBinance` y `AddSimulatedTradeExclusionTag` NO estaban pendientes — error de interpretación mío al leer el `tail` de un script `--idempotent` sin acotar rango (imprime TODO el historial, no solo lo pendiente). Verificado por consulta directa a `__EFMigrationsHistory`: las 37 migraciones del proyecto salvo la de Trail-1 ya están aplicadas y corriendo desde julio, sin incidentes. Documentado qué corrige cada una (BroadcastToBinance = espejo de entradas/salidas contra Binance real; ExclusionTag = excluir un trade puntual de las estadísticas sin borrarlo) y confirmado que ambas son necesarias (código ya las referencia activamente) y seguras (aditivas, ya probadas ~2 meses).
- **Trail-1 rediseñado como configuración de StrategyProfile, no canary por hash**: nuevo campo `StrategyProfile.UseTrailStop` (bool, default false), mismo patrón exacto que `BroadcastToBinance` — expuesto en los DTOs, mapeado en `StrategyProfileAppService.ApplyInput`/`MapToDto` (mismos endpoints Create/Update de siempre, sin API nueva), con toggle agregado al editor de estrategias en Angular (mismo estilo visual que el toggle de Binance).
- `SimulationMarkPriceWorker` ahora resuelve los perfiles con `UseTrailStop=true` UNA vez por ciclo (no por trade) y aplica Trail-1 solo a los trades de esos perfiles. `TrailStop:Enabled` en appsettings sigue como interruptor maestro global (defensa en profundidad). Se eliminó el mecanismo de `CanaryPercentage`/hash de Guid de R40.
- **Migración nueva generada, NO aplicada**: `AddUseTrailStopToStrategyProfile` (un solo `ADD COLUMN` aditivo, verificado por SQL directo que la columna no existe todavía en producción — a diferencia de las 2 de la Parte 1, esta sí es genuinamente pendiente).
- Compilación verificada (0 errores, directorio temporal). 17/17 tests siguen pasando. **No se verificó visualmente en navegador** el toggle nuevo de Angular (requiere levantar el stack completo) — pendiente de revisión visual rápida la próxima vez que se abra el editor en desarrollo.
- **NO se aplicó ninguna migración, NO se activó nada, NO se reinició ningún servicio, NO se tocó entrada/scoring/sizing.**
- Deliverable: `TRAIL1_STRATEGY_ROUND42.md`.

## 2026-09-24 (52) — ROUND 41: audit de migración + equivalencia con código real compilado (harness standalone) — READY TO ACTIVATE
- **Audit de migración**: generado el SQL real con `dotnet ef migrations script --idempotent` (solo lectura, no toca la DB). Confirmado: solo 4 `ALTER TABLE...ADD COLUMN` (TrailLevelApplied/TrailStopCanary/OriginalSlPrice/TrailAuditJson), todas nullable o default seguro, `BreakevenLocked` confirmado AUSENTE del script (el fix manual de R40 funcionó). Hallazgo adicional: la DB de producción tiene 2 migraciones previas pendientes, ajenas a este trabajo (`AddStrategyProfileBroadcastToBinance`, `AddSimulatedTradeExclusionTag`) — se aplicarían junto con la de Trail-1 si se corre `database update`.
- **Equivalencia con código real**: construido `tools/TrailReplayHarness` (proyecto standalone, fuera de la solución de producción, no referenciado por nada) que compila e invoca DIRECTAMENTE el archivo real `TrailingStopCalculator.cs` (no una réplica en Python) contra los 2,986 trades históricos vía Npgsql+Sqlite.
- **99.7% idéntico (2,977/2,986) contra el backtest corregido de R39.** 9 casos discrepantes (0.3%, $4.06 total = 1.10%) con causa exacta cuantificada: el cálculo de excursión favorable (`favBp`) usa log-return en el script Python de validación vs porcentaje simple en el código real — en 7/9 casos la barra de salida es idéntica pero el nivel de Trail-1 alcanzado difiere por estar justo en el borde de un umbral. Dirección del residuo sigue siendo conservadora (código real $375.14 > backtest $371.08, tercera vez consecutiva R38/R39/R41).
- **Canary**: confirmado que esta ronda no tocó ningún archivo de producción (solo el harness standalone + script de investigación). NO se ejecutó `database update`, NO se cambió el flag, NO se reinició nada, NO se tocó entrada/scoring/sizing/TP-SL base — confirmado indirectamente por un trade real (`PUNDIXUSDT`) abierto durante el audit, el sistema sigue operando normalmente sin interferencia.
- **VERDICT: A) READY TO ACTIVATE.**
- Deliverable: `CANARY_PRECHECK_ROUND41.md`.

## 2026-09-24 (51) — ROUND 40: PLAN DE CANARY para Trail-1 — READY TO ENABLE CANARY (nada activado)
- Convertido `TrailStopEnabled` de `const bool` a configuración (`appsettings.json` sección `TrailStop:Enabled`/`CanaryPercentage`, defaults `false`/`0` = comportamiento idéntico a hoy). Leído vía `IConfiguration` inyectado en `SimulationMarkPriceWorker`.
- **Canary determinístico sin símbolos hardcodeados**: `IsCanaryAssigned(tradeId, pct)` usa `Guid.GetHashCode() % 100 < pct` — estable entre reinicios, asignado UNA vez por trade y persistido en columna nueva `TrailStopCanary` (null=sin evaluar, true=canary, false=control). Grupo control queda con cero cambios de comportamiento.
- **3 columnas nuevas en `SimulatedTrade.cs`**: `TrailStopCanary` (bool?), `OriginalSlPrice` (decimal?, preserva el SL original para el contrafactual post-hoc del grupo canary), `TrailAuditJson` (string?, bitácora acotada a 3 eventos por trade, poblada solo en cruces de nivel — nunca por tick).
- **Migración EF Core generada (`AddTrailStopCanaryFields`), NO aplicada** (`dotnet ef migrations add` solo genera archivos locales, no toca la DB). **Hallazgo no relacionado**: EF propuso agregar también `BreakevenLocked`, que YA EXISTE en la base de producción (drift preexistente sin migración asociada, no causado por este cambio) — removido manualmente del Up()/Down() generado para que la migración no falle.
- Compilación verificada a directorios temporales (proceso productivo seguía corriendo, sin interferencia): 0 errores. 17/17 tests siguen pasando sin cambios.
- **Criterio de duración del canary basado en cantidad de trades, no tiempo fijo**: ~100 trades canary para señal inicial (test de signo, dado que R37 mostró 73% de trades "salvados" — proporción alta, detectable con muestra chica), ~300 recomendado para un intervalo de confianza ajustado del delta en dólares.
- **VERDICT: READY TO ENABLE CANARY.** NO se aplicó la migración, NO se activó el flag, NO se reinició ningún servicio, NO se tocó `risk_manager.py`/`OpenTradeAsync`/señales/scoring/ranking/tamaño inicial — la asignación de canary ocurre en el worker de monitoreo, después de que el trade ya fue abierto por el flujo existente.
- Deliverable: `TRAIL1_CANARY_PLAN_ROUND40.md`.

## 2026-09-24 (50) — ROUND 39: cerrada la discrepancia R37 vs R38 ($9.2/2.5% → $4.0/1.1%, causa exacta identificada) — PASS
- Comparación trade-por-trade sobre el MISMO pool y la misma carga de klines (descarta de entrada la hipótesis de ONUSDT/BBUSDT). 913/2985 trades con resultado distinto entre los dos algoritmos.
- **Causa raíz exacta encontrada** (no "diferencias menores"): PHAROSUSDT 2026-07-07 explicaba +$7.95 de los +$9.26 — el script de validación R37 calculaba el precio de breakeven/lock con compounding EXPONENCIAL (`entry*exp(side*lockBp/1e4)`), mientras el código C# real (`TrailingStopCalculator.cs`) usa porcentaje SIMPLE (`entry*(1+lockBp/10000)`). Diferencia de ~0.005% que, contra un OHLC de precisión finita, cayó en el lado equivocado del umbral en un mínimo de vela, cambiando el resultado del trade de SL a TP.
- **Corregido**: `agent/backtest/r37_trail1_validation.py` línea 99 ahora usa la misma fórmula lineal que producción. Re-corrida la validación completa (TRAIN/VAL/OOS/mitades/perfil/dirección/símbolo/régimen) — discrepancia total bajó de $9.2 (2.5%) a $4.0 (1.1%).
- Residuo restante explicado: segunda fórmula menor (excursión favorable en bp también usaba log-return en R37 vs porcentaje simple en producción) — no corregida esta ronda por ser inmaterial (1.1%) y **conservadora**: el código real ($377.0) sigue rindiendo por encima de la validación corregida ($373.0), nunca al revés — nunca le da al backtest una ventaja que el sistema real no tiene.
- **Ninguna conclusión cualitativa de R37 cambia**: Trail-1 sigue mejorando los 5 cortes, ambas direcciones, 67.3% de símbolos, los 3 regímenes de volatilidad.
- **VERDICT: PASS.** Código sigue con `TrailStopEnabled=false`. NO se generó migración, NO se activó nada, NO se reinició ningún servicio, NO se tocó `risk_manager.py`.
- Siguiente paso (próxima ronda, decisión del usuario): diseñar el canary de activación.
- Deliverable: `DISCREPANCY_AUDIT_ROUND39.md`.

## 2026-09-24 (49) — ROUND 38: Trail-1 llevado a código real (DESACTIVADO) — READY FOR PRODUCTION
- Auditoría del flujo real: apertura en `SimulatedTradeAppService.OpenTradeAsync` (SimulatedTradeAppService.cs:48), TP/SL calculados en `agent/risk_manager.py` (no en C#), monitoreo en `SimulationMarkPriceWorker` (tick de 1s). Ya existía un breakeven lock escrito pero deliberadamente inerte (umbral=101%, nunca dispara) y un trailing continuo ("Cosecha Inteligente") removido explícitamente el 2026-07-15 — comentario del propio código documenta que un despliegue de 2026-08-18 sin backtest previo fue corregido por el usuario con la instrucción de no reactivar sin validar candle-a-candle primero (R35-R38 es exactamente esa validación).
- **Implementado**: `TrailingStopCalculator.cs` (nuevo, función pura con la matemática de Trail-1: 100bp→BE, 150bp→+50bp, 200bp→+100bp, salto directo al nivel más alto si el precio saltea varios umbrales en un tick), campo nuevo `TrailLevelApplied` en `SimulatedTrade.cs` (único estado agregado, deriva excursión favorable del `MaxFavorablePrice` ya existente), integración en `SimulationMarkPriceWorker.cs` gateada por `TrailStopEnabled = false` (mismo patrón que el breakeven lock existente — compilador elimina la rama, cero riesgo de activación accidental).
- **17/17 tests xunit pasan** (`TrailingStopCalculator_Tests.cs`): LONG/SHORT en los 3 niveles, retroceso de precio no deshace el nivel, SL ya mejor nunca se empeora, idempotencia multi-tick, reinicio de proceso sin regresión, salto de +80bp a +220bp en un solo tick aplica directo el nivel 3.
- **Replay de la lógica real de producción** (reimplementación línea por línea en Python) sobre la misma población de 2,985 trades de R37: $377.0 vs $367.8 esperado — diferencia de 2.5%, explicada (orden de aplicación de niveles matemáticamente equivalente, 2 trades de diferencia en carga de símbolos), sin discrepancias estructurales.
- **VERDICT: READY FOR PRODUCTION** (el código). Compilación del host verificada a directorio temporal (proceso productivo seguía corriendo, sin interferencia). NO se activó `TrailStopEnabled`, NO se generó/corrió la migración EF Core para la columna nueva, NO se reinició ningún servicio, NO se tocó `risk_manager.py` ni lógica de entrada.
- Pendiente (decisión del usuario): generar y revisar la migración EF Core, decidir cuándo activar el flag (recomendado: canary antes que todo el libro).
- Deliverable: `TRAIL1_IMPLEMENTATION_ROUND38.md`.

## 2026-09-24 (48) — ROUND 37: VALIDACIÓN FINAL DE TRAIL-1 → PASS, no era sesgo de muestra
- `agent/backtest/r37_trail1_validation.py`. **Fase 1 resuelta**: el "sesgo" de R36 (baseline negativo en el subconjunto reconstruible vs +$34,890 en la población completa) NO era sesgo de muestreo — son 10 trades de ONUSDT + 2 de BBUSDT con `ClosePrice` CORRUPTO (saltos ~1000x, ej. ONUSDT $0.09→$98.63), generando PnL de 5-6 cifras imposibles sobre posiciones de $150. Verificado por SQL directo: excluyendo esos 12 trades, la población limpia (3,271 trades) suma −$2,488.02 — negativa, consistente con el baseline de R36.
- **Fase 8 — cobertura recuperada al 100%**: se encontró que `agent/data/klines.db` (caché propio del agente, 858 símbolos) cubre los 189 símbolos que faltaban en `binance_vision_clean.db`. Fusionando ambas fuentes, cobertura 608/608 símbolos (100%), población simulable creció de 2,337 a 2,985 trades.
- **Trail-1/2/3 mejoran el baseline en los 5 cortes independientes** (TRAIN, VAL, OOS, mitad1, mitad2), sin excepción — OOS: baseline PF=0.76 → Trail-1 PF=1.23, Trail-2 PF=1.28, Trail-3 PF=1.25 (las 3 variantes rentables y de magnitud similar, robusto a variar el umbral ±25-50bp).
- **11 de 12 perfiles de estrategia mejoran** (el único que empeora es marginal, −$6.3). **Ambas direcciones mejoran** (LONG +$2,394.6, SHORT +$585.5, aunque LONG sigue negativo por sí solo). **67% de 382 símbolos con ≥3 trades mejoran** (no concentrado en pocos). **Los 3 regímenes de volatilidad mejoran**, con el régimen de alta volatilidad volviéndose completamente positivo por sí solo (+$805.6 desde −$895.3).
- **VERDICT: PASS.** Es una mejora real, no un artefacto de muestra. Matices: TRAIN queda cerca de breakeven pero no positivo con ninguna variante; LONG mejora muchísimo pero no se vuelve rentable por sí solo.
- **NO se implementó nada.** Siguiente paso (fuera de esta ronda): diseñar la incorporación a `risk_manager.py` sin tocar la lógica de entrada, pendiente de decisión del usuario.
- Deliverable: `TRAIL1_FINAL_VALIDATION_ROUND37.md`.

## 2026-09-24 (47) — ROUND 36: SIMULACIÓN CONTRAFACTUAL DE GESTIÓN DE SALIDA → RESULTADO A, trailing stop robusto en TRAIN/VAL/OOS/ambas mitades
- `agent/backtest/r36_exit_management.py`. Simulación barra a barra (15m) de 12 reglas de gestión de salida (5 break-even, 4 take-profit parcial, 2 trailing) sobre los 2,337 trades reales simulables de `SimulatedTrades`, con convención pesimista explícita (ambigüedad intrabar = se asume que el stop original se ejecuta primero).
- **Calibración del motor**: diff mediana vs RealizedPnl real = −$0.01 (motor fiel), diff media = −$71.92 (cola de trades en símbolos de baja liquidez con error de reconstrucción, documentado no oculto).
- **Advertencia de sesgo de muestra**: el subconjunto reconstruible (2,337 de 3,315) tiene baseline NEGATIVO en TRAIN y OOS pese a que el total real de producción es positivo (+$34,890) — los símbolos sin cobertura de klines no son aleatorios respecto del resultado. Las comparaciones RELATIVAS entre reglas siguen siendo válidas; las cifras absolutas de PnL/mes no.
- **RESULTADO A: Trail-1 (100→BE, 150→+50bp, 200→+100bp asegurados) es la ÚNICA regla de las 12 que convierte el baseline de negativo a positivo en los 3 segmentos (TRAIN, VAL, OOS) Y en ambas mitades temporales** — PF OOS=1.28 (mejor de todas), 780 trades salvados vs 173 empeorados en TRAIN (ratio ~4.5:1). Las reglas BE mejoran pero no cruzan a positivo en TRAIN; los 4 Partial quedan negativos en OOS.
- **Fase 9 (desglose 1,032 SL con MFE≥100bp)**: MAE posterior al pico favorable sistemáticamente grande (−249 a −490bp en todos los buckets) — clasificación (D) del brief: trades que necesitan gestión activa/toma parcial, no ruido. Bucket ≥500bp (262 trades) es el más costoso individualmente (−$701.9).
- **NO se tocó producción.** Pendiente antes de proponer el cambio: (1) resolver el sesgo de muestra de cobertura de klines, (2) documentar como propuesta a `risk_manager.py` (no `portfolio_engine.py`, que gobierna señales de entrada nuevas, no gestión de posiciones existentes — aclarado explícitamente), (3) validar por perfil/símbolo (pendiente, Fase 8).
- Deliverable: `EXIT_MANAGEMENT_SIMULATION_ROUND36.md`.

## 2026-09-24 (46) — ROUND 35: AUDITORÍA FORENSE DE TRADES REALES — hallazgo fuerte: 45.7% de los SL tuvieron +100bp de excursión favorable antes de revertir
- Cambio de fuente: no más OHLCV/OI sintético — auditoría de trades REALES ejecutados por el sistema de producción. **Fuente nueva localizada**: tabla `SimulatedTrades` en Postgres (localhost:5433/Verge), 3,315 trades reales, 17 perfiles de estrategia (no solo Nexus), 2026-05-16 a 2026-09-22 — nunca usada en las 34 rondas anteriores.
- **Hallazgo de cobertura crítico**: las columnas ricas del esquema (Ma7DistancePctAtEntry, MaxAdversePrice, MaxFavorablePrice, AgentDecisionJson, ExitAuditJson) están 100% NULAS en los 3,315 registros — confirma el incidente de reset de Docker de memoria (`verge_backtest_gate`); se reconstruyó todo el contexto desde cero con klines_clean 15m, sin inventar nada (421/610 símbolos con cobertura suficiente, 2,612 trades reconstruidos con éxito, descartes explícitamente contados).
- **TP=443 (13.5%) / SL=2,840 (86.5%)**, PnL total +$34,890, consistente con estrategia de tendencia (muchas pérdidas chicas, pocas ganancias grandes).
- **Hallazgo fuerte (Fase 6 MFE/MAE)**: 1,032 de 2,257 trades SL reconstruidos (45.7%) habían alcanzado +100bp o más de excursión favorable ANTES de revertir completamente hasta el stop — el sistema actual no tiene mecanismo de breakeven/captura parcial. TP reales en cambio corren casi sin retroceso (MAE medio −102.9bp vs −350.8bp en SL).
- Fase 3 (contexto pre-entrada): estructura por encima de MA99 (MA7>MA99, MA25>MA99) es 7 puntos porcentuales más frecuente en TP que en SL (67% vs 60%) — direccionalmente consistente pero débil, no se sobrevende. ATR relativo, distancia a máx/mín 20 barras, velas alcistas, volumen y rango relativo: SIN diferencia apreciable, descartados explícitamente.
- Fase 5 (los 3 setups del usuario en trades reales): ninguno muestra ventaja fuerte (win rates 8%/16%/12% vs baseline 13.5%) — coherente con el resultado sintético de R34.
- **2 candidatos documentados (no 5 forzados)**: #1 gestión de salida (breakeven/captura parcial tras MFE≥100bp) con evidencia sólida y ejemplos concretos (AGTUSDT, ZBTUSDT, CHILLGUYUSDT); #2 filtro de estructura MA99 (débil, requiere TRAIN→VAL→OOS propio). NO se convirtió nada en `portfolio_engine.py` ni se tocó producción — pendiente simular la regla de salida sobre AMBAS poblaciones (TP y SL) antes de cualquier propuesta de cambio real.
- Deliverable: `FORENSIC_AUDIT_ROUND35.md`.

## 2026-09-24 (45) — ROUND 34: 13 SETUPS DE PRICE ACTION/MEDIAS MÓVILES (1H) → 0/11 sobreviven TRAIN, diagnóstico por setup, no cierre genérico
- Cambio de paradigma pedido por el usuario: setups discrecionales tipo trader (contexto/estado/evento/confirmación/entrada/stop/TP/invalidación/no-trade/hipótesis) en vez de factor mining. 3 semillas del usuario (giro MA7 pre-pullback MA25, MA7 toca MA99 como soporte, quiebre rápido MA7 desde pico) + 10 nuevos diseñados por mí (pullback+rechazo, 2 piernas, ruptura+retest fallido, falsa ruptura+recuperación, compresión→expansión, separación+pullback, cruce sin confirmación, precio cruza pero estructura aguanta, 2do giro en tendencia de fondo, rechazo zona MA25/50). Spec completa en `SETUPS_ROUND34_SPEC.md`.
- `agent/backtest/r34_ma_setups.py`: velas horarias agregadas de klines_clean 15m, MA7/25/50/99 (SMA), ATR14, stop=1.5×ATR, TP=2R, límite 72h, barrido intrabar (precisión horaria). 11 de 13 setups implementados (5 y 9 requieren detección de piernas multi-barra no codificada este round, declarado explícitamente, no rellenado con una versión débil).
- **Ningún setup sobrevive TRAIN** (CI excl 0 + signo positivo). Diagnóstico específico por grupo: **5 de 7 setups LONG (1,2,7,12,13) tienen pérdida NEGATIVA y significativa en TRAIN** — coincide con el mercado bayista de la muestra (R25/R31/R33) — el problema es el CONTEXTO (asumen tendencia alcista de fondo), no la lógica de entrada. Los 3 setups SHORT (3,6,10) no muestran ningún efecto, ni siquiera negativo — problema distinto: la entrada no captura nada distinguible del ruido. Setup 8 (compresión→expansión con MAs) reconfirma el cierre ya establecido de esa familia en R18/R23.
- **VERDICT: FAILED, con diagnóstico separado por setup, no un cierre genérico.**
- **PRÓXIMO PASO (Round 35)**: (1) endurecer el filtro de contexto/régimen de los 5 LONG que fallaron con significancia y re-correr solo esos 5 (corrección quirúrgica, no exploración nueva); (2) implementar los setups 5 y 9 pendientes con el rigor declarado antes de descartar la familia completa.
- Deliverables: `SETUPS_ROUND34_SPEC.md`, `MA_SETUPS_ROUND34.md`.

## 2026-09-24 (44) — ROUND 33: GEOMETRÍA TP/SL (MFE/MAE, asimetría de trayectoria) → FAILED, hallazgo metodológico sobre R-múltiplos
- `agent/backtest/r33_tp_sl_geometry.py` + `r33b_economics.py`. Cambio de objetivo de búsqueda: no retorno medio, sino P(TP antes que SL) y asimetría MFE/MAE. Unidad de riesgo 1R=volatilidad realizada causal por barra, SL=1R fijo, TP∈{1R,2R,3R}, horizonte 24h, universo ancho 357 símbolos.
- **Fase 1 (baseline sin condicionar)**: LONG negativo en R-múltiplos en los 3 segmentos; SHORT positivo — reconfirma el sesgo de mercado bajista de R25, ahora visible también en la geometría de trayectorias.
- **Fase 2 (6 estados simples: rv/vol/ret1 alto/bajo)**: 12/36 configuraciones "sobreviven" TRAIN→VAL→OOS en R-múltiplos, algunas pareciendo 80% mejores que el baseline (rv_hi+SHORT TP=3R: +0.101R vs +0.056R baseline).
- **Hallazgo metodológico central (Fase 2B, conversión a bp real)**: condicionar en volatilidad alta infla mecánicamente R (la unidad de riesgo en bp) — rv_hi eleva R_avg de 62.2bp a 157.3bp (2.5×), haciendo que un R-múltiplo similar o mayor represente bp reales similares o MENORES. Al convertir todo a bp: **ninguna de las 12 configuraciones supera 5.4bp gross** (vs 24bp de costo), y **ninguna aporta más de 3.5bp de información incremental sobre estar SHORT sin condición alguna**.
- No se llevó nada a `portfolio_engine.py` — ninguna configuración se acercó al costo de ejecución.
- **VERDICT: FAILED**, pero con un hallazgo metodológico reutilizable: normalizar a bp antes de confiar en estadísticas de R-múltiplos cuando el denominador R varía con la condición testeada (evita falsos positivos en futuras rondas con TP/SL).
- Refuerza la conclusión de R32: el eje OHLCV+volumen+volatilidad+OI+posicionamiento parece agotado tanto para retorno medio como para asimetría de trayectoria. Se mantiene la recomendación de datos nuevos (liquidaciones, order book, on-chain).
- Deliverable: `TP_SL_GEOMETRY_ROUND33.md`.

## 2026-09-24 (43) — ROUND 32: DISCOVERY ENGINE AUTOMATIZADO (384 pruebas) → 0 candidatos, dataset OHLCV+volumen+OI+posicionamiento sistemáticamente agotado
- `agent/backtest/r32_discovery_engine.py`. Grid automático (no hipótesis nombradas a mano): 128 máscaras (16 simples + 112 pares de 8 features causales) × 3 horizontes de criba = 384 pruebas en TRAIN. Incorpora por primera vez con rigor completo `toptrader_ls_pos`/`global_ls_acct`/divergencia (pendiente desde R31, solo tocado superficialmente en R12).
- Advertencia de comparaciones múltiples aplicada desde el diseño: se esperaban ~19 "significativas" por azar puro; pasaron 70 — indica estructura real a 72h, pero resultó ser la MISMA firma de drift de mercado bajista ya cerrada en R25, no una señal nueva.
- **82% de los 15 candidatos principales invierten de signo en VAL** (incluidos los de mayor magnitud en TRAIN, +266 a +280bp). De los 5 que sí confirman VAL, **el 100% muere en el control de placebo temporal** — en cada caso el placebo reproduce magnitud igual o mayor que la señal real.
- **`toptrader_ls_pos`/`global_ls_acct` evaluados con rigor completo: sin información incremental** — el único caso que sobrevivió hasta placebo (rv_pct=LO & lsacct_pct=HI, 72h) tiene OOS net=+14.1bp vs placebo=+16.7bp (placebo MAYOR) — cierra definitivamente la sospecha dejada abierta en R31.
- MFE/MAE de los 5 finalistas: simétricos, win rates 0.51-0.55 (esencialmente azar) — sin asimetría explotable.
- No se construyó sim económica — cero candidatos sobrevivieron el placebo, prerrequisito mínimo.
- **VERDICT: 0 candidatos. Evidencia sistemática (no solo intuición) de que OHLCV+volumen+OI+posicionamiento sobre el universo superviviente está agotado** para alpha direccional, combinando los hallazgos de R29 (OI puro), R30 (8 combos intradía), R31 (6 familias multi-día) y R32 (384 pruebas automáticas).
- **NO SE RECOMIENDA R33 sobre el mismo eje de datos.** Prioridad de datos nuevos: (1) liquidaciones reales con 60-90+ días (bloqueo operativo reversible — reiniciar colector), (2) order book histórico (requiere fuente nueva), (3) on-chain (requiere inversión del usuario).
- Deliverable: `ALPHA_DISCOVERY_ROUND32.md`.

## 2026-09-24 (42) — ROUND 31: CAMBIO DE HORIZONTE (multi-día 3-30d) → FAILED en las 6 familias, espacio OHLCV+volumen+OI agotado
- `agent/backtest/r31_multiday_regimes.py`. Ranking cross-sectional diario, top/bottom-3 (calza con 3 slots reales), 6 familias (momentum, reversión, OI acumulación/distribución, OI+momentum alineado/desacuerdo), W=H en {3,7,14,30}d, dirección y ventana TRAIN-decididas, VAL debe confirmar antes de OOS.
- **BTC buy&hold profundamente negativo en los 3 segmentos** (train≈−201bp/mes, val≈−488, oos≈−462 equivalente) — reconfirma el hallazgo de R25 de mercado bajista en toda la ventana del dataset.
- **Las 6 familias mueren en el filtro VAL**, con swings negativos enormes (−216bp a −658bp) respecto de lo que parecía ganador en TRAIN. Caso más revelador: Momentum W=14d parecía el mejor de TRAIN (+$302/mes) pero pierde −658bp en VAL — firma clásica de overfitting a ruido (la ventana "ganadora" varía sin patrón entre familias: 14d/3d/7d/3d/14d/7d).
- Canasta aleatoria de 3 símbolos (sin ninguna regla) da retorno positivo del mismo orden que el mejor caso de TRAIN de las familias "inteligentes" — ninguna regla de ranking aporta selección real sobre elegir al azar.
- **VERDICT: FAILED en las 6 familias.** No es problema de costos/concentración — el mecanismo (perseguir o desvanecer tendencia de 3-30 días) no sobrevive el control más básico de repetirse en el período siguiente.
- **DIAGNÓSTICO FINAL tras 31 rondas**: el dataset actual (OHLCV+volumen+OI, universo superviviente, mercado mayormente bajista) parece agotado para una segunda fuente de alpha direccional, intradía o multi-día. No se recomienda R32 repitiendo/combinando precio-volumen-OI. Prioridad de datos nuevos: (1) liquidaciones reales si se reinicia el colector, (2) order book histórico (no disponible), (3) on-chain (requiere inversión), (4) `toptrader_ls_pos`/`global_ls_acct` de oi_metrics con el rigor TRAIN→VAL→OOS→placebo actual (R12 lo tocó sin ese rigor).
- Deliverable: `MULTIDAY_REGIMES_ROUND31.md`.

## 2026-09-24 (41) — ROUND 30: ALPHA DISCOVERY ENGINE — 8 combinaciones estado+evento, todas FAILED, espacio de features parece agotado
- `agent/backtest/r30_discovery_engine.py`. Motor combinatorio (no hipótesis aislada): 8 combinaciones predeclaradas de 2-3 features cruzando precio/volumen/volatilidad (357 símbolos) y precio/volumen/volatilidad/OI (43 símbolos feature-complete) — ninguna repite exactamente lo ya probado en R18-R29.
- **Disciplina reforzada tras el caso E de R29**: VAL debe confirmar el mismo signo que TRAIN ANTES de calcular OOS — si no confirma, se descarta sin generar el número.
- **Ninguna de las 8 combinaciones se acerca al costo de 24bp RT** — mejores casos OOS gross entre −44bp y +3bp, siempre net negativo. En varios casos el placebo temporal tiene magnitud similar o mayor que la señal real (indistinguibles).
- **Fase 5 (distribución completa, no solo media)**: win rates pegados a 0.46-0.51 (esencialmente moneda), medianas ~0, dispersión P25/P75 enorme pero SIMÉTRICA — sin cola favorable explotable vía TP/SL/salida parcial. Skew alto en algunas ramas es producto de outliers puntuales, no de estructura recurrente.
- No se llegó a `portfolio_engine.py` — ninguna combinación superó el filtro de DISCOVERY.
- **VERDICT: FAILED en las 8 ramas.** Después de 30 rondas cubriendo sistemáticamente precio/volumen/volatilidad/OI (solos, en secuencia, y ahora combinados), el espacio de features causales disponibles en este dataset (OHLCV+volumen+OI de Binance USDⓈ-M, universo superviviente 357/43 símbolos) parece agotado para señal direccional de corto plazo.
- **PRÓXIMO EXPERIMENTO (Round 31)**: dos caminos honestos, no otra variante — (1) funding como feature de INTERACCIÓN (nunca combinado, solo aislado en H16 FAILED) cruzado con OI/precio; (2) cambio de horizonte a régimen semanal/baja frecuencia en vez de eventos intradía. Si ninguno rinde, la conclusión honesta sería que el dataset actual no contiene una segunda fuente de alpha direccional distinta de la existente sin datos estructuralmente nuevos (order book, liquidaciones reales, on-chain).
- Deliverable: `ALPHA_DISCOVERY_ENGINE_ROUND30.md`.

## 2026-09-24 (40) — ROUND 29: OI EVENT SEQUENCES → FAILED, 8 formulaciones predeclaradas, ninguna sobrevive TRAIN→VAL→OOS
- `agent/backtest/r29_oi_sequences.py`. Fase 0: universo Feature-Complete = 43 símbolos con ≥120,000 filas de OI (~14.5 meses sin huecos), no 63 — sin forward-fill ni datos inventados. 8 eventos predeclarados (OI expansion/contraction/aceleración/shock, cada uno solo y combinado con confirmación de volumen o desacuerdo de precio), dirección TRAIN-decidida, 6 horizontes.
- **Ninguna de las 8 ramas sobrevive la cadena completa**, con causas distintas por rama: A (expansion h=8h) muere por costo+placebo (net siempre negativo, placebo reproduce la magnitud); B y F invierten de signo en OOS respecto de TRAIN/VAL; C, D, G, H no generalizan más allá de TRAIN.
- **Caso límite documentado en detalle: E (expansion+volumen, h=8h)** — OOS net=+11.28bp, el ÚNICO resultado que supera el costo — pero VAL es negativo y no significativo, rompiendo la cadena TRAIN→VAL→OOS exactamente en el paso de confirmación exigido por el propio protocolo. Placebo también significativo (contaminación parcial). Declarado FAILED explícitamente, no rescatado con el número de OOS.
- No se llegó a Fase 3 (información incremental de OI) ni a sim económica — ninguna rama sobrevivió el filtro básico que las justificaría.
- **VERDICT: FAILED — OI como disparador primario (nivel/cambio/aceleración, solo o combinado) no contiene información direccional robusta en este universo de 43 símbolos feature-complete.**
- **PRÓXIMA FAMILIA (Round 30)**: comportamiento relativo entre altcoins vía DIVERGENCIA DE OI (ΔOI normalizado entre pares que se mueven juntos), explícitamente distinto del beta-hedge de precio ya cerrado en R13.
- Deliverable: `OI_EVENT_SEQUENCES_ROUND29.md`.

## 2026-09-24 (39) — ROUND 28: LIQUIDITY TRANSITION → FAILED, CIERRE DEFINITIVO del hilo 22:00-UTC/iliquidez (6 rondas)
- `agent/backtest/r28_liquidity_transition.py`. 3 transiciones predeclaradas (D: salida simple de iliquidez sostenida ≥4 barras, E: salida+movimiento extremo, F: salida+recuperación fuerte de volumen), dirección TRAIN-decidida, horizontes 15m-48h.
- **D (29,501 eventos)**: sin señal en NINGÚN horizonte, el más limpiamente nulo.
- **E (1,140 eventos)**: única señal (15m, fade) muere por el mismo patrón de R24 — el placebo temporal ES significativo, contamina la señal real.
- **F (268 eventos, insuficiente desde el inicio)**: TRAIN "aprueba" a 24h/48h pero con n=44 y CI absurdamente ancho ([2.59bp, 448.53bp]) — significancia espuria de muestra chica. OOS plano o negativo, nunca significativo.
- **VERDICT: FAILED. CIERRE DEFINITIVO del hilo 22:00-UTC/iliquidez** — 6 rondas independientes (R19-R21, R26-R28) sin producir una estrategia. No se abre R29 sobre otra variante de liquidez, tal como instruyó el usuario explícitamente.
- **PRÓXIMA FAMILIA (Round 29), completamente distinta**: secuencias OI+precio+volumen donde el OI sea el disparador (no capitulación, ya cerrada) — ej. expansión/contracción anormal de OI → confirmación de volumen → reacción de precio. Cobertura: `oi_metrics` 63/357 símbolos (18%). Alternativa: comportamiento relativo entre altcoins vía divergencia de OI (no beta-hedge de precio, ya cerrado en R13).
- Deliverable: `LIQUIDITY_TRANSITION_ROUND28.md`.

## 2026-09-24 (38) — ROUND 27: LIQUIDITY REGIME → FAILED limpio, sin dirección en TRAIN
- `agent/backtest/r27_liquidity_regime.py`. Construyó régimen de iliquidez causal (vol_pct decil≤10%, propio de cada símbolo, no reloj de mercado) y probó 4 formulaciones: estado solo, estado+movimiento extremo, Modelo A (hora==22+evento) vs Modelo B (iliquidez+evento), control volumen-only.
- **Ninguna de las 4 formulaciones alcanza CI que excluye 0 en TRAIN**, en ningún horizonte (15m/1h/4h/8h/24h) — a diferencia de R24 (donde había señal en TRAIN pero el placebo la reproducía), acá no hay ni siquiera señal aparente que llegue a probarse en VAL/OOS. Magnitudes crecientes con horizonte (hasta ±87bp @24h en estado+evento) pero sin consistencia de signo entre símbolos.
- BTC solo: 0 eventos (nunca cae en el decil más bajo de volumen relativo propio — coherente, es el más líquido del universo).
- **VERDICT: FAILED limpio, causa raíz = sin dirección, no problema de magnitud/costos** — se cerró en el filtro TRAIN, protocolo respetado (no se fuerza dirección sin evidencia).
- **PRÓXIMO EXPERIMENTO (Round 28)**: probar la TRANSICIÓN de liquidez (iliquidez→retorno de liquidez) en vez del estado estático — mecanismo genuinamente distinto, anticipado como plan B en el propio brief. Si también falla sin señal en TRAIN, cerrar definitivamente el hilo 22:00-UTC/iliquidez (sexto intento consecutivo) y pasar a secuencias OI+precio+volumen con secuencia distinta a capitulación, o comportamiento relativo entre altcoins con framing distinto al beta-hedge ya cerrado.
- Deliverable: `LIQUIDITY_REGIME_ROUND27.md`.

## 2026-09-24 (37) — ROUND 26: LIQUIDATION CASCADE bloqueada por datos (Caso C) — pivote a 22:00 UTC, hallazgo: es la hora MÁS TRANQUILA del día, no una transición de sesión
- Auditoría directa de `liquidations_research` en `agent/data/klines.db`: solo 48.5 horas de cobertura real (2026-09-09 a 2026-09-11), el colector se detuvo hace 13 días y no fue reiniciado. Fuente=Bybit (no Binance, mismatch de venue con el resto de la investigación). Binance dio de baja su endpoint histórico de liquidaciones globalmente — no hay forma de reconstruir histórico como se hizo con OI en R9. **CERRADO Caso C: datos insuficientes, causa raíz exacta documentada** (no fallo de la hipótesis).
- **Pivote autorizado por el propio brief**: `agent/backtest/r26_hour22_diagnostic.py`, diagnóstico puramente descriptivo (solo TRAIN) de volumen/rango/dispersión por hora UTC.
- **Hallazgo nuevo**: 22:00 UTC (el efecto horario más fuerte de R19/R20, nunca explicado causalmente) resultó ser la **segunda hora de MENOR actividad de todo el día** (volumen 0.76× el promedio, la más tranquila junto con las 23h) — no una "transición de sesión" de alta actividad como se asumía implícitamente. Las horas de mayor actividad son 14-16h UTC (apertura bursátil US). Esto es consistente con un mecanismo de iliquidez temporal (libro fino → flujo residual mueve el precio desproporcionadamente → reversión cuando vuelve la liquidez), no con un evento informado.
- **PRÓXIMO EXPERIMENTO (Round 27)**: reemplazar el filtro rígido "hora==22:00" por una variable causal continua de régimen de liquidez trailing (por símbolo, no reloj de mercado), descubrir umbral en TRAIN, validar con placebo temporal Y control de magnitud emparejada.
- Deliverable: `LIQUIDATION_CASCADE_ROUND26.md`.

## 2026-09-24 (36) — ROUND 25: SHORT BIAS → FAILED, era beta a un mercado bajista, no alfa alt-específica
- `agent/backtest/r25_short_bias_phaseA.py`. Fase A: reproducir el "sesgo bajista" de R12/R13/R15/R18/R24 de forma simple y transparente ANTES de diseñar cualquier estrategia — retorno forward no condicionado del universo completo vs majors (20 símbolos grandes) vs antiguos vs nuevos vs BTC/ETH solos.
- **Hallazgo decisivo**: BTC SOLO (sin ninguna selección de altcoins) ya es negativo y significativo en TRAIN/VAL/OOS a todos los horizontes. Verificado directamente contra precios crudos: BTCUSDT cayó de $104,545 (2025-06-01) a $64,504 (2026-08-17), ~38% en toda la ventana del dataset.
- **Conclusión**: el "sesgo estructural de altcoins" nunca fue alfa alt-específica — es BETA a un mercado cripto completo que bajó durante toda la ventana muestral. El gradiente antiguos(-15.5bp)/nuevos(-51.6bp) @24h OOS es consistente con mayor beta en activos chicos frente a un mercado que cae, no con dilución estructural independiente.
- **No se construyó sim económica** (Modelos A-F, portfolio_engine) porque hacerlo sería simular una estrategia cuyo mecanismo ya se identificó como beta de mercado — exactamente la familia "beta betting" ya cerrada en R13 (prohibida explícitamente en el brief de esta ronda).
- **VERDICT: FAILED con causa raíz identificada** — depende 100% del régimen de mercado (bajista en esta muestra), no sobrevive la prueba de selección-inteligente-vs-aleatoria, y en un régimen alcista el mismo mecanismo revertiría con pérdidas simétricas.
- **PRÓXIMO EXPERIMENTO (Round 26)**: liquidation cascade (familia 4, verificar cobertura del colector) o microestructura de 22:00 UTC investigando el fenómeno subyacente (familia 6) — las dos únicas familias de la lista de prioridad sin tocar con el motor corregido.
- Deliverable: `SHORT_BIAS_ROUND25.md`.

## 2026-09-24 (35) — ROUND 24: ALPHA DISCOVERY — 5 hipótesis nuevas FAILED, pero aparece x5 el mismo confounder (pista fuerte para R25)
- `agent/backtest/r24_discovery_battery.py`. Batería de 5 hipótesis nuevas (evitando todo lo ya cerrado en el ledger), dirección congelada en TRAIN antes de mirar VAL/OOS, placebo temporal +25 barras en cada una.
- **Bug propio detectado y corregido antes de reportar**: la selección de dirección TRAIN comparaba magnitud absoluta entre "continuación"/"reversión" (espejos matemáticos exactos) → siempre elegía "continuación" por empate de orden de inserción, sin importar el signo real. Corregido a elegir por signo positivo de mean_bp. Re-corrida completa.
- **H1-UP (fade de subida extrema), H1-DOWN (continuación de caída extrema), H3 (drop+recuperación parcial+continuación)**: las tres pasan TRAIN→VAL→OOS con CI excl 0, PERO el placebo temporal (+25 barras) reproduce casi exactamente la misma magnitud (a veces mayor) — no detectan el evento, detectan el mismo sesgo estructural bajista persistente del universo de altcoins. **Quinta aparición independiente del mismo confounder** (ya visto en R12, R13, R15, R18) — pero esta vez sin depender de una ventana temporal específica (2026Q3) ni de un mecanismo condicional particular, lo cual lo hace más creíble como fenómeno de mercado real (emisión/dilución de altcoins chicas) que como artefacto de datos.
- **H2 (doble shock), H5 (relativo cross-sectional 4h)**: fallo limpio, no generalizan de TRAIN a VAL/OOS. Familias cerradas con esta formulación.
- **H4 (ruptura fallida vs control de magnitud emparejada)**: el control da resultados del mismo orden que el grupo "fallido" — la falla del breakout no aporta nada que el tamaño del movimiento no aporte ya. Resuelve limpiamente la pista abierta de R23 (era magnitud/drift, no la falla específica).
- **VERDICT: caso B del brief** — ninguna hipótesis nueva es operable, pero el confounder recurrente (5ta vez) es la pista más fuerte y replicada de todo el proyecto.
- **PRÓXIMO EXPERIMENTO (Round 25)**: testear el drift estructural DIRECTAMENTE como candidato (canasta short no-condicionada, sin evento disparador) en vez de como confound a evitar, con el motor corregido de R23 (portfolio_engine, funding, order-invariant) y un control LONG-vs-SHORT + partición temporal (no placebo temporal, que ya sabemos que lo reproduce).
- Deliverable: `ALPHA_DISCOVERY_ROUND24.md`.

## 2026-09-24 (34) — ROUND 23: FIX THE LAB, THEN HUNT ALPHA — motor corregido, capitulación sigue cerrada, discovery nuevo FAILED
- `agent/backtest/portfolio_engine.py` (motor central nuevo, reemplaza la lógica de slot-filling duplicada en r18-r22) + `r23_fix_lab_and_hunt.py` (objetivo A) + `r23_discovery.py` (objetivo B).
- **Regla de asignación determinística declarada** (Parte 2 del brief): score descendente, tie-break símbolo alfabético ascendente — nunca orden de iteración.
- **6/6 tests sintéticos PASS** (2 candidatos+2 slots, score gana en 6 permutaciones, funding aplicado 1 vez, point-in-time listing/delisting, 100 seeds random → 1 resultado único).
- **Order invariance CONFIRMADA**: 100 seeds de orden de entrada aleatorio sobre el candidato de capitulación dan std=$0.00. **Corrección importante a R22.5**: el swing de $145/mes que parecía "bug de orden" era en realidad una comparación entre DOS REGLAS DE ASIGNACIÓN DISTINTAS (mejor-primero vs peor-primero), no un bug de iteración de diccionario — nunca hubo tal bug, lo que faltaba era declarar la regla explícitamente de antemano.
- **Funding integrado**: cobertura real 63/357 símbolos (18%) con `funding_hist` propio; resto usa mediana cross-sectional causal (asunción declarada). Impacto sobre capitulación: −$3/mes (insignificante).
- **R21 corregido: $169/mes** (vs $171 sin funding) — numéricamente cruza el umbral PASS, PERO los hallazgos sustantivos de R22 (walk-forward inestable 9/14 meses dominados por 5 outliers, régimen no generaliza, score-select no generaliza por mitad) usan la MISMA regla y siguen vigentes sin cambios. **VEREDICTO: CAPITULACIÓN SIGUE CERRADA, FAILED** — el motor corregido confirma, no reabre, el cierre de R22.
- **Discovery nuevo (volatility transition follow-through, compresión→ruptura→2da barra)**: FAILED — la hipótesis de continuación está mal orientada ya en TRAIN (reversión fuerte y significativa −151bp @24h en TRAIN, contradice la dirección declarada de antemano). Protocolo cerrado sin re-testear con signo invertido (evitar sobreajuste post-hoc).
- **Hallazgo colateral sin confirmar**: control (ruptura sin compresión) muestra reversión significativa en TRAIN/VAL/OOS, pero el placebo temporal también da negativo — firma del mismo drift estructural que ya contaminó R12/R13/R18. Pista para R24, no tomar en serio sin control de magnitud emparejada.
- **PRÓXIMO EXPERIMENTO (Round 24)**: liquidation cascade (si cobertura del colector alcanza), relative altcoin events con framing distinto a beta-neutral, o microestructura de 22:00 UTC investigando el fenómeno subyacente.
- Deliverable: `FIX_THE_LAB_ROUND23.md`.

## 2026-09-24 (33) — ROUND 22.5: BACKTEST REALITY AUDIT → C) PARCIALMENTE CONFIABLE, componentes específicos FALLAN
- `agent/backtest/r22_5_reality_check.py` + inspección directa de `binance_vision_clean.db`/`config.py`/scripts de descarga. NO busca alpha — audita el motor de investigación (r6-r22), distinto del motor de producción (ya gateado FAILED aparte).
- **SURVIVORSHIP CONFIRMADO (FAIL)**: el universo de 450 símbolos se sembró de `config.WATCHLIST` (el watchlist ACTUAL del agente), no reconstruido históricamente. 0/450 símbolos terminan antes del final del dataset → ningún contrato deslisteado está presente. Toda magnitud medida en 22 rondas aplica solo al universo superviviente, no al mercado cripto histórico completo.
- **LOOKAHEAD (PASS)**: test automatizado (spike futuro inyectado) confirma que `pctile_causal` no ve el futuro. Entrada siempre `open[i+1]`. Joins de funding y OI causales (verificados con tests sintéticos).
- **FEES (PARTIAL)**: fee taker 5bp/maker 2bp coinciden con la tabla real de Binance USDⓈ-M. Spread (3bp) y slippage (4bp) son estimaciones sin forma de verificar (no hay order-book histórico).
- **FUNDING (FAIL)**: R18-R22 (capitulación) sostienen posiciones hasta 24h pero NO incluyen ningún término de funding en el PnL, pese a que `funding_hist` existe y se usa correctamente en otras rondas (R9-R17).
- **SLOT COMPETITION (FAIL, el hallazgo más importante)**: se re-corrió el candidato de capitulación con el orden de prioridad EXACTAMENTE INVERTIDO (mismos candidatos/costos/timestamps) → $171/mes se convierte en $26/mes. **$145/mes (85% del resultado) depende solo del orden de procesamiento** — magnitud comparable al propio umbral PASS (≥150) del proyecto.
- **REAL-vs-REPLAY (INSUFFICIENT DATA)**: no existe operativa real; el único registro de producción (`trades.csv`) es de estrategias distintas (Nexus/SCAR/LSE) con reconstrucción de PnL ya marcada "no confiable" en memoria previa.
- **Perturbación de latencia**: robusto (60min de delay solo degrada $171→$156, ~9%) — la fragilidad NO es de velocidad de ejecución, es de asignación de capital entre candidatos.
- **VERDICT: BACKTEST STATUS = C) PARCIALMENTE CONFIABLE.** La maquinaria de detección de falsos positivos (placebo/OOS/cluster-bootstrap) es sólida y confiable. El número exacto de $/mes de cualquier candidato con competencia de slots o holding ≥8h NO es confiable sin corregir esos dos componentes primero.
- **REGLA ANTES DE R23**: no reanudar investigación de alpha hasta (1) exigir que toda sim con selección de candidatos reporte al menos 2 órdenes de procesamiento distintos, (2) incluir funding real en cualquier candidato con holding ≥8h, (3) documentar explícitamente que las magnitudes solo aplican al universo superviviente.
- Deliverable: `R22_5_BACKTEST_REALITY_AUDIT.md`. Esta ronda NO propone una nueva estrategia (regla explícita del brief).

## 2026-09-24 (32) — ROUND 22: PROVE IT OR BREAK IT → CAPITULACIÓN+SCORE-SELECT CERRADA FAILED
- `agent/backtest/r22_prove_or_break.py`. Auditoría adversarial del candidato de R21 ($150/mes), congelado sin tocar parámetros, más scoping de una 2da familia.
- **Walk-forward mes a mes sobre 14 meses completos (jul-2025→ago-2026, no solo VAL+OOS)**: 9/14 meses positivos pero dominados por 5 meses outlier (dic-25 +$464, may/jun/jul/ago-26); los otros 9 meses suman −$607 en conjunto.
- **Régimen sin lookahead (BTC-rv y universo-rv trailing-30d, umbral fijado SOLO en TRAIN)**: el umbral óptimo-en-TRAIN de BTC-rv, aplicado hacia adelante, prende la estrategia exactamente en los 3 PEORES meses de VAL+OOS ($139 de $1046 posibles) — el régimen es descriptivo, no predictivo. ON/OFF con este régimen EMPEORA el resultado OOS ($149/mes→$46/mes).
- **Generalización de score-select (el hallazgo que mata al candidato)**: comparando 6 métodos de selección (score/FIFO/random/low-vol/high-vol/raw-signal) por separado en cada mitad de VAL+OOS — en la 2da mitad hasta la selección ALEATORIA ($366/mes) supera a score-select ($341/mes). El "10× de mejora" de R21 (FIFO $15→score $150) era artefacto de mezcla de períodos, no habilidad de selección real.
- **Estrés extremo**: top-10 trades de 983 (1%) sostienen 73% del resultado — más concentrado de lo que R21 había medido.
- **Scoping liviano de 2da familia (failed-breakout)**: inconsistente entre TRAIN(+31.5bp)/VAL(−42.1bp)/OOS(+26.2bp) — descartado sin gastar una ronda completa.
- **VERDICT: CAPITULACIÓN (R18→R21) = CERRADA, FAILED.** No se rescata con otra variable. Lección metodológica reutilizable: cualquier mecanismo de "selección entre candidatos concurrentes" debe probarse por sub-período separado, nunca solo en el blend completo.
- **PRÓXIMO EXPERIMENTO (Round 23):** familia C (liquidation continuation) o F (secuencia price+OI+volumen+liquidación con secuencia distinta a capitulación) — nunca probadas formalmente. Verificar cobertura del colector de liquidaciones (corriendo desde 2026-09-10) antes de empezar; si insuficiente, usar el archivo histórico de metrics de data.binance.vision como en R9.
- Deliverable: `CAPITULATION_AUDIT_ROUND22.md`.

## 2026-09-24 (31) — ROUND 21: CAPITULATION EXECUTION + SCORE-SELECT → PARK, tocó exactamente $150/mes pero temporalmente inestable
- `agent/backtest/r21_capitulation_exec.py`. Re-audita capitulación de R18 con el costo maker validado en R20 + features pre-orden como predictor de selección adversa + selección de slot por SCORE en vez de FIFO cuando compiten varios candidatos por un slot de capital.
- **Bug propio encontrado y corregido antes de reportar**: la primera corrida de robustez apuntaba a la config equivocada (MAKER $70/mes) en vez del candidato headline (TAKER cap$450/slots5/score-select, $150/mes) — corregido y re-corrido.
- **Fill mecánico MAKER: 83.6% observado, filled net=+31.9bp — POSITIVO** (selección adversa presente pero mucho más leve que en R20: filled cae a 0.43× del gross taker, no a 1/4).
- **Features pre-orden predicen fuerte el edge post-fill** (monotónico, 30-40× de separación entre tercil bajo/alto: volatilidad realizada, distancia al mínimo de 20 barras, magnitud de la caída) — hallazgo nuevo y accionable.
- **Selección de slot por score (`drop_mag×volp_extra`) en vez de FIFO: $15/mes → $150/mes, 10× de mejora** — la mayor parte del valor viene de ELEGIR BIEN entre candidatos simultáneos, no de la señal cruda.
- **Robustez del candidato $150/mes**: sobrevive remove-best-trade ($120), fee×2 ($119), slippage×3 ($129) — el perfil de robustez más fuerte del proyecto. Pero **1ra mitad VAL+OOS = −$16/mes, 2da mitad = +$316/mes** (4 meses consecutivos may-ago positivos vs ene-abr mixto/negativo) — el mismo patrón de inestabilidad temporal que mató a R10/R12/R13/R18.
- **VERDICT: PARK** (no PASS) — cumple 7/8 criterios pero falla estabilidad temporal. No se descarta: es el candidato con mejor evidencia de 21 rounds, buen candidato para combinar con una 2ª estrategia independiente.
- **PRÓXIMO EXPERIMENTO (Round 22):** buscar una variable de régimen (volatilidad agregada del universo, régimen de BTC) que separe limpiamente marzo-abril de mayo-agosto — si existe, condicionar la estrategia a ese régimen la vuelve operable con una regla explícita de encendido/apagado. Si no aparece: mantener PARK y empezar la búsqueda de una 2ª estrategia independiente usando el mismo método de score-select sobre un mecanismo distinto.
- Deliverable: `CAPITULATION_EXECUTION_ROUND21.md`.

## 2026-09-23 (30) — ROUND 20: EXECUTION ALPHA + REALISTIC FILL → FAIL, pero cierra buena parte de la brecha
- `agent/backtest/r20_execution.py`. Ataca el hallazgo de R19 (hora UTC+compresión) con ejecución realista. **Se encontró y corrigió un bug real de signo** (la dirección se fijaba invertida respecto al retorno crudo de TRAIN) + un bug de doble escala en 2 tablas de diagnóstico — corrida reportada es la corregida. Deliverable: `EXECUTION_ALPHA_ROUND20.md`.
- **Costo descompuesto** (1 pata, distinto del modelo de 2 patas de R16/R17 confirmado correcto): TAKER fee5+spread3+slip4=12bp/lado→24bp RT. MAKER fee2+residual1=3bp/lado→6bp RT SI llena.
- **Familia horaria (dirección fijada en TRAIN, sin mirar VAL/OOS)**: de 6 horas probadas (20,21,22,23,00,01 UTC), **solo H=22 es consistente y creciente en VAL(+6.6bp) Y OOS(+14.1bp)** — evidencia de que no es hora arbitraria.
- **Maker con fill MECÁNICO (no asumido)**: 71.7% de fill observado (limit 6bp mejor, ventana 1h, usando máximos/mínimos intrabar). **Selección adversa real y medida**: maker-filled gross=3.79bp vs taker-todas gross=15.0bp (4× más chico) — las señales que llenan son las que menos continuación tienen después, tal como advertía el brief.
- **Timing de entrada**: óptimo = exactamente en la señal (open[t+1]); adelantar o demorar solo empeora.
- **TP/SL desde percentiles congelados en TRAIN**: mejora WR a 0.56 en OOS pero taker cost sigue devorando el gross.
- **Sim económica (12 configs: 3 capitales × slots × taker/maker)**: TODAS negativas. Mejor caso: **−$4/mes** (maker, $150, 5 slots) — mucho menos malo que taker (−$9 a −$29/mes) pero sigue sin cruzar a positivo, ni cerca de $150.
- **VERDICT: FAIL, pero informativo — ni "costo muy conservador" ni "señal muy chica" por separado explican el fallo, es la combinación**: corregir dirección + modelar maker realista cerró buena parte de la brecha (de −$29 a −$4/mes en el mejor caso) pero no toda.
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (11 rounds consecutivos, R10-R20).
- **PRÓXIMO EXPERIMENTO (Round 21, único):** re-auditar los mejores casi-PASS anteriores (R18 capitulación N=5/12h, R10 dOI-régimen-UP) bajo el costo maker AHORA VALIDADO (~6-9bp con fill mecánico+selección adversa medida, no 24bp genérico) — no requiere señal nueva, solo re-medir economía conocida con el costo correcto.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-22 (29) — ROUND 19: TEMPORAL MICROSTRUCTURE + EXECUTION → FAIL por magnitud (no por placebo, primera vez)
- `agent/backtest/r19_temporal.py`. Universo ancho 357 símbolos. Placebo aplicado DESDE EL DISEÑO (lección de R18), 4 tracks en paralelo. Deliverable: `TEMPORAL_MICROSTRUCTURE_ROUND19.md`.
- **Track A (hora del día)**: peor hora 22:00 UTC = −7.74bp (CI excl 0). **Placebo shuffle de etiqueta-hora (3 iter) da máximo 2.2-2.4bp — el efecto real es 3.2× más grande que el ruido de multiple-testing sobre 24 horas. El placebo NO reproduce el resultado, a diferencia de R12/R13/R18.** Probabilidad de breakout/expansión de volumen SÍ varía por hora (pico 13-15 UTC, solape US/EU: P(breakout) 0.25-0.28 vs 0.15-0.19 en horas quietas). PERO inestable: TRAIN=+0.5bp(CI incl 0) / VAL=−9.9bp / OOS=−11.5bp.
- **Track B (ventana funding settlement, reloj universal 00/08/16 UTC, sin necesitar funding_hist)**: firma limpia y monótona — efecto crece al acercar la ventana al settlement (−0.94bp @±60min → **−2.67bp @±15min**) y se disipa a los +15min. Volatilidad realizada NO cambia (43-44bp constante) — es sesgo direccional puro, no expansión de vol.
- **Track D (hora 22:00 × compresión previa)**: **−12.32bp, el mayor efecto de toda la ronda** — interacción genuina (vs −8.55bp sin compresión).
- **Track C (reversión sobre-reactores tras shock)**: sin efecto, CI incl 0.
- **VERDICT: FAIL en los 4 tracks — pero por MAGNITUD, no por placebo.** Es la primera vez en el proyecto que un hallazgo bate limpiamente al placebo y aun así falla: el mejor efecto de la ronda (−12.3bp) es la mitad del piso de costo asumido (24bp RT = fee+spread+slippage ×2 patas). **Confirma que la fricción de ejecución, no la ausencia de estructura de mercado, es el techo real del proyecto.**
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (10 rounds consecutivos, R10-R19).
- **PRÓXIMO EXPERIMENTO (Round 20, único):** atacar el COSTO, no la señal — modelar ejecución con órdenes límite (maker ~2bp vs taker 5bp) + probabilidad de fill realista (no 100%) para los candidatos casi-PASS de R18/R19 (capitulación N=5/12h ≈$19/mes teórico a 24bp; hora 22:00+compresión −12.3bp bruto). Si el piso baja de 24bp a ~10-12bp podrían cruzar a positivo sin señal nueva. Palanca nunca girada en 19 rounds.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-21 (28) — ROUND 18: ALPHA HUNT BREAK THE FRAME → casi-PASS que resultó ser drift (3ª confirmación) + lead nuevo
- `agent/backtest/r18_discovery.py` + `r18b_capitulation.py`. Universo ancho 357 símbolos, solo OHLCV+taker. Cambio de modo: secuencias de eventos + MFE/MAE intrabar + reacción-no-predicción (no factores aislados). Listing events evaluado por WebFetch en minutos y descartado (dato real pero universo de eventos limpios insuficiente, ya sabido desde R7/R8) — sin gastar la ronda ahí, tal como pedía el brief. Deliverable: `ALPHA_HUNT_ROUND18.md`.
- **Secuencia 1 (compresión→expansión breakout): FAILED.** No continúa en promedio (WR<50%, retorno final negativo en todos los horizontes); control sin compresión da números casi idénticos → la compresión previa no aporta nada.
- **Secuencia 2 (capitulación drop+volumen climax → SHORT continuación): el resultado más fuerte de 18 rounds, y FAILED tras auditoría profunda.** +87.5bp @24h, n=67045 eventos, **concentración bajísima** (quitar top-20 de 355 símbolos solo baja el promedio a +50.3bp). Pero: TRAIN=+117bp / **VAL≈0** / OOS=+89bp (inestable); y **el placebo random-timestamp (SHORT una barra cualquiera, sin ninguna condición de capitulación) ya da +30bp CI excl 0** — es el mismo drift estructural del universo altcoin, **re-confirmado por 3ª vez independiente (R12→R13→R18)**. Placebo time-shift+24h da +73bp, similar magnitud al "real". Sim económica: aun ignorando el placebo, con 1-5 posiciones de $450 solo se capturan 398-3697 de 67045 eventos (<6%) y el NET/mes es NEGATIVO en 7/8 configuraciones (mejor caso apenas +$19/mes a 5 slots/12h).
- **Familia C (rezagados tras movimiento amplio): FAILED**, sin efecto (cercano a H18 ya fallado, no se insiste).
- **LEAD NUEVO SIN EXPLOTAR: efectos de hora del día (Familia H).** Varias horas UTC con retorno medio CI excl 0 sobre ~62k observaciones cada una (18:00 UTC −9.9bp*, 22:00 −9.7bp*, 13:00 −5.7bp*, 19:00 +4.9bp*, 15:00 +4.4bp*) — nunca antes probado en 18 rounds, barato de auditar.
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (9 rounds consecutivos, R10-R18).
- **PRÓXIMO EXPERIMENTO (Round 19, único):** hora del día/sesión con rigor completo — TRAIN/VAL/OOS, placebo random-timestamp/time-shift (aplicado desde el inicio esta vez, el mismo test que mató al hallazgo de R18), sim económica ≤450. Si también falla: las 9 familias A-I del brief de R18 quedan agotadas.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-20 (27) — ROUND 17: CHRONIC FUNDING YIELD ATTACK → FAIL (turnover resuelto, pero la señal decae)
- `agent/backtest/r17_chronic_funding.py`. Mismo universo real de R16 (43 símbolos funding∩spot∩perp). Histéresis (enter |score|≥1.0, keep hasta sign-flip / |score|<0.5 / maxhold) en vez del re-ranking diario que mató a R16. Holding 7/14/30/60/90d, selección A(threshold absoluto)/B(top-N magnitud)/C(top-N persistencia-score), N=1..10, capital 150/300/450. Deliverable: `CHRONIC_FUNDING_ROUND17.md`.
- **Turnover SÍ se resolvió**: 15-45 trades en 8.5 meses (vs 599-1400 de R16), fees cayeron ~30× (de $20-40/mes a $1-4/mes).
- **PERO el funding capturado también cayó a ~$0.** El sesgo "crónico" es real mirando hacia atrás (diagnóstico corregido: t-stats de la media rolling 30-340 para LINKUSDT/GIGGLEUSDT/XAUTUSDT/PUMPUSDT) pero NO predice funding forward a 30-90 días.
- **Placebo decisivo**: random_selection reproduce el resultado real casi exacto (funding/mes $0.0 random vs $0.1 real, NET −$0.9 vs −$0.8) → criterio de FAIL explícito del brief cumplido literalmente.
- **No hay zona de holding rentable en 7-90d**: a 7d las fees vuelven a dominar (−$4.1/mes), a 60-90d las fees casi desaparecen pero el funding capturado también (−$0.8/mes). No existe sweet spot.
- **No depende de 2026Q3**: consistentemente ~$0 en TRAIN/VAL/OOS/ex-Q3/solo-Q3 — de hecho peor dentro de Q3.
- **Causa exacta (distinta de R16): decaimiento de la señal, no turnover.** La correlación trailing-vs-próximo-settlement de R16 (0.82 a 8h → 0.68 a 7d) sigue cayendo; a 30-90d ya no queda información utilizable, ni para los símbolos más "crónicos".
- **Funding carry (R16 alto-turnover + R17 bajo-turnover) queda CERRADO — no hay tercera variante.**
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (8 rounds consecutivos, R10-R17).
- **PRÓXIMO EXPERIMENTO (Round 18, único):** listing events con fecha verificada — único mecanismo de ~50 probados NUNCA refutado por evidencia, solo bloqueado por calidad de dato (R7/R8). Plan: WebFetch sobre anuncios públicos de Binance (no API baneada) para fechas verificadas, cruzar con `klines_clean`, correr event-study de reversión post-listing limpio.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-19 (26) — ROUND 16: FUNDING CARRY ATTACK → FAIL (causa exacta identificada)
- `agent/backtest/r16_funding_carry.py`. Universo REAL (intersección física funding_hist∩spot_klines∩klines_clean perp) = 43 símbolos, ventana 2025-12→2026-08 (8.5m, NO coincide con 2026Q3). Estructura A (LONG SPOT+SHORT PERP, funding positivo) OPERABLE — 20+ configs probadas (N=1/2/3/5/10, rebal 8h/24h/72h, trailing 8h/24h/72h/7d, weight equal/funding-weighted/capped, capital 150/300/450, lev 1x/3x). Estructura B (SHORT SPOT+LONG PERP, funding negativo) NO OPERABLE — sin borrow-rate, solo diagnóstico matemático descartado (66183bp-equiv bruto, irrelevante sin costo de préstamo real). Deliverable: `FUNDING_CARRY_ROUND16.md`.
- **VERDICT: FAIL.** 0/9 meses positivos en TODAS las configuraciones sin excepción. Mejor caso: −$7.2/mes (cap$150, lev1x, N=3, rebal24h). Causa exacta identificada: fees (dos patas spot+perp = 46bp RT, el doble que las estrategias direccionales de R9-R15) superan al funding capturado por 5-20× en cada config. Persistencia individual del funding es real (corr trailing-8h vs próximo settlement = 0.82, decae a 0.68 a 7d) pero el RANKING cross-sectional rota cada ~36h (duración media de racha de signo = 4.4 settlements), forzando turnover que las fees castigan.
- **No depende de 2026Q3**: excluirlo explícitamente EMPEORA el resultado (−$31.8 vs −$28.2 dataset completo) — confirma que el FAIL es estructural, no de ventana temporal.
- **Placebo**: random-selection PEOR que ranking real (−$38.1 vs −$28.2) → el ranking por funding SÍ aporta señal genuina sobre el azar, pero insuficiente para cubrir el costo de 2 patas.
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (7 rounds consecutivos, R10-R16).
- **PRÓXIMO EXPERIMENTO (Round 17, único):** funding carry de BAJO TURNOVER ("chronic yield") — símbolos con funding estructuralmente sesgado durante MESES (no la última semana, ya documentado en R15: BTWUSDT/HOMEUSDT etc.), rebalanceo mensual/trimestral en vez de diario. Ataca la causa raíz exacta de R16 sin cambiar el mecanismo. Si también falla: pivotar a listing events con fecha verificada (bloqueado en R7/R8 solo por calidad de dato).
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-18 (25) — ROUND 15: WIDE UNIVERSE DISCOVERY (~357 pares) → FAILED (las 6) + hallazgo nuevo: funding carry
- `agent/backtest/r15_wide_discovery.py`. Primera vez con el universo ANCHO (357 símbolos, no restringido a 45-63 OI-completos): todo klines_clean+taker_flow con ≥8000 barras, excl. equity/commodity. 6 hipótesis (familias A-evento/extremo, B-cross-sectional, C-rotation), screening TRAIN @24h N=3 vs random + oracle. Deliverable: `WIDE_UNIVERSE_DISCOVERY_ROUND15.md`.
- **VERDICT: FAILED (las 6).** Ninguna con CI excl 0 en TRAIN. Oracle (+3116bp) confirma que hay oportunidad de tamaño si se supiera el futuro, pero ninguna señal ex-ante la encuentra.
- **Limitación de datos descubierta:** `taker_flow` para los ~300 símbolos fuera del universo OI-completo (45-63) solo cubre 2025-12→2026-08, y la mayoría listó en 2026 → el grid con ≥50 símbolos simultáneos solo arranca ~abril-2026 → **el OOS (2026-07-15→08-17) coincide casi exacto con 2026Q3**, la ventana anómala de R13/R14. Cualquier lectura OOS "positiva" (ej. B1_momentum_wide oos+129bp@24h) está contaminada por construcción — no cumple el propio criterio del round ("sin depender exclusivamente de 2026Q3").
- **Confirmado (informativo):** el universo ancho SÍ amplifica la magnitud bruta (momentum TRAIN: +4.5bp/45sym R14 → +42.5bp/357sym R15) pero NO la estabilidad — sigue viviendo en la pata short dominada por eventos extremos (mismo patrón "placebo gana" de R13), no en selección real. Por pata: `long_top` y `short_bottom` divergen por cientos de bp en todos los candidatos.
- **HALLAZGO NUEVO (mecanismo no testeado en 15 rounds): funding-rate carry, NO direccional.** Chequeo de factibilidad sobre `funding_hist` (63 sym, 14.5m): dispersión de funding persistente y grande (HOMEUSDT −8.5bp/8h≈−93%/año n=1988; BTWUSDT +4bp/8h≈+44%/año n=529), sostenida en cientos de observaciones, no puntual. `spot_klines` (240 sym) permite el hedge spot-perp delta-neutral. Nunca se usó funding como fuente de yield (solo como predictor direccional, que ya falló en H16/R12).
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (5 rounds consecutivos, R10-R15).
- **PRÓXIMO EXPERIMENTO (Round 16, único):** funding-rate carry delta-neutral — portfolio top-N por funding trailing con signo, spot-perp hedge, costos de ambas patas + financiamiento, TRAIN/VAL/OOS excluyendo 2026Q3 aparte. Si tampoco alcanza $150/mes: ambas categorías (predictivo + carry) quedan agotadas con evidencia → recién ahí conclusión formal.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-17 (24) — ROUND 14: ALTCOIN CROSS-SECTIONAL DISCOVERY → FAILED (las 10)
- `agent/backtest/r14_discovery.py`. Sin descargas nuevas. Universo 45 símbolos feature-completos (documentado el límite: OI/funding/L-S histórico no existe para ~380 de los ~400 pares del venue). Grid de rebalanceo 4h, 1452 usables. 9 scores de INTERACCIÓN predeclarados (compresión+OI, funding+taker, momentum+OI, outlier multi-feature, etc.) + 2 baselines (momentum-only, vol-only) + 1 oracle (retorno futuro real, sanity, nunca usado como estrategia). Deliverable: `CROSSSECTIONAL_DISCOVERY_ROUND14.md`.
- **Screening TRAIN @24h vs random-ranking**: NINGUNO de los 10 candidatos supera al random (+6.5bp, CI incl 0) con signo correcto. Los 2 con CI excl 0 (`S1_expansion_OI` −14.9bp, `vol_only` −18.5bp) tienen signo PERDEDOR. **Oracle** (rankear por retorno futuro real) da +298bp CI[235,370] → confirma que el motor de ranking/portfolio funciona y detecta separación cuando existe: el resultado nulo de los 9 candidatos reales es genuino, no un bug.
- `momentum_only` mostró tendencia OOS aparente (24h: train+4.5/val+32.6/oos+48.3; 72h: train+12.6/val+19.6/**oos+99.8**) pero se desarma por pata: long_top train−30.8/val+69.3/**oos−29.2** (se invierte 2 veces), N=1 oos=+203bp (típico de 1-2 trades extremos dominando). Es el mismo evento de dispersión de 2026Q3 aislado en R13 reapareciendo, no una prima estable.
- **VERDICT: FAILED (las 10 hipótesis). MAX VALIDATED: $0/mes.**
- **Qué aprendimos:** 14 rounds, ~40 mecanismos (precio/taker/funding/OI nivel-aceleración/L-S retail-vs-smart/beta/momentum/dispersión/interacciones cross-sectional) sobre 15m-72h no producen alpha capturable en este universo. El único "retorno grande" del dataset sigue siendo 2026Q3, que ningún mecanismo anticipa ex-ante.
- **PRÓXIMO EXPERIMENTO (Round 15, último, 2 partes):** (1) retest acotado de mejores candidatos previos (dOI-UP R10/11, momentum_only-72h) EXCLUYENDO 2026Q3 (test de robustez, no cherry-pick); (2) si no produce nada, veredicto final con evidencia de que ≥150 USDT/mes no es alcanzable con datos/universo actuales + qué inversión de datos haría falta (decisión del usuario).
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-16 (23) — ROUND 13: DRIFT ALTS vs BTC (market-neutral) → FAILED
- `agent/backtest/r13_altbeta.py`. Sin descargas nuevas. 45 símbolos, rebalanceo semanal (56 rebalanceos, 2025-06→2026-08). Beta causal 30d vs BTC con hedge explícito (book llevado a beta-neto-0), residual-momentum 14d, ambos sentidos evaluados ex-ante, placebo de bucket aleatorio con el mismo hedge. Deliverable: `ALTBETA_MARKETNEUTRAL_ROUND13.md`.
- **Betting-against-beta**: OOS −1310bp, se invierte completamente según qué dirección se elige (la espejo da +1310). **Placebo aleatorio (+149.5bp, PF 1.74) igual o mejor que el portfolio "inteligente"** → criterio explícito de FAILED del brief. Sim económica: −$16 a −$48/mes (1x-3x lev), TODAS negativas.
- **Residual-momentum cross-sectional**: mismo patrón — OOS −506bp inestable, placebo mejor, sim económica −$11 a −$32/mes.
- **Hallazgo:** descomponiendo RAW / HEDGE-LEG / HEDGED se vio que el "drift" de R12 no era beta ni momentum — es **dispersión cross-sectional extrema concentrada en 2026Q3 (últimas 6 semanas del dataset)**: 2025Q3=+289/2025Q4=−82/2026Q1=−26/2026Q2=−290/**2026Q3=+1737** en el placebo. Cualquier book long-short (bien elegido, mal elegido, o al azar) queda dominado por ese evento único. El componente de *selección* no aporta nada. $450/11 patas semanales es además capital-ineficiente (nocional $41-123/pata, costo 24bp/sem pesa fuerte).
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED: $0/mes** (mejor backtest real −$11/mes).
- **PRÓXIMO EXPERIMENTO (Round 14, único):** dispersion-regime timing — exponer el book long-short SOLO en semanas de dispersión cross-sectional anormalmente alta (causal, ex-ante), plano el resto. Ataca lo que R13 reveló: el edge (si existe) está en el timing, no en la selección.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-15 (22) — ROUND 12: SMART MONEY DIVERGENCE + OI ACCELERATION → ambos FAILED
- `agent/backtest/r12_smartmoney.py`. Sin descargas nuevas (14.5 meses ya ingeridos, universo 45 símbolos igual que R11). Dos mecanismos testeados con el mismo rigor (TRAIN/VAL/OOS, placebos, regresión incremental, sim económica causal `open[t+1]` con capital ≤450). Deliverable: `SMARTMONEY_OIACCEL_ROUND12.md`.
- **M1 Retail-vs-Smart-Money** (`D=z(global_ls_acct)-z(toptrader_ls_pos)`, extremos P90-99, ambos lados): **FAILED**. Signo se invierte TRAIN/VAL/OOS. **Placebo positivo explícito**: matched-control/time-shift+24h/random-ranking reproducen el efecto casi exacto (ej. real −7.4bp @4h vs matched −3.3bp, time-shift −7.3bp, random −6.9bp) → no es la divergencia, es drift genérico de la muestra. Regresión incremental: `t(D)` significativo (n=497k) pero `ΔR²=0.00001` — cero economía. Sim: 15/16 configs pierden dinero, mejor caso +$29/mes con mediana mensual negativa.
- **M2 OI Acceleration** (2da diferencia de dOI, confirmado/divergente): **FAILED**. Mismo patrón — signo no estable TRAIN/VAL/OOS, mejor caso +$35/mes (dirección fijada por TRAIN, evaluada honestamente en VAL+OOS) con mediana mensual negativa.
- **Hallazgo metodológico:** en ambos mecanismos el retorno forward crudo de eventos extremos es sistemáticamente negativo y los 3 placebos lo reproducen casi exacto → es el drift estructural del universo (altcoins medianas/chicas depreciándose en la ventana dic-2025→ago-2026), no un efecto de posicionamiento. Confirma que el protocolo de controles funciona correctamente.
- **¿Estrategia ≥150 USDT/mes? NO. MAX VALIDATED MONTHLY PNL: $0.**
- **PRÓXIMO EXPERIMENTO (Round 13, único):** testear el drift altcoin-vs-BTC como prima de riesgo sistemática (canasta corta alta-beta financiada BTC/USDT, no evento de señal) — es la pista que dejaron los placebos de R12. Si tampoco alcanza $150/mes estable → declarar con evidencia que el objetivo no es alcanzable con datos/universo actuales.
- Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-13 (21) — ROUND 11: dOI ECONOMIC RESCUE-OR-KILL → FAILED (dOI muerto)
- **Backfill ejecutado en el round** (no pospuesto): `agent/download_klines_back.py` + `download_oi_metrics.py` (env OIM_D0/D1) → `klines_clean` 15m y `oi_metrics` 5m extendidos a **2025-06-01 → 2026-08-17** (14.5 meses) desde `data.binance.vision`. oi_metrics 6.8M filas; 45/63 símbolos con cobertura completa desde 2025-06 (9 sin OI pre-dic-2025, listaron después).
- **Análisis** `agent/backtest/r11_stationarity.py`: re-corrido EXACTO del test de régimen R10 (misma def A/D, régimen BTC ±1.5%, mismos horizontes/controles) sobre **universo restringido a los 45 símbolos que ya cotizaban antes de TRAIN** (drop 18 listados a mitad de muestra — incluidos LAB/ESPORTS/GIGGLE, que eran los top-concentración de R10). + estacionariedad por mes/trimestre/mitad + OLS beta-control + leave-top-k + sim económica causal `open[t+1]` con capital ≤450, concurrencia y funding real. Deliverable: `DOI_ECONOMIC_RESCUE_ROUND11.md`.
- **VERDICT: FAILED.** La extensión de historia **empeoró** el caso, no lo rescató:
  1. **Estacionariedad FAILED:** el signo del efecto @4h oscila mes a mes (−26 bp jul-2025 → +46 bp ago-2026, cruza 0 ≥4 veces) y **era del signo opuesto antes de 2026** — régimen UP mitad-1 = **−12 bp**, mitad-2 = **+54 bp**; jul-2025 UP = −75 bp. Lo de R9/R10 era un fenómeno exclusivo de la 2ª mitad de 2026.
  2. **Concentración FAILED:** aun con 45 símbolos limpios, quitar top-5 baja el edge UP∪FLAT@4h de 12.2 → 3.7 bp (−70%).
  3. **Magnitud económica FAILED:** sim causal + RT 24 bp + funding real + capital ≤450 → TODO pierde dinero salvo "solo UP" (selección post-hoc), que da máx **+$77/mes** (8h, 1 slot $450, PF 1.82, 9/14 meses+) — < 150, tail-dependiente (P50 mensual +$27, P95 +$382).
- **Conocimiento que queda:** asociación estadística real dOI↔retorno en régimen UP (OLS `t(dOI) = −7 a −9`, ΔR² +1–2%, replicada en 14.5 meses) pero con **signo económico NO estacionario** → no operable. **dOI MUERTO.** No se rescata con features (dOI+funding/taker/L/S/ML): prohibido por brief y sin sentido (el problema es el signo, no las features).
- **¿Estrategia ≥150 USDT/mes? NO. Máximo PnL validado: $0/mes.**
- **PRÓXIMO EXPERIMENTO (Round 12, único):** divergencia **posicionamiento retail vs smart-money** — `global_ls_acct` (L/S de CUENTAS = retail crowd) vs `toptrader_ls_pos` (L/S de POSICIÓN de top traders). Ambos ya en `oi_metrics`, 5m, 14.5 meses, 45 símbolos. Mecanismo económicamente distinto de ΔOI. Mismo rigor R10/R11, kill inmediato si falla estacionariedad/capturabilidad. NO variaciones de dOI.
- Colectores OI (PID 15164) + liq (PID 32924) vivos. Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-12 (20) — ROUND 10: dOI REGIME BREAK TEST → sigue PARK (motivo re-entendido)
- `agent/backtest/r10_doi_regime.py` + `r10b_econ.py`. Test de régimen BTC (UP/FLAT/DOWN, threshold ±1.5% sobre BTC trailing-24h, congelado ANTES) del efecto dOI de H13-C A/D (SHORT). TRAIN/VAL/OOS, cluster bootstrap, matched control por régimen, residual-BTC, OLS beta-control, placebo random-bars por régimen, per-symbol leave-top-k, sim económica `open[t+1]`. Deliverable: `DOI_REGIME_BREAK_ROUND10.md`.
- **La lectura de R9 era INCORRECTA.** El efecto NO es beta de régimen bajista. Descompuesto por régimen: el dOI-SHORT vive en **BTC UP y FLAT** (A+D: +22/+38/+71 bp @2h/4h/8h, CI excl 0) y **DESAPARECE en BTC DOWN** (CI incluye 0 en todo ≤8h; con threshold ≥1.5 el signo DOWN incluso se invierte a −35 bp @2h).
- **Control de beta (OLS `signed_fwd ~ dOI + btc_fwd + mkt_fwd + retlong + rv`):** en **UP** `t(dOI)=−10.2` y **ΔR²=+4.8%** → información incremental REAL, la más fuerte del proyecto. En **FLAT** `t(dOI)≈0` → el efecto FLAT es beta-timing de la cesta (los eventos se agrupan en pequeños techos/pisos y todo revierte), NO dOI. En DOWN, nada.
- **Placebo limpio:** barras aleatorias en el mismo régimen ≈ 0 bp (FLAT −0.5/+1.2/+1.6). El efecto separa +44/+88 bp por encima del baseline del régimen.
- **PERO falla CANDIDATE por 3 motivos:** (1) **estabilidad temporal** — TRAIN (dic-2025→25-abr) ≈ 0 bp a 2h/4h; todo el efecto es de la 2ª mitad (may–ago); (2) **concentración** — quitar top-7 de 63 símbolos baja UPFLAT@4h de 42→7.5 bp (−82%); mediana por símbolo 10 bp < costo 24 bp; (3) **magnitud "real"** — mediana de trade NEGATIVA, WR<0.5, PF 1.1–1.24 → PnL de cola derecha.
- **VERDICT: PARK.** **ECONÓMICO: NO** se puede afirmar ≥150 USDT/mes con ≤450 USDT (sim en papel $100–500/mes @4h pero mediana negativa + ~960 eventos/mes no capturables + ausente en 1ª mitad).
- **PRÓXIMO EXPERIMENTO (Round 11, único):** TEST DE ESTACIONARIEDAD. Backfill de `oi_metrics` + klines 15m **hacia atrás** desde `data.binance.vision` (métricas ≥2025-11; intentar 2025-06→11) y re-correr EXACTAMENTE el test R10 con **universo = los ~40 símbolos que ya cotizaban en dic-2025** (elimina el vaciado de TRAIN por los ~20 listados a mitad de muestra). Si el efecto sigue apareciendo solo post-abr en ventana ≥12 meses → FAILED (no estacionario). Si aparece parejo → re-evaluar concentración → CANDIDATE.
- Colectores OI (PID 15164) + liq (PID 32924) vivos. Sin tocar producción / perfiles / SL-TP / agente / capital.

## 2026-09-11 (19) — ROUND 9: OI HISTORICAL ALPHA HUNT → edge está en ΔOI, pero PARK
- **Ingesta:** `agent/download_oi_metrics.py` + `agent/download_funding_hist.py` (solo `data.binance.vision`, nunca api/fapi). Tablas nuevas en `binance_vision_clean.db`: `oi_metrics` (4.34M filas, OI 5m + L/S ratios + taker ratio, 63 símbolos, 2025-12-01→2026-08-17) y `funding_hist` (74.9k filas, funding 8h). Universo EX-ANTE congelado `research/universe/oi_universe.json` (63). **Validado**: 0 filas fuera de grilla 5m, `create_time`=inicio de barra, PK sin dups, ~48/63 con los 8.5m completos (resto 100% desde su listing), reconciliación `sum_oi` vs colector live (CAPUSDT, 3831 pts) = −0.7 ppm.
- **Análisis** (`agent/backtest/r9_oi_alpha.py`, panel 15m, TRAIN/VAL/OOS, cluster bootstrap, placebo +24h, matched control, OLS incremental, costo 20 bp). Deliverable: `OI_HISTORICAL_ALPHA_ROUND9.md`.
- **H13-C (4 cuadrantes price×ΔOI):** B (p↓oi↓) y C (p↑oi↑) = **FAILED** (planos). **A (p↓oi↑) y D (p↑oi↓) = PARK**: efecto incremental de ΔOI REAL — matched-control con OI plano ≈ 0 vs cuadrante −13 a −17 bp @2h; OLS `t(dOI)` hasta −7.7; residual-BTC ≈ raw; VAL y OOS mismo signo (ambos → precio BAJA). PERO net-positivo solo a 4–8h holding, **ausente en TRAIN** (posible régimen), `symPos` 0.33–0.49, `conc` ~0.45, placebo filtra 30–40% a ≥8h, y ambos cuadrantes → SHORT (sesgo direccional).
- **H15 (OI×taker×price, 8 estados):** **FAILED**. OLS `t(z_tbr)` ≈ 0 a TODO horizonte → el taker flow **no aporta nada** incremental sobre precio+OI. Premisa refutada; lo que sobrevive = el efecto de ΔOI (=H13-C).
- **H16 (funding×OI):** **FAILED**. OLS `fwd ~ z_fund + dOI + retlong`: `t(z_fund)` nunca significativo; `retlong` (momentum 24h) domina (t hasta +30). Funding extremo solo describe el momentum del crowd; OI no aporta incremental sobre funding.
- **M15 (reaction-beta persistente ante shocks BTC):** **FAILED**. Persistencia corr(TRAIN,OOS)=0.61 (tendencia real) pero el portfolio congelado under/over-reactors: spread OOS −2.6 bp @2h ≈ **ranking aleatorio −2.4 bp**; signo cambia por horizonte; `conc`=1.0 `symPos`=0 (7+7 símbolos se mueven todos juntos = crash del mercado, sin independencia cross-sectional); solo 21 símbolos califican.
- **RESULTADO CENTRAL:** el edge incremental de posicionamiento está **100% en `dOI` (cambio de Open Interest)** — NO en taker flow ni en funding (ambos `t≈0` en OLS). El efecto ΔOI es reproducible VAL→OOS pero NO alcanza el umbral económico (≥150 USDT/mes) tal como está → **PARK**. Avance durable: OI histórico 5m ingerido+validado; H15/H16 cerradas.
- **PRÓXIMO EXPERIMENTO (Round 10, decidido):** test de régimen de `dOI` — recomputar H13-C A/D y su OLS separando barras por régimen de BTC (up/down/flat). Kill si `dOI` solo predice en down/flat y se apaga/invierte en up (= beta de régimen). Si sobrevive los 3 → análisis por símbolo + net con funding real + entrada `open[t+1]`. Sin explorar aún con el OI nuevo: `toptrader_ls_pos` / `global_ls_acct` (ratios L/S) y aceleración de OI.
- Colectores OI (PID 15164) + liq (PID 32924) vivos. Sin tocar producción / StrategyProfiles / agente / SL-TP / capital.

## 2026-09-10 (18) — ROUND 8: M18 KILL-OR-CONFIRM → FAILED + data-expansion unlock
- Reconstrucción causal y paranoica de M18 (perp de acción tokenizada vs apertura RTH del cash US → reversión del gap). `agent/backtest/r8_m18_kill.py` + `scratch_r8_m18_kill.json`. Deliverable: `M18_KILL_OR_CONFIRM_ROUND8.md` (17 secciones). Regla: costos determinados ANTES (~26 bp RT: taker 10 + spread 10 + slip 6; maker NO asumido), todo pre-declarado, 0 params fiteados.
- Universo ex-ante: 35 símbolos accion/ETF US (excl XAU/XAG/SPX), 2 837 eventos, rango 2025-12-08 → 2026-08-17. 5m OHLCV, entrada `open[t0+1]` (RTH_open+5min, estrictamente post-señal). Self-tests DST (EST/EDT), feriados NYSE, early-closes, causalidad. Cluster bootstrap sobre símbolos, TRAIN/VAL/OOS 50/25/25, 5 controles + OLS incremental.
- **VERDICT: FAILED.** Se cumplen TODAS las kill conditions: (1) **control C4** (misma señal, forward medido **+24h después**) reproduce el efecto entero (+5.9/+9.6/+12.2 bp @30/45/60m, CI excl 0) → el open RTH NO es incremental, es mean-reversión lenta visible con cualquier offset de 24h; (2) **signo se invierte** TRAIN +5.8 @10m / VAL −5.4 @30m −11.5 @60m / OOS +17 @30m; (3) **concentración** top5_conc 0.8–1.0 en TODOS los estratos de liquidez (1–2 símbolos cargan el PnL); (4) mejor gross pooled +8 bp @30m (CI apenas excl 0) vs costo RT ~26 bp → **net −18 bp**; (5) OLS `t(β_gap)` = 0.48/0.98/0.11 (no significativo; a 5–10m negativo, wrong sign); (6) **NO monotonía** en buckets de |gap| (el bucket 0.5–1.5% está muerto); (7) monetización ≈ **−$8/mes**. El "edge" de R7 era artefacto de barras 15m + entrada en el print de apertura (no ejecutable, ~7–9 bp vivían ahí) + cortes de tercil post-hoc + 3.5 meses dominados por la 1ª mitad. **M18 cerrada — no más variantes** (brief §18).
- **DATA EXPANSION (hallazgo verificado en vivo):** `data.binance.vision/data/futures/um/daily/metrics/<SYM>/<SYM>-metrics-YYYY-MM-DD.zip` **es accesible desde este entorno** (200, ZIP, sin tocar api/fapi — mismo CDN que construyó `binance_vision_clean.db`). Descargado y verificado `BTCUSDT-metrics-2026-08-01.zip`: 288 filas 5m/día, cols `create_time,symbol,sum_open_interest,sum_open_interest_value,count_toptrader_ls_ratio,sum_toptrader_ls_ratio,count_ls_ratio,sum_taker_ls_vol_ratio`. → **desbloquea OI histórico 5m para TODOS los perps en la ventana congelada (dic 2025 → ago 2026)** = H13-C formal + H15 + familia OI HOY, con breadth completo y régimen bajista real, sin esperar al colector live de octubre. Checklist de validación (cobertura, alineación de timestamp, reconciliación con tabla `open_interest` live, funding equivalente) en el deliverable.
- **Próximo experimento (automático, Round 9):** **M15** — factor de "reaction-beta" persistente: ranking de over/under-reacción de cada símbolo ante shocks comunes (BTC/market-wide), ¿persiste entre shocks? Datos en mano. No es catch-up (H18 FAILED) ni momentum (H8 FAILED).
- **Colectores:** OI (PID 15164) + liquidaciones (PID 32924) vivos y frescos (2026-09-10 15:17 UTC, 0h atraso), desacoplados del agente. Sin cambios de infraestructura (regla del round).
- **Sin tocar producción**: StrategyProfiles, setup_validator, entry/SL/TP/slots/ejecución, trades.

## 2026-09-10 (17) — ROUND 7: BREAK THE SEARCH SPACE → 3 FAILED / 3 PARK (lead: M18)
- Metodología: dejar de derivar familias del plano OHLCV+taker. Buscar mecanismos *evento → actor obligado → desequilibrio → reacción monetizable*, ranking por expected monetizable alpha (no p-value). Deliverable: `ALPHA_HUNT_ROUND7.md` (9 secciones: current state, exhausted space, new data sources, 20 hipótesis de mecanismo, ranking económico, top-3, resultados, verdict, next move). 4 experimentos reales. Scripts: `agent/backtest/r7_common.py`, `r7_m10_absorption.py`, `r7_m11_btc_leadlag.py`, `r7_m8_funding_event.py`, `r7_m18_rth_open.py`. Scratch: `scratch_r7_*.json`.
- **M10** (absorción de 1 barra: flujo taker masivo unilateral + precio ~0 → release/continuación): **FAILED**. Efecto 1–3 bp (≪ 16 bp), matched-control "flujo que SÍ movió el precio" domina, quiet-only (barra tranquila sin flujo) reproduce, symPos < 0.5.
- **M11** (BTC 5m shock → catch-up de alts rezagados en 5–30 min; el hueco entre H9.1 sub-segundo y H18 1h): **FAILED**. Efecto real +4 bp @5m pero `uncond` (todos los alts tras el shock) ≈ `real` → es beta de mercado continuando, no lead-lag de rezagados. 4× < costo, decae a 15 min.
- **M8a** (funding extremo en el settlement → unwind contra el lado que paga): **FAILED**. Placebo +48h y matched-control reproducen 60–70%; es drift de un tramo alcista × convención de signo short (mismo modo de falla que advierte el brief). symPos 0.33.
- **M8b** (funding sign-flip +↔− → momentum de posicionamiento): **PARK**. Hint @24h (+32 bp tras costo, CI excl 0) pero placebo se come la mitad, decae entre mitades, sólo 38 d de funding history.
- **M1** (listing forced-flow → reversión post-launch): **PARK (datos)**. Hygiene analysis: los first-appearance de klines_clean se agrupan en el día-1 de cada mes (72/129) = artefacto de colección; sólo ~20 listings crypto-native con firma limpia (vol/rango primer día >> después). n insuficiente para monetizar. Necesita listing dates verificados.
- **M18** (perp de acción tokenizada — AAPLUSDT/NVDAUSDT/COINUSDT/… — vs apertura del cash US 13:30 UTC → convergencia): **PARK (lead vivo)**. Reversión parcial del drift nocturno: +10 bp @30–60m, CI excl 0, placebo-mediodía limpio, **escala monótonamente con el tamaño del gap** (tercil grande: +25 bp @60m). Contra: < 16 bp costo genérico, concentrado (top5 ~0.5), decae entre mitades, 3.5 meses, calendario US aproximado. Es el único mecanismo del round con firma real.
- **VERDICT: NO PROMISING (3 FAILED, 3 PARK).** NO es "no alpha found": M18 es un lead, y M1/M8b/H13-C/H14/H15 están gateadas por datos **backfilleables desde `data.binance.vision`** (`futures/um/.../metrics/` = OI histórico, `/monthly/fundingRate/` = funding ≥90d) SIN tocar la API baneada — palanca de mayor impacto del proyecto ahora.
- **PRÓXIMO EXPERIMENTO (decidido):** re-correr M18 en `klines_5m` (entrada 13:30, salidas 5–60 min), con fees reales de los contratos de equity tokenizada (taker ~4.5 bp/lado, o maker ~2 bp con entrada límite) en vez de 16 bp genérico, subset de ~12 nombres líquidos, y condicionando el signo del gap vs la última sesión RTH real. Gate: neto >8 bp en ≥10 nombres con mitades estables → validación completa; si no → FAILED. En paralelo: verificar backfill de `data.binance.vision/metrics` + `/fundingRate`.
- **Colectores:** OI (PID 15164) y liquidaciones (PID 32924) vivos y frescos (2026-09-10 01:2x), desacoplados del agente (muerto). funding/whale/OFI collection SÍ está acoplada al agente → parada desde 2026-09-09 15:37. No se tocó más infraestructura (regla del round).
- **Sin tocar producción**: StrategyProfiles, setup_validator, entry/SL/TP/slots/ejecución, trades. Research únicamente.

## 2026-09-09 (16) — ROUND 6: ADVERSARIAL ALPHA HUNT → NO ALPHA FOUND
- Metodología adversarial: no seguir la lista H13–H16, sino buscar mecanismos de mercado NO probados con estructura **condicional/interacción** (A ocurre + B confirma + C determina dirección). 4 hipótesis nuevas, 5 experimentos reales, 5 resultados reales. Deliverable: `ALPHA_ADVERSARIAL_HUNT_ROUND6.md` (secciones A–H). Scripts: `agent/backtest/r6_adversarial_screen.py`, `r6_h22_validate.py`, `r6_h23_screen.py`, `r6_h13c_majors.py`.
- **H21** (volume-climax + one-sided + sin follow-through → reversión): **FAILED**. Efecto −1.6 a −5.8 bp (el climax continúa), CI cruza 0, placebo ≈ real, control CON follow-through +8 a +21 bp → el conditioning es anti-predictivo.
- **H22** (agresión taker vs precio realizado → reversión): **FAILED**. El screen dio +24 bp @15m / +72 bp @60m → escepticismo escalado → **bug de LOOKAHEAD**: `np.convolve(a, np.ones(W), "full")[W-1:]` sumaba la barra actual + 3 FUTURAS de taker-flow. Fix aplicado (`np.convolve(a, np.ones(W))[:n]`, backward) en ambos scripts. Limpio, entrada a open[t+1], slippage explícito: BULL inexistente, BEAR +6 bp @15m (CI apenas > 0, front-loaded al tercio 1, symPos 0.56) < piso de costos. Pierde ~$20-28/mes. Mecanismo refutado.
- **H26** (vol-expansion + taker confirmation → continuación): **FAILED**. Efecto −1 a −7.3 bp < costos; el flujo taker no discrimina continuación de reversión.
- **H23** (perp-taker apalancada sin respaldo spot → reversión): **NOT VIABLE**. Solo 78 sym con spot+perp+taker; `lev_long` 0 eventos en 7.6 meses. En nombres líquidos perp/spot van arbitrados, la divergencia no se forma a 1h.
- **H13-C majors** (sub-screen BTC/ETH/SOL, el "próximo experimento" de Round 5): **PARK data-gated**. 0 eventos — solo ~7 d de OI de majors, ventana z de 2 d no alcanza ±1. Confirmado: H13-C no es testeable en majors hasta que el colector acumule.
- **VEREDICTO: NO ALPHA FOUND.** El plano OHLCV + taker-flow está agotado también para estructuras condicionales. Ningún experimento local tiene expected value positivo. Datos que faltan: (1) taker-flow FORWARD — barato, se levanta ya; (2) OI ≥70 d con tramo bajista → ~oct; (3) liquidaciones ≥60 d → ~nov; (4) fuente nueva (OI multi-venue / opciones-greeks / on-chain) = decisión de inversión del usuario.
- **PRÓXIMO EXPERIMENTO (decidido, no se le pregunta al usuario):** levantar un colector de **taker-flow forward** (`taker_flow_forward` en `klines.db`, desde el archivo diario de data.binance.vision que ya trae `taker_buy_volume`) para desbloquear H15 (evento conjunto OI↑ × taker-sell × precio↓) cuando OI llegue a 70 d. En segundo plano: protocolo formal de H13-C con split TRAIN/VAL/OOS cuando la profundidad de OI lo permita.
- **Colectores:** OI (PID 15164) y liq tracker (PID 32924) relanzados tras caerse ~15:30 UTC con el agente. Task Scheduler sigue **UNVERIFIED** (Acceso denegado sin elevación). Comando para el usuario (PS elevado): `powershell -ExecutionPolicy Bypass -File "C:\Users\Nicolas\Desktop\Verge\Verge\research\ops\register_research_tasks.ps1"`.
- **Sin tocar producción**: StrategyProfiles, setup_validator, entry/SL/TP/slots/ejecución, trades. Research únicamente.

## 2026-09-09 (15) — ROUND 5: ALPHA DISCOVERY FROM ZERO (cambio de misión)
- Nueva misión: descubrir 1–3 estrategias desde cero (≥150 USDT/mes neto, ≤450 USDT c/u). Legacy (MA Slope Caso 3 / FVG-15m / ARROW-PEAK) = congeladas NON-CANONICAL, no se retunean más. Research desacoplado de producción (backend .NET / agente pueden estar caídos, no bloquea).
- Deliverable: `ALPHA_DISCOVERY_ROUND5.md` (secciones A–H: alpha map, exhausted classes, hipótesis rankeadas, screening, failed, promising, data requirements con fechas, cola de investigación). Ledger actualizado.
- **Inventario de datos 2026-09-09**: OI 36 d (solo 13 sym ≥90% cob; BTC/ETH/SOL con 7 d); `liquidations_research` 0.6 d; OFI 30 d; funding ~2 m; OHLCV limpio de binance_vision CONGELADO 2026-08-17 → overlap OHLCV×OI de alta calidad = solo ~13 d.
- **Screening H13-C** (re-corrida `h13_pilot.py` con 36 d): idéntico al piloto — A/B/D FAILED (placebo reproduce el drift), **C (precio↓+OI↑) a 1–4h sigue vivo pero PARK** (raw +56–59 bp placebo-limpio, pero incremental vs matched IC cruza 0; solo 90/1517 eventos en régimen bajista). Sin cambios; re-evaluar con OI ≥70 d (~2026-10-13) + tramo bajista real.
- **Screening H18** (BTC shock → catch-up, `h18_btc_shock_screen.py` + `h18_fast.py`, 8.5 m OHLCV, 264 shocks / 360 alts / 14.270 laggard): **FAILED**. Laggard fwd +6.2 bp @1h (3× < costo), −2.8 @2h, **−13.3 @4h CONTRA la dirección**. Sin persistencia, inestable entre mitades. El "edge vs follower" es reversión del follower, no catch-up. **Cerrado — confirma agotamiento de OHLCV.**
- **Colectores de research CAÍDOS** (OI + liq tracker) desde ~15:30 UTC junto con el agente → **relanzados** (OI PID 15164 backfill 2d; liq tracker PID 32924). ~2.7 h de liquidaciones perdidas irreversiblemente. Confirma: sin Task Scheduler no sobreviven (el liq tracker corría como thread del agente + standalone, murieron todos).
- **Familias AGOTADAS confirmadas** (no repetir): todo OHLCV continuo, H8=momentum, H9.2 real-no-monetizable, H10=funding, H12 cerrado, OFI-filtro AUC 0.46, funding-standalone AUC 0.539, y el MOTOR DE BACKTEST para estimar rentabilidad (Gate V4 FAILED).
- **Cola (máx 3):** (1) asegurar colección de H14 (liquidaciones — cada día perdido es irrecuperable, Task Scheduler pendiente); (2) H13-C congelada esperando datos + sub-screen majors-only sobre los 7 d de OI; (3) screening OHLCV de sanidad, y si falla → decisión de inversión en datos (taker-flow forward / liquidaciones pagas / opciones).
- **Sin tocar**: producción, StrategyProfiles, motor live, ejecución. El agente sigue detenido (crasheó por backend down, Round 5-operativo).

## 2026-09-09 (14) — ROUND 5: desbloqueo operativo (UPDATE MinConfluenceScore)
- **Cambio aplicado** 2026-09-09 ~17:47 UTC: `UPDATE "StrategyProfiles" SET "MinConfluenceScore"=70 WHERE "IsActive" AND "Name" IN ('MA Slope Caso 3','FVG - 15m','ARROW-PEAK')`. **3 filas** (ARROW-PEAK 36ad567e, FVG - 15m cd74d9cf, MA Slope Caso 3 d988cdc6). Antes: `60` en las 3. Después: `70` en las 3.
- **Verificado**: diff campo-por-campo pre/post → SOLO cambió `MinConfluenceScore`; ningún otro campo, `ConcurrencyStamp` y `LastModificationTime` sin tocar. Evidencia en `.claude/evidence/strategyprofiles_pre_round5_*.jsonl` + `_post_round5.jsonl` + `round5_agent_baseline.txt`.
- **El agente re-lee perfiles cada ciclo** (`verge_agent.py:814` Step 1/6) → no requiere reinicio para tomar el 70.
- **PERO no se pudo observar el efecto**: el agente (PID 25848) **CRASHEÓ 2026-09-09 12:37:51 -03 (15:37 UTC)** con `FATAL: Could not authenticate with ABP Backend. Stopping.` — **el backend .NET (`Verge.HttpApi.Host`, localhost:44396) está caído** (nada escuchando, curl→refused; venía flaky desde 2026-09-05 22:08). Es la "siguiente causa real" (criterio #5 del round). NO se tocó nada más: backend/agent los levanta el usuario (VS F5). Baseline pre-cambio: 1305 vetos `low_confluence_for_tier3` / 3000 líneas, 0 trades, 96 candidatos MA-pattern/ciclo.

## 2026-09-09 (13) — DIAGNÓSTICO: el agente no abre posiciones hace 2 días
- Revisado `agent/logs/agent.log` (PID 25848, arrancado 2026-09-06 21:00). **0 trades desde el restart; 187.710 vetos `low_confluence_for_tier3`.**
- **Causa raíz (config, NO código de research):** las 3 estrategias activas (MA Slope Caso 3, FVG-15m, ARROW-PEAK) son perfiles RECONSTRUIDOS (`reconstruct_db.py`, post factory-reset) con `MinConfluenceScore=60`. El motor (`setup_validator.py:1361`) vetea todo candidato Tier-3 con score < 65. `_evaluate_ma_geometry_profile` stampea `score=minConfluenceScore=60` → 100% veto. ARROW-PEAK score 85 pero su patrón casi nunca dispara.
- Secundario (menor): cascade único de circuit breakers + `[MSF] All exchanges failed` intermitente (solo 44/10.5k son DNS real; DNS/curl a Binance/Bybit OK ahora). No es el bloqueo.
- Reporte: `AGENT_NO_TRADES_DIAGNOSIS.md`. **NO se modificó nada** (round prohíbe tocar StrategyProfiles). Fix propuesto pendiente de OK del usuario: `UPDATE "StrategyProfiles" SET "MinConfluenceScore"=70 WHERE "IsActive" AND "Name" IN ('MA Slope Caso 3','FVG - 15m','ARROW-PEAK')`.

## 2026-09-08 (12) — ROUND 4: activación y verificación de los colectores
- Docs: `RESEARCH_COLLECTORS_HEALTH.md` (nuevo) + update de `RESEARCH_DATA_INFRASTRUCTURE.md`. Solo data collection/persistencia/auto-start/backup. NO H15/señales/producción.
- **OI collector — VERIFIED corriendo.** Killeado PID 30652 (universo viejo), relanzado con `OI_BACKFILL_DAYS=7` (env nueva, reinicio suave para no cargar la IP compartida con verge_agent que SÍ está corriendo). PID final 54348, **1 sola instancia**. Universo = 63 (`oi_universe.json`). Evidencia: **BTC/ETH/SOL presentes** (2015 filas c/u, 7 días 2026-09-02→09-09); OI 469k→584k+ filas; 283 símbolos; **`syms_ge90_fresh` 2→40**; **0 grupos duplicados** (PK); último bucket ~5 min; circuit breaker Binance CLOSED todo el tiempo (el backfill de 7d no disparó 418/429).
- **Liquidation tracker — VERIFIED corriendo.** Arrancado (`agent/_run_liq_tracker.py`, nuevo, UTF-8 forzado), PID 18672, **1 sola instancia** (se limpiaron zombies — el `nohup &` de git-bash duplicaba; se pasó a `Start-Process` de PowerShell). Bybit WS `allLiquidation`, **63 topics**. Eventos entrando a `liquidations_research` desde 2026-09-09 00:31 UTC (burst de WLDUSDT $10.7k + ARB/AKE/NEAR/TRUMP), 15 eventos / 6 símbolos, **0 duplicados**, `pct_events_persisted_ok=100`. Ritmo bajo = mercado quieto (health table + heartbeats distinguen "quieto" de "roto"). Binance forceOrder sigue muerto → Bybit es la fuente; backfill histórico IMPOSIBLE.
- **Restart-continuidad — VERIFIED en vivo** (no solo test): snapshot `_restart_test_snapshot.json` pre-kill (469.375 filas) → post-restart 542k→584k, sin reset, 0 dups nuevos.
- **Storage fuera de Docker — VERIFIED**: `agent/data/klines.db` (host FS, no volumen); ya sobrevivió el Docker reset del 2026-09-06. No se hizo reset nuevo.
- **Backup — VERIFIED ×2**: `research/backup/research_klines_20260909_003348.sql.gz` 48.109.435 B, sha256 `b89db63f44129cfecf32225a6df0463f7d3be511cf90463f2b3bf880c2641e41`; restore independiente en `:memory:` OK (open_interest 542.374 / liquidations_research 14/15 / funding 48.667 / OFI 2.24M). `_run_liq_tracker.py` fix de encoding; `.ps1` con `python -u`.
- **AUTO-START = UNVERIFIED**: `Register-ScheduledTask` y `schtasks /Create` → "Acceso denegado" (sesión NO elevada, probado). Script listo: `research/ops/register_research_tasks.ps1` (5 tareas, correr en PS elevado).
- **REBOOT = UNVERIFIED**: no se puede rebootear el host en la sesión. Los colectores corren como procesos detached (sobreviven cierre de shell, NO reboot hasta registrar las tareas).
- **OHLCV estático off-host = UNVERIFIED**: sin disco externo. Comando listo (`--copy-static`). Nada movido/borrado.
- **Pendiente usuario**: (1) `research\ops\register_research_tasks.ps1` en PS elevado; (2) reboot de verificación; (3) `--copy-static` a disco externo.

## 2026-09-07 (11) — RESEARCH DATA INFRASTRUCTURE (OI + liquidaciones)
- Doc: `RESEARCH_DATA_INFRASTRUCTURE.md`. Solo infra de datos: NO H15, NO señales, NO producción/StrategyProfiles/DB-prod/motor-live/validation-protocol. H13=PARK, H14=congelado.
- **Universo de research ex-ante** (`research/universe/build_oi_universe.py` → `oi_universe.json`, 63 símbolos): Tier 0 = BTC/ETH/SOL obligatorio; Tier 1 = top-60 perps por mediana de dollar-volume 5m sobre 30d locales (binance_vision), cobertura ≥95%. Excluye ex-ante perps de acciones/commodities (XAU/SOXL/MSTR/COIN/...). **NO usa resultados de H13.**
- **OI collector** (`agent/open_interest_collector.py`): re-apuntado de `config.WATCHLIST_TIER1` (~30 microcaps, sin majors) al universo de research vía `load_research_universe()`. Persistencia ya era buena (PK `(exchange,symbol,period,timestamp)` + UPSERT idempotente, timestamps de Binance alineados+no-futuros, prune keep_days=400). Corre como proceso del HOST (no container). **UNVERIFIED que colecte el universo nuevo** (no reinicié — IP compartida con agente live). ETA gate 70d ≈ **2026-10-13** (34.38d hoy).
- **Liquidaciones — fix crítico**: `kline_cache.py:844 prune_old_liquidations(keep_hours=24*7)` borra >7d cada reconexión → destruía la historia. AHORA: tabla nueva **`liquidations_research`** (append-only, PK dedup `(venue,symbol,timestamp,side,price,qty)` + `ON CONFLICT DO NOTHING`) + `liq_collector_health` (connect/disconnect/heartbeat). `prune_old_liquidations` acotado a `liquidations` (live cache de prod). `liquidation_tracker.py` re-apuntado al universo de research (Bybit, `allLiquidation`), escribe a AMBAS tablas, registra salud. **El tracker NO está corriendo** — cada día sin correr = dato perdido (Bybit sin REST histórico, backfill IMPOSIBLE).
- **Monitores** (`research/data_quality/`): `oi_quality_monitor.py` + `liquidation_quality_monitor.py` → reportes JSON+MD (cobertura por símbolo, gaps, dups, edad del último dato, gate ETAs, downtime). Ejecutados: OI 34.38d/0 dups; liq research 0 filas (tracker no corrió).
- **Backup** (`research/backup/backup_research_data.py` + `.ps1`): checkpoint WAL → dump compacto `.sqlite` + `.sql.gz` portable → verifica restore en `:memory:` → rota 14. Ejecutado y verificado independiente: `.sql.gz` restaura open_interest 458463 / orderbook_ofi 2.24M / funding 48667 / etc. OHLCV estático (5.6GB) = copia one-shot `--copy-static <disco>` (UNVERIFIED, pendiente usuario).
- **Docker independence**: `klines.db` + `binance_vision_clean.db` viven en `agent/data/` del HOST (no volúmenes) → ya sobrevivieron el Docker reset del 2026-09-06 que sí borró Postgres. El único riesgo real es reboot del host matando los colectores → auto-arranque en Task Scheduler = **UNVERIFIED/pendiente** (necesita PS elevado).
- **Tests** (`research/tests/test_data_integrity.py`): **15/15 PASS** — incl. "prune borra del live cache" + "**prune NO toca liquidations_research**" + "prune keep_hours=0 vacía live, research intacto" + idempotencia OI/liq + out-of-order + sobrevive reopen.
- **Pendiente usuario**: (1) reiniciar OI collector; (2) levantar liquidation_tracker; (3) programar auto-arranque + backup + monitores; (4) copia off-host del OHLCV estático.

## 2026-09-06 (10) — FASE 3: H13 pilot (PARK) + H14 freeze
- Scripts: `agent/backtest/h13_pilot.py`. Docs: `H13_PILOT_REPORT.md`, `H14_LIQUIDATION_CASCADE_PROTOCOL.md`. Sin tocar producción/DB/motor.
- **H13 PILOT = PARK.** Event study OI/precio, 1516 eventos, 45 símbolos (todos alt/smallcap — el colector NO tiene BTC/ETH/SOL), 33 días, 1h bars, params pre-declarados sin grid. Cuadrantes A(r+OI+)/B(r+OI-)/C(r-OI+)/D(r-OI-), horizontes 1/4/12/24h, placebo +48h, matched control por vol+ret_prior.
  - **A/B/D = FAILED**: el placebo temporal REPRODUCE el retorno forward positivo a 12-24h → es drift alcista de la ventana (BTC 73% flat/21% up/6% down), no efecto OI.
  - **C (precio↓ + OI↑) a 1-4h = el único hilo vivo**: rebote +56-59 bp, IC90 del raw excluye 0, **placebo NO lo reproduce** (1h placebo -24bp signo opuesto), signo estable en las 2 mitades, mismo signo en 71% de símbolos, se apaga a 12-24h. Mecanismo = short-squeeze de shorts nuevos agresivos (= H15). PERO el efecto **incremental vs matched** (lo atribuible a OI, no a mean-reversion pura) tiene **IC que incluye 0** (+32bp [-4,70]) → NOT MONETIZABLE con confianza a costos realistas (estos son nombres ilíquidos, slippage real >>5bp).
  - **Necesita para test formal**: OI ≥70d (hoy 33, ETA ~mediados oct), al menos un régimen bajista real (la ventana fue 93% flat/up), y **agregar BTC/ETH/SOL + top-20 al `open_interest_collector.py`**. Protocolo formal congelado en el reporte §8: foco Quadrant C @ 1h/4h, gate = incremental IC excluye 0 en VALIDATION Y FINAL-OOS + ≥15bp tras costos reales, o FAILED (H13+H15 juntas).
- **H14 = protocolo CONGELADO, sin correr.** Definiciones de spike/cascade/directional/abnormal fijadas (p95/p99/p99.5 intensidad + piso $50k + imbalance 0.6). Dos mecanismos rivales (CONTINUATION vs EXHAUSTION), no se asume cuál.
  - **Datos inservibles**: `liquidations` = 7d, 106 sym microcaps, notional mediano $124. **`kline_cache.py:844 prune_old_liquidations(keep_hours=24*7)` BORRA todo lo >7d cada hora** → la historia se destruye sola. Colector (`agent/liquidation_tracker.py`) es Bybit `allLiquidation` (Binance forceOrder muerto globalmente), solo suscribe ~30 símbolos TIER1, e intermitente (gap de 30h, conteos 3931→10/día).
  - **Backfill histórico IMPOSIBLE** (Bybit solo real-time, Binance muerto, data.binance.vision no publica liq, Coinglass/Coinalyze de pago).
  - **Acción requerida (research infra, no producción)**: (1) parar el prune o tabla `liquidations_research` sin prune; (2) ampliar universo a majors + top-100 Bybit; (3) supervisión de uptime; (4) 60-90d de colección forward. Test formal + gate ya congelados en el doc §5.
- **NEXT**: esperar OI ≥70d para H13 formal; decidir si se aplican los cambios de infra del colector de liquidaciones (H14) y de OI (agregar majors). Sin implementar estrategias.

## 2026-09-06 (9) — SCIENTIFIC RESEARCH RESET (solo mapa, sin implementar nada)
- 5 docs nuevos: `RESEARCH_RESET_PLAN.md`, `DATA_INVENTORY.md`, `ALPHA_HYPOTHESIS_LEDGER.md`, `VALIDATION_PROTOCOL.md`, `PORTFOLIO_CAPITAL_PROTOCOL.md`. No se tocó DB, producción ni se corrió backtest.
- **Legacy congelado como NON-CANONICAL:** la DB (3315 trades, 20 profiles reconstruidos), los reportes y `trades.csv` NO son ground truth. No restaurar estrategias borradas, no reparar PnL.
- **Data inventory (verificado por query):** binance_vision_clean.db = klines_5m/clean (8.5m, 450 sym, 2025-12→2026-08-17), spot (240), taker_flow (429, H12 FAILED), multi_exchange (bitget/bybit/okx, corta jul-25, H9 FAILED), btc_1m (78d). klines.db = klines 1h con 2.3 AÑOS (858 sym, cache del agente, lookahead risk), funding_rates (2 MESES, 496), **open_interest 33.3d/188 sym pero solo 45 ≥90% cobertura** (gate H11=70d → ETA ~mediados oct), liquidations 7d/106 (trivial), orderbook_ofi 30d/499 (AUC 0.46 FAILED), whale 3d.
- **Tesis del reset:** lo NO probado no es una fuente sino una CLASE DE MECANISMO = el evento de flujo forzado. H8–H12 midieron corr(x_t, ret_{t+h}) continua; nunca se hizo event-study de episodios de flujo forzado (liq cascades, spikes de OI, blow-ups de funding) con placebo temporal + muestra matcheada sin-evento.
- **8 hipótesis priorizadas (H13–H20):** H13 OI/price divergence (MED, piloto ya / full oct); H14 liquidation cascade continuación vs vacío (MED, sin datos, juntar 60-90d); H15 evento conjunto OI×taker-sell×precio↓ (MED-LOW, gateado por H13); H16 funding extremo × OI crowdeado (LOW-MED, overlap mínima); H17 transición de régimen de vol como evento (LOW, sanidad de pipeline); H18 catch-up de alts tras shock BTC 15m-1h (LOW-MED, sanidad); H19 OFI en ventana de evento (LOW, PARK); H20 OI en perps jóvenes (LOW, PARK).
- **NEXT:** el usuario aprueba el mapa y elige qué arranca Fase 3. Recomendado: H13 piloto (dimensionar, no concluir) + congelar protocolo H14 esperando datos. Sin implementar estrategias.

## 2026-09-06 (8) — RECOVERY auth + limpieza de 3 estrategias + BACKUPS
- **Auth roto tras el reset de Docker**: `OpenIddictTokens`/`Authorizations` vacías + el usuario `agent@verge.internal` NO existía (el reset lo borró; la reconstruccion solo seedea `admin`). El login del dashboard/agente daba 403 + 500 (`SimulatedTradeAppService.cs:583` hace `CurrentUser.Id!.Value` sin guard). FIX: recreé `agent@verge.internal` / `1q2w3E*` via `/api/account/register`, le di rol admin, moví los 20 StrategyProfiles + 3682 SimulatedTrades de `admin` (`3a238694…`) a ese usuario (`3a238b27-442d-5ee4-8f98-8722fba950e8`).
- **Historial salía vacío** aunque la API devolvía los 3682: `angular/src/app/history/history.component.ts:238-247,390` oculta trades de estrategias con `IsActive=false`, y la reconstruccion las dejó todas inactivas. Workaround: `UPDATE "StrategyProfiles" SET "IsActive"=true` (las 20). Ojo: eso las marca RUNNING → si se levanta `verge_agent.py` opera las 20 (el agente NO está corriendo ahora).
- **PnL agregado del dashboard = basura** (Nexus +$227k, FVG-15m −$107, total varía según filtro) — confirmado el problema ya documentado: la reconstruccion desde `trades.csv` tiene PnL no confiable. Lista trade-por-trade sí es real.
- **Borrado a pedido del usuario**: `DELETE FROM "SimulatedTrades"` de FVG-15m (293) + ARROW-PEAK (17) + MA Slope Caso 3 (57) = **367 trades**. Quedan 3315. Los 3 StrategyProfiles siguen existiendo, vacíos.
- **BACKUPS (nuevo)**: `backups/verge_20260906_204820.sql` (ANTES del borrado, 3682 trades — restaurable), `backups/verge_20260906_204926.sql` (después, 3315). Script `backups/backup_verge.ps1` (pg_dump con timestamp, retiene 30, usar `cmd /c` no `>` de PS por encoding). `backups/RESTORE.md` con el procedimiento + por qué el reset de Docker borra todo (volumen) + fix real (bind-mount de pgdata / nunca "factory reset"). Task Scheduler `VergeDbBackup` = pendiente (usuario, PS elevado).

## 2026-09-06 (7) — GOLDEN STANDARD Caso 3: ¿el backtester representa el sistema ACTUAL? = PARTIAL
- Reporte: `GOLDEN_CASO3_BACKTESTER_FIDELITY_REPORT.md`. Script: `agent/backtest/golden_caso3.py`. Premisa corregida por el usuario: el sistema ACTUAL NO usa timeout artificial (48/72/192/209/720h). Regla global impuesta: `NO ARTIFICIAL TIMEOUT`; trades sin resolver = OPEN/CENSORED, nunca pérdida.
- **AUDITORÍA del código de producción:** entry = precio vivo al escanear (`price_at_signal`); SL = `max(highs[-10:])×1.01` recentHigh estructural; TP = `entry − max(2.5×SLdist, 10%×entry)` (rr_target capado a `TP_MULT_TREND_FOLLOWING_MAX=2.5` porque confluence_score=80→"Trend Following", + piso min_tp_pct 10% que domina → por eso 0/2615 disparos con RR<4); sizing $150 fijo, 1x, qty=150/entry; fees 0.04% taker entry+exit; funding flat 0.01%/8h siempre pagado; slots 3/estrategia, admisión por ciclo 5m por score×bucket_calibrator (score CONSTANTE 80 para Caso 3 → orden = escaneo, no reconstruible), sin cola, 3/3→descarta.
- **CONTRADICCIÓN CLAVE:** la premisa "sin timeout" NO está en el código commiteado. El worker .NET `SimulationMarkPriceWorker` (tick 1s, ejecutor autoritativo del bracket) cierra SOLO por tp_hit/sl_hit/liquidated — sin timeout. PERO el **monitor Python del agente** (`verge_agent.py:6505-6545`) TODAVÍA tiene `zombie_timeout` (a `maxTradeDurationCandles×15min` si pierde) + `Max duration exceeded` (720h), y NO se saltan para Caso 3 (solo para is_diamond = Golden U-Turn/TOTAL-SWEEP). → **DIVERGENCIA ABIERTA:** objetivo = sin timeout; código Python = con timeout. Hay que alinear `verge_agent.py` o declarar que sí hay timeout.
- **`zombie_timeout_decision` del motor (engine.py):** réplica exacta del monitor Python, usado por `_run_generic`/`ma_slot_sim`/`_capital_sim`. Marcado **LEGACY / INVALID FOR CURRENT BACKTEST** (del sistema-objetivo sin timeout). Deshabilitado en `golden_caso3.py`. NO se ajusta, NO se elige otro valor.
- **GOLDEN REPLAY (sin timeout):** A REAL histórico (reglas viejas c/ 48h, 27/45 timeouts) net +$64.7 PF 2.54. **B señales reales + lifecycle actual sin slots:** 35 cerrados + 10 censurados, net +$45.9, **PF 1.81**, WR 49%, TP/SL 17/18 (vs 11/7 histórico — quitar timeout manda al SL lo que antes se cerraba chico). **C lifecycle actual + competencia causal 3 cupos (pool 2615 disparos):** determinista (orden escaneo) net +$20.1 **PF 1.38** WR 21%; desempate aleatorio x20 → **PF mediana 0.59, 5/20 seeds net-positivos**; + competencia entre-estrategias → **PF 0.29 WR 5%**. Censurados: MTM ≈ break-even, nunca contados como pérdida. Concurrencia máx 3/3 ✓.
- **HALLAZGO:** el PF 1.70 del análisis anterior (`CASO3_SLOT_CAUSAL_REPORT.md`) **dependía del cierre artificial a 48h**. Sin timeout y compitiendo causalmente por 3 cupos, el edge de Caso 3 NO sobrevive de forma robusta (moneda al aire hacia el lado perdedor). La premisa "Caso 3 = señal fuerte" se debilita.
- **VEREDICTO PARTIAL:** núcleo del lifecycle (entry/SL/TP/sizing/slots/fees/funding) coincide con el código; censurados correctos. Pero divergencias estructurales materiales: (1) `zombie_timeout_decision` sigue en el motor (= monitor Python, ≠ objetivo); (2) intrabar 5m TP-antes-SL vs mark-price 1s (optimista); (3) orden de admisión de slots no reconstruible; (4) 15% trades sin datos + resolución 5m; (5) funding plano irreal ambos lados. Tabla componente-por-componente en el reporte §6.

## 2026-09-06 (6) — FASE SIGUIENTE Caso 3: alpha vs slots vs timeout vs minRR = DIAGNOSTICADO
- Reporte: `CASO3_SLOT_CAUSAL_REPORT.md`. Script: `agent/backtest/phase_slot_causal.py` (precompute cacheado `scratch_slot_candstream.pkl` — 2615 disparos de patrón Caso3, 353 símbolos, universo 450; ventana 2026-07-10→08-14). Todo causal, sin optimizar, sin elegir el mejor.
- **VEREDICTO: DIAGNOSTICADO. El edge de Caso 3 SOBREVIVE la competencia causal por 3 cupos.** Política simple: "al liberarse un cupo, admitir la señal Caso3 pendiente más antigua (bar 1h ya cerrado); cerrar todo incondicionalmente a ~48h" → n=73, net **+$58.6 sobre $450 (~+13%)**, **PF 1.70**, WR 40%, maxDD −$19.7. Con competencia entre-estrategias (occupied de 3108 trades reales): PF 1.76. Desempate aleatorio x20: **20/20 seeds net-positivos**, PF P5=1.18/P50=1.46. Peor política determinista (FIFO/sym_asc): PF 1.35, igual positiva.
- **Contradice Gate V4 (PF 0.81 LOSER).** Causa de la brecha = **el modelo de timeout**, no la competencia de slots. Producción cierra por tiempo INCONDICIONALMENTE (cluster real 48.0-48.3h, 27/45 trades = timeout). El motor (`zombie_timeout_decision`, usado por `ma_slot_sim`/Gate V4) cierra a 48h SOLO si pierde y deja correr ganadores a 720h → deja que ganadores con MFE 2-3% hagan round-trip al SL. Aislado: REAL señales+lifecycle a 48h→PF 2.48, a 720h→PF 1.94; el motor con zombie-logic→~0.8. **El timeout vale PF 0.8→1.4-1.8 a nivel portfolio.**
- **minRR 3 vs 4 = INERTE.** 0/2615 disparos del motor tienen RR<4 (el TP estructural `tpMinPct 10`+`tpMult 3` sin cap genérico da siempre RR≥4). minRR 0.5/3/4 dan resultado idéntico byte a byte. Cuál usó producción: NO DETERMINABLE (21/45 trades reales tienen RR<3, inconsistente con ambos → producción no aplicaba veto RR efectivo en agosto). Incertidumbre declarada, no afecta nada.
- **Conclusiones A-F:** (A) señal Caso3 = BUENA/robusta, alpha en el patrón no en la selección de producción (solapamiento 6/45 con las políticas causales y aun así positivo); (B) lifecycle FIEL en entry/SL/TP (PF 2.48 vs 2.54 real sobre mismas señales), único defecto = timeout; (C) admisión de slots RESUELTA como causal-viable ("oldest" o aleatorio preservan edge); (D) timeout = el driver, modelo del motor mal; (E) minRR inerte+incierto; (F) viabilidad económica POSITIVA pero MODESTA (+9-15% en 5 sem, maxDD −3.6 a −8%, costos 5bp despreciables a este PF) — **NO se declara rentabilidad de producción** (muestra corta, 1 estrategia, sin funding, ventana quizá benigna, 15% trades irreproducibles).
- **Limitación clave:** no hay log del scanner de producción → el orden real de admisión entre símbolos simultáneos se aproximó con "oldest"/aleatorio; puede que producción tuviera una heurística mejor (explicaría PF 2.5 real vs 1.7 causal).

## 2026-09-06 (5) — DIAGNÓSTICO CAUSAL del sesgo de WR (MA Slope Caso 3) = DIAGNOSTICADO
- Reporte: `DIAG_CASO3_WR_REPORT.md`. Scripts nuevos: `agent/backtest/diag_caso3_wr.py` (factorial entrada×salida sobre 45 señales reales) + `diag_caso3_detection.py` (¿el motor detecta las señales reales?). Ground truth: `scratch_caso3_gt.json` (45 trades, join p2 + trades.csv). Sin optimizar nada, sin reparar nada.
- **Veredicto: DIAGNOSTICADO.** Waterfall de la caída WR ~53% → ~10%: (1) **detección = 0 pp** — el motor enciende Caso 3 en el bar exacto de la señal (±2 min) en **37/38 trades reales con datos**; (2) **ciclo de vida del trade (entrada + SL/TP intrabar 5m) ≈ −3 pp** — alimentar las señales reales al código de entrada/SL/TP del motor da **PF 3.5–3.96, net +$81–87 = GANADORA**, y los 4 modelos de entrada (cierre vela / next-open / prev-close / precio real) dan lo mismo; (3) **gap de salida por tiempo ≈ −8 pp** — 4/19 ganadores reales → perdedores por zombie-timeout 48h, producción los cerró antes con el monitor 5-min (MFE devuelta ~2.3%); (4) **competencia por 3 cupos + veto stack + máscaras occupied/BTC ≈ −32 pp = CAUSA PRIMARIA** — el replay llena sus cupos con un conjunto **casi disjunto** del real: solo **3 de 45 pares (símbolo, día)** coinciden; los 42 trades de reemplazo son 90% perdedores.
- **REFUTADA la hipótesis del log 2026-09-06(4)**: el sesgo NO es "entrada a cierre de vela 5m que voltea TP→SL". Offset de entrada mediana **−0.02%**, |>0.5%| en 2/38; desplazamiento −0.2%→+0.2% mantiene ganadora (WR 37%→45%). Ningún REAL_TP→replay_SL; los 4 flips son REAL_WIN→zombie-timeout.
- **Precisión sobre la causa A (path dependence de slots)**: no es "reordenar los mismos trades" — es que **el motor opera OTROS trades**. Con 3 cupos entre ~200 candidatos y ocupación caótica sembrada por cierres de otras estrategias (algunos en símbolos irreproducibles), las señales reales de Caso 3 casi nunca ganan un cupo en el replay.
- **Qué habría que reparar** (no se hace ahora): (1) sembrar el replay con el estado REAL de cupos de producción por timestamp, o evaluar cada estrategia aislada; (2) modelar la salida por tiempo como el monitor 5-min real; (3) 1m para altcoins + 7 símbolos faltantes; (4) loguear el `patternParamsJson` efectivo en cada gate.
- **Qué NO sabemos**: por qué exactamente las señales reales pierden la carrera de cupos (falta el log del scanner de producción, no existe); si `minRR` 3 vs 4 cambia el pool; impacto de los 7 símbolos irreproducibles (15% de trades reales); MAE/MFE exacto (DB perdida, reconstruido aprox de klines 5m).

## 2026-09-06 (4) — RECOVERY FORENSIC: configs perdidas + auditoría de MA Slope Caso 3
- Reporte: `RECOVERY_FORENSIC_REPORT.md`. Solo lectura de artefactos, sin ejecutar nada.
- **Recuperación de configs de las 7 del benchmark**: Caso 2 = **A** (config textual en `gate_out.log` + ground truth `scratch_ma2_real.csv`). Caso 3 / Caso 3(15m) / FVG-15m = **B** (núcleo recuperable pero con parámetros no verificables: Caso 3 tiene CONFLICTO `minRR` 4.0 en 9 scripts `run_engine_*` vs 3 en los `verify_ma_slope_caso3_*`; la config real de la DB al correr Gate V4 nunca se logueó y la DB se perdió; FVG-15m: margin/slots no están en ningún artefacto). **Caso 1 / FVG-1m / ADN Micro = C — NO RECUPERABLE** (sus `patternParamsJson` no existen en ningún script/log/reporte/git). Sides SÍ recuperados de `scratch_all_trades_p2.csv` (Caso 3 = SHORT-only confirmado; Caso 1/2 = LONG; ADN = LONG; FVG = ambos).
- **Auditoría MA Slope Caso 3 (REAL PF 1.85 vs replay mediana 0.812)**: hallazgo central = **la WR**. REAL 53% (30/70 tp_hit) vs replay baseline **9.5%** (4/42 tp_hit) vs `run_ma_geometry` julio **14.1%**. **El motor produce ~10-14% de WR para MaGeometry en TODAS sus versiones desde julio**, sin importar la WR real. Comparación: Caso 2 (LONG, real WR 21% → replay 9-12%) "funcionó" en Phase 2 solo porque un loser subestimado sigue siendo loser; Caso 3 (SHORT, real WR 53% → replay 10-14%) se convierte en loser. → causa = **B (modelado) DOMINANTE + A (path dependence) secundaria**. NO es C (jitter estable en los 5 niveles). El motor tiene **sesgo pesimista sistemático**: casi nunca llega al TP (10% vs 43% real) — con `tpMinPct 10` + `slBufferPct 1.0`, la entrada del motor al cierre de vela 5m (vs precio vivo real) voltea TP→SL.
- **Distribución correcta para estimar expectancy** (respuesta al test conceptual): **MEDIA** del ensemble Monte Carlo (no mediana — sesgada para PnL asimétrico; Gate V4 usó mediana = error), + P5-P95/IC para el riesgo de path-dependence, + fracción net-positiva. PERO solo sirve si cada realización es insesgada — acá no lo es (WR sesgada) → "estimación precisa del número equivocado".
- **Gate V4**: diseño sólido, pero **no concluyente como veredicto formal del motor** (1/7, configs no logueadas, mediana en vez de media, mide el motor con el motor). La evidencia parcial + Caso 2 + `run_ma_geometry` julio **ya alcanzan** para la decisión práctica: **el motor NO sirve para estimar rentabilidad**.
- **Repetir 2-7**: NO vale la pena. Caso 1/FVG-1m/ADN = C (no ejecutar sin config real). FVG-15m/Caso3(15m) = confirmarían el mismo sesgo, sin info nueva.
- **Perdido definitivamente**: configs de Caso 1/FVG-1m/ADN; `minRR` real de Caso 3; margin/slots de FVG; config exacta de Gate V4; ground truth con precios de las 6; `SimulatedTrades` completo; las ~20 `StrategyProfiles`.
- **Próximo paso**: ninguna corrida. Decisión del usuario: (a) aceptar motor NO APTO y seguir con OI/H11 (el estudio de correlación de H11 no usa el motor), o (b) Phase 4 de reparación del bug de WR — pero requiere re-ingresar a mano las configs reales primero.

## 2026-09-06 (3) — RECOVERY: fix del backend .NET (crash-loop tras la reconstrucción de DB)
- El usuario señaló `Desktop\logs.txt`: el **backend .NET** (`Verge.HttpApi.Host`, corriendo bajo el debugger de VS, PID 30832 — NO es un contenedor) estaba en crash-loop en `SimulationMarkPriceWorker` por 2 causas:
  1. **`column s.BreakevenLocked does not exist`** (+ `ExitAuditJson`, `ExclusionTag`, `BroadcastToBinance` también faltantes). Causa: el `Verge.DbMigrator` usado en la reconstrucción de la DB (2026-09-06) es un **DLL compilado stale (build 2026-07-19)** que no conoce las migraciones Trading posteriores a esa fecha (`20260615...ExitAuditJson`, `20260720...BroadcastToBinance`, `20260725...ExclusionTag`). `BreakevenLocked` no tiene migración en el fuente (prop del entity sin `ef migrations add`). **FIX**: `ALTER TABLE ADD COLUMN IF NOT EXISTS` para las 4 columnas (tipos según el entity) + `INSERT` de los 3 MigrationId reales en `__EFMigrationsHistory`. La query que crasheaba ahora corre limpia. Efecto inmediato, sin reinicio.
  2. **Redis: `no connection ... serverEndpoint: localhost:6379`** (flood de excepciones en `MarketDataManager`). El backend conecta a Redis en `localhost:6379` (`VergeHttpApiHostModule.cs:161`, un solo `ConnectionMultiplexer` singleton); `verge-redis` está en **6380**. Antes del reset de Docker el que respondía en 6379 era `mirasoles-core-dragonfly` (de otro proyecto), borrado por el reset → nada en 6379 → cada GET timeout 5s. Mi 1er intento de fix editó `src/.../appsettings.secrets.json` pero el proceso corriendo lee la copia de `bin/Debug/net10.0/` (VS solo la refresca en rebuild) → no aplicó. **FIX definitivo**: (a) contenedor `verge-redis-6379` (`redis:7-alpine`, `--restart always`, `-p 6379:6379`) — el backend reconectó solo (`abortConnect=false`), verificado: 3 clients, 250+ comandos, escribiendo `price:*`; (b) además copié el secrets.json corregido (`Redis:Configuration=localhost:6380`) al bin output. **NO requiere reiniciar el backend.**
- Ambas causas del crash-loop del backend resueltas y verificadas. `verge-backtest` sigue caído por el bug pre-existente `orderblock`.
- `verge-backtest` sigue en crash-loop por el bug pre-existente `ModuleNotFoundError: orderblock` en `Dockerfile.backtest` (roto desde antes de esta sesión; no lo necesita el research).
- Sin cambios en research: gate v4 sigue PARCIAL (1/7), H11 congelado, H12 FAILED, OI collector vivo (PID 30652).

## 2026-09-06 (2) — RECOVERY tras reinicio(s) de PC. Solo hechos verificados; sin ejecutar nada nuevo.
- **Gate V4 = PARCIAL / murió durante ejecución.** `agent/backtest/gate_v4_out.log` congelado 2026-09-05 21:55 -03, termina en "FVG - 15m ... fvg 0/9730" (la corrida fiel de FVG arrancó y murió a los ~10s). `scratch_gate_v4_results.json` (21:31 -03) contiene **solo la estrategia 1/7 (MA Slope Caso 3), COMPLETA**: 350 corridas MC = 50 seeds × 7 configs de perturbación (js=0/1/2/5/10, jt=1/2) + baseline + costos 0/2/5bp + prod. Sin bloque `_gates` → el veredicto mecánico final nunca lo calculó el script (lo calculé yo en el reporte). Estrategias 2-7: NO corridas. OOS (gate H): NO corrido. % completado ≈ 14% (1/7), pero esa 1 tiene el resultado completo. Resultado preservado: REAL PF 1.85 WINNER → replay mediana PF 0.812 LOSER, 29.7% seeds del lado correcto, PF P5=0.334/P95=1.613, LOSER en los 5 niveles de jitter, net-negativo a 0/2/5bp. **NO re-ejecutar la #1.** Re-correr 2-7 requeriría re-ingresar sus configs reales (perdidas en el reset de Docker).
- **OI collector = OPERATIVO.** PID 30652 (`nohup python -u open_interest_collector.py`, uptime ~37min al momento del recovery). 438.047 filas, 167 símbolos. Span 2026-08-04 16:25 → 2026-09-06 02:20 UTC = **32.41 días**. Último write 11min atrás (latencia normal 5m). **0 gaps > 6min en BTCUSDT** — el backfill de arranque (30d idempotente) tapó los huecos de los 2 reinicios. Cobertura: mediana 1.000; **110/167 símbolos ≥90%** (los peores son listings nuevos con archivo de OI escaso en la fuente, no bug del colector).
- **Task Scheduler NO recuperó `VergeOICollector`** — la tarea NO existe (nunca se registró, o el reset la borró). `Register-ScheduledTask` en este entorno da "Acceso denegado" (necesita PowerShell elevado). `agent/register_oi_collector_task.ps1` reescrito ASCII-only (tenía parse error por encoding UTF-8). **PENDIENTE (usuario, PS elevado)**: `powershell -ExecutionPolicy Bypass -File agent\register_oi_collector_task.ps1` + `Start-ScheduledTask -TaskName VergeOICollector`. Mientras tanto el colector corre como proceso suelto (nohup) → muere en el próximo reboot, pero el backfill idempotente cubre el hueco si se re-arranca dentro de 30 días.
- **H11 gate = NOT READY.** 32.41 días < 70 requeridos (~37.6 días / ~5.4 semanas faltan). Símbolos ≥90%: 110 (cumple ≥20). H11 sigue CONGELADO. ETA gate ≈ mediados de octubre 2026 si el colector corre continuo.
- **H12 = FAILED confirmado.** No reabierto, sin H12.1/2/3.
- **Docker**: verge-redis / verge-db / verge-market-ws (healthy) / verge-python-ai = UP y respondiendo (HTTP 200). **verge-backtest = crash-loop** por bug PRE-EXISTENTE en `agent/Dockerfile.backtest` (`ModuleNotFoundError: No module named 'orderblock'` — el engine importa módulos de `python-service/` que la imagen no incluye). Estaba roto desde antes de toda esta sesión. NO lo necesita el research (el motor corre como proceso del host).
- **Lab evolutivo** (PID 10436 `lab_control_server` + 18144 `evolutionary_lab`): CORRIENDO, auto-arrancado por la tarea `Verge-LabControl-AutoStart`. gen 15, probadas 360, encontradas 0. Es infra del usuario, no la toco. Consistente con "grid search = EXHAUSTED" ya documentado.
- **Verge Postgres**: sigue en el estado reconstruido del 2026-09-06 (esquema + 20 profiles aproximados + 3682 trades de `trades.csv` con PnL NO confiable). Sin cambios.
- Reportes (Phase 1/2/3), GATE_V2/V3/V4_CRITERIA.md, PROGRESS_LOG y memoria: todos íntegros.

## 2026-09-06 — INCIDENTE: reset accidental de Docker Desktop + Phase 3 = FAILED
- **Pérdida de datos**: la PC se reinició y un reset accidental de Docker Desktop borró TODOS los volúmenes → base `Verge` (SimulatedTrades ~5000 + StrategyProfiles) perdida, sin backup. También imágenes y volúmenes de todos los otros proyectos (mirasoles/novalis/atajo/etc.).
- **A salvo** (archivos de disco, no volúmenes): todo el código, `binance_vision_clean.db` (5.97GB), `klines.db` (2.37GB con datos del colector de OI = 32.4 días), y exports pre-reset: `scratch_ma2_real.csv` (Caso 2 completo), `scratch_gate_v4_results.json` (resultado Phase 3 de MA Slope Caso 3), `scratch_all_trades_p2.csv`.
- **Reconstrucción** (`agent/reconstruct_db.py`): esquema recreado con `Verge.DbMigrator`; 20 StrategyProfiles (solo Caso 2 con config REAL, resto aproximado); 3682 SimulatedTrades desde `agent/data/trades.csv` — **PnL NO confiable** (el log legacy reconstruye Nexus a +$228k, FVG-1m a −$192k; marcados `reconstructed` en ExtraProperties). Sirve solo para que la app no esté vacía. Colector de OI reiniciado a mano (re-registrar la tarea programada).
- **PHASE 3 (GATE_V4) = FAILED**. El benchmark de 7 estrategias arrancó y completó la #1 (MA Slope Caso 3) contra la config REAL, con la grilla de perturbación PRE-REGISTRADA (`GATE_V4_CRITERIA.md`), ANTES del reset. Resultado preservado: REAL PF **1.85** (ganadora clara, +$65, exp +$0.93/trade) → replay mediana PF **0.81 = clasificada LOSER**; **solo 29.7% de las 50 seeds del lado correcto**; PF por seed va de P5=0.33 a P95=1.61; LOSER estable en jitter ±1/±2/±5/±10s (sesgo sistemático, no ruido); net-negativo en el replay a 0/2/5 bp (real +$65). Gates A/C/E/G = FAILED → veredicto FAILED por la regla pre-registrada.
- **No se pudo medir el false-positive rate** (perdedoras reales pintadas de ganadoras = el error peligroso) — las perdedoras del benchmark no se corrieron y su ground-truth se perdió. NO PODEMOS CONCLUIR sobre eso.
- **Conclusión**: el motor NO sirve para evaluar rentabilidad de estrategias nuevas. Convirtió una ganadora real en perdedora con un PF que es una moneda al aire. Phase 1 corrigió bugs estructurales (necesario, no suficiente); Phase 2 lo dio por LIMITED prematuramente (solo probó 1 perdedora); Phase 3 lo rompe con la primera ganadora. Reporte: `BACKTEST_RELIABILITY_PHASE3_REPORT.md`.
- **Para volver a confiar en el motor**: Phase 4 = re-ingresar las configs reales de las estrategias, re-correr el benchmark completo de 7, y medir sobre todo el false-positive rate.
- Alpha research sin cambios: H12 CLOSED/FAILED, H11 congelado, OI acumulando. El motor queda marcado NO CONFIABLE para evaluación económica.

## 2026-09-05 (4) — BACKTEST RELIABILITY REPAIR Phase 2: PARTIAL / motor = LIMITED
- Objetivo: cerrar el gap de Phase 1 (el replay reproducía decisiones pero no el timeline de ocupación de los 3 cupos). Reporte: `BACKTEST_RELIABILITY_REPAIR_PHASE2_REPORT.md` (A-H). Criterios pre-registrados: `agent/backtest/GATE_V3_CRITERIA.md`. Gate: `agent/backtest/gate_v3.py`. Sin tocar estrategias/producción/H11/H12; ningún parámetro ajustado para forzar match.
- **Timeline reconstruido** (`phase2_timeline.py`): la hipótesis "latencia del scanner" era FALSA — latencia decisión→entrada real = **mediana 1.1s**. El mecanismo real: producción estuvo **saturada de cupos** (2/3 ocupados en 97/113 entradas) y entra **cuando se libera un cupo**, dentro de una ventana de elegibilidad larga (el patrón MA-geometry persiste **mediana 2.2h, máx 12.8h**). El replay v2 abre en la 1ª vela → 55/87 trades reales tienen su señal ≥30min después. Datos que NO existen (marcados): ts de "scanner llegó al símbolo", uptime minuto a minuto, multiplicador histórico de bucket_calibrator.
- **Correcciones**: (1) `occupied` — reconstrucción de intervalos de posición abierta de las 26 estrategias (3316 trades reales), replica `_should_skip()` cláusula 2 (no abrir símbolo ya ocupado por otra estrategia). **Era el gate que faltaba y el de mayor impacto en el agregado.** (2) `run_ma_geometry_global` (nuevo, opt-in en engine.py) — reloj global 5min, admisión por cupo libre. (3) backfill 21/22 símbolos UNREPLAYABLE desde data.binance.vision (`download_missing_symbols.py`, 416k filas 5m) → trades irreconstruibles **22% → 0.9%** (solo ETHBTCUSDT). (4) sesgo intrabar TP/SL auditado (`phase2_intrabar.py`, 1922 trades): **NEGLIGIBLE** para FVG (0-0.2% velas abarcan TP y SL a 5m); solo material para scalping SL≈TP, que es UNTESTABLE igual.
- **Resultado A/B**: con `occupied`, tanto V2 como V3 dan **agregado FIEL**: PF 0.53/0.62 vs real 0.55 (**|ΔPF| 0.02-0.06**), net PnL −$99/−$79 vs −$116 (15-32% rel, mismo signo, sin flip), n=90-100 vs 108. **El sign flip del gate v1 está resuelto.** PERO **selección de trades específicos NO reproducible**: P2 match 4-11%, correlación de timeline de cupos ≈0, S4 PnL-corr 0.35-0.61. Gate v3 mecánico = FAILED (todas las métricas por-trade en banda FAILED). **Veredicto = PARTIAL**: todos los fallos son por-trade con una causa raíz — la ocupación de cupos es un **proceso caótico dependiente de la trayectoria** (sembrado por cierres de decenas de posiciones de 26 estrategias, algunas no replayables); no es un bug, es propiedad del sistema.
- **¿Puede Verge usar el motor para evaluar el PnL de una estrategia nueva? = LIMITED.** SÍ: signo + magnitud aproximada de la expectancy agregada (¿PF ~0.5 o ~1.5?), comparación relativa entre variantes, descartar una perdedora, impacto de vetos. Un "PF 1.4" ahora significa "probablemente rentable de verdad". NO: PnL exacto / $ mensuales, qué trades entran, edge que dependa de capturar una entrada específica, listings nuevos (outliers), scalping SL≈TP, fuentes Nexus/SCAR. Runner recomendado: `run_parallel` con `fidelity` completo (btc_block, daily_change, traded_before, occupied, blackout); `run_ma_geometry_global` como cross-check.
- Alpha research sin cambios: OI acumulando, H11 congelado, H12 FAILED, sin H13. Prioridad: Phase 2 (cerrado LIMITED) → OI → H11 → alpha confirmado → estrategia → backtest agregado.

## 2026-09-05 (3) — BACKTEST RELIABILITY REPAIR Phase 1: PARTIAL
- Reparación del motor de replay (`agent/backtest/engine.py`, correcciones ADITIVAS opt-in vía `fidelity=`) para reproducir la LÓGICA DE DECISIÓN de producción. NO se tocó ninguna estrategia ni producción ni H11/H12; ningún parámetro ajustado para forzar match. Reporte: `BACKTEST_RELIABILITY_REPAIR_REPORT.md` (9 secciones). Criterios PRE-registrados: `agent/backtest/GATE_V2_CRITERIA.md`. Gate: `agent/backtest/gate_v2.py`.
- **Correcciones**: (F1) filtro macro BTC histórico real wired — tabla nueva `btc_klines_1m` en binance_vision_clean.db (112.320 filas, backfill `agent/download_btc_1m.py`, solo data.binance.vision); valida 110/113 (97.3%) vs `btc_context.regime` real, 0 errores peligrosos (`validate_btc_filter_repro.py`). (F2) `historical_daily_change_pct` inyectado — el veto daily pump/dump ya no pega a la API en vivo. (F3) timeout FIEL: helper puro `zombie_timeout_decision` + 7 tests unitarios (`test_engine_timeout.py`, 5m/15m/1h/4h). Hallazgo: producción usa 15min a propósito (verificado vs 34 trades reales de timeout, moda 24.0-24.2h); el bug real era que el motor cerraba GANADORES a las 24h (producción solo cierra si pnl<0). (F4) `has_traded_symbol_today` CRUZADO entre las 26 estrategias, reconstruido de 3057 trades reales. (F5) funding real donde hay cobertura (`funding_rates`, 490 sym), 0+flag donde no ($2.92 total en 113 trades). (F6) blackout flash-crash. (F7) cobertura clasificada: 75 REPLAYABLE / 22 UNREPLAYABLE (25 trades=22%, −$18.7, irreconstruibles).
- **Resultado**: sign flip CORREGIDO (replay pasó de +$43/PF 1.16 a −$3.81/PF 0.98; real −$116/PF 0.55). Inflación de señal 11.2×→2.6×. Diagnóstico punto-a-punto (`gate_v2_diagnose_misses.py`): **85/88 trades reales replayables → el motor los abriría en su timestamp real** — la lógica es fiel. PERO gate v2 mecánico = FAILED (P1 match 9%, P2 false-pos 69%, P5 ΔPF 0.43, S2 corr r=0.52 en banda FAILED). **Veredicto final = PARTIAL**: los 4 fallos tienen una sola causa raíz — el motor no reproduce el TIMELINE EXACTO DE OCUPACIÓN DE LOS 3 CUPOS (abre en la 1ª vela de 5m que pasa vetos; producción abre minutos después por latencia real del escáner → FIFO reparte los ~93 trades a otros símbolos).
- **Qué soporta el replay reparado**: preguntas de lógica/dirección, exposición/régimen agregado, detección de que una señal NO aporta (sesgo optimista → "no aporta" es conservador), sanidad de vetos. **Qué NO**: PnL/PF/expectancy precisos, "estrategia X hace $N/mes", rankear por PnL de backtest, convertir un alpha en estrategia y confiar en su PF. Para eso → Phase 2 (replay con cadencia real del escáner; backfill/exclusión de los 22 UNREPLAYABLE; cuantificar sesgo intrabar TP/SL para FVG/OrderBlock).
- Alpha research sin cambios: OI acumulando, H11 congelado, H12 FAILED, sin H13. Prioridad: (1) Phase 2 replay → (2) OI → (3) H11 al cumplir gate → (4) recién ahí convertir alpha en estrategia.

## 2026-09-05 (2) — GATE de confiabilidad del backtest: FAILED
- **LIVE vs HISTORICAL REPLAY RECONCILIATION** (auditoria, no research). Universo congelado: MA Slope Caso 2 (MaGeometry, 113 trades reales cerrados 2026-07-11→08-09, ciclo de vida 100% dentro del dataset). Config leida de verge-db sin tocar nada. Motor `run_parallel("MaGeometry")`. Script: `agent/backtest/gate_live_vs_replay.py`, tabla `scratch_gate_trades.csv`.
- **Resultado FAILED**: solo 19/113 (17%) trades reales tienen contraparte en el replay. El motor detecta **11.2x mas señales** que las que produccion ejecuto (1268 crudas vs 113). PnL agregado del mes: replay **+$43 / PF 1.16** vs real **−$116 / PF 0.55** — cambia de signo, PF cruza 1.0. El motor es **optimista** (riesgo = falso positivo, no falso negativo).
- Causas de la inflacion 11x (todas del codigo): (1) veto macro de BTC desactivado en el replay (`validate_pre_trade(btc_filter=None)`, engine.py:800 — falta 1m de BTC en el dataset); (2) sin estado de cartera cruzado entre estrategias (has_traded_symbol_today global, cooldowns, loss-streak throttle, score buckets); (3) uptime del agente asumido 100%; (4) watchlist actual != la de julio.
- Bugs puntuales encontrados: **timeout con divisor de 15min fijo** (engine.py:938/742) — en TF 1h con maxTradeDurationCandles=96 el replay cierra a 24h en vez de 96h, afecto 7/19 pares matcheados; **funding no modelado** (`_capital_sim` asume 0); **22% de los trades reales sobre 22 simbolos sin klines historicos** (listings nuevos/deslistados/stock-perps); fills sin slippage.
- TP/SL intrabar: 0 casos en MA Slope (SL ~2% vs TP ≥7%, muy separados) — pero NO cubre FVG/OrderBlock/scalping (stop ajustado), auditar aparte.
- **Alcance del veredicto**: invalida el lab evolutivo/grid-search, los PF del subset baseline (Pase 04), H1 Regime Router, y cualquier backtest de trade-replay futuro (incl. una eventual estrategia de OI). **NO toca H8/H9/H10/H12** (correlacion parcial sobre datos crudos, sin motor). H11 congelado tampoco (es estudio de correlacion, no usa el motor).
- Publicado: artifact "Verge Backtest Reliability Gate" (https://claude.ai/code/artifact/95cb3a4e-18fe-4d2d-b397-3a79a2a138fd). Prioridad ahora: cerrar gaps 1-3 antes de usar el motor para cualquier conclusion.

## 2026-09-05
- **Global Alpha Post-Mortem v2** (síntesis, sin ningún test nuevo, sin tocar H11 ni producción): consolidada la tabla completa de las 15 hipótesis corridas H1-H12 (fuente/ventana/cobertura/incremental/magnitud/costos/estado), distinguiendo explícitamente "estrategia FAILED" de "fuente de información agotada" — H8/H9.3/H10/H12 = fuente agotada (A), H9.1/H11 = dato insuficiente (D/UNTESTABLE, no evidencia negativa), H9.2 = único caso de efecto real no monetizable (C).
- Clasificadas todas las fuentes actuales (OHLCV=EXHAUSTED, funding/cross-exchange/OFI/taker-flow=FAILED como alpha, OI=UNTESTABLE genuino, liquidaciones/on-chain=UNTESTABLE por falta de historia/feed, opciones=NEVER TESTED). Identificadas 3-4 dimensiones informativas realmente ortogonales a todo lo probado: OI (ya en marcha), liquidaciones/forced-flow (bloqueado por feed poco confiable), on-chain (bloqueado por proveedor/presupuesto), opciones (prior bajo, liquidez sólo BTC/ETH).
- Análisis dedicado de OI sin correr H11: qué distingue sobre el precio solo (stock de posicionamiento vs flujo), qué señales serían realmente incrementales (interacciones ΔOI×precio×funding, no nivel aislado), y una nota conceptual (sin modificar el protocolo) de que los horizontes congelados (horas) podrían sub-potenciar un mecanismo de leverage que típicamente ciclúa en días-semanas.
- Veredicto de distancia informacional: **(C) lejos** — no por falta de rigor sino porque la región barata-de-reconstruir (precio/flujo/funding/OFI coarse) ya está agotada y arbitrada, y las regiones distintas (posicionamiento-stock, on-chain, opciones) recién empiezan o no existen. Decisión final: **NEXT ACTION = A) esperar a H11/OI** (colector corriendo a costo marginal cero, única hipótesis viva no contaminada); B/C explícitamente descartadas por ahora (requieren decisión de inversión del usuario o repetirían fuentes ya agotadas); D descartada por prematura. Publicado como §09 del artifact "Verge Alpha Post-Mortem".

## 2026-09-02
- **Pase 04/05 — subset baseline + Alpha Opportunity Map** (artifact `verge-research-log.html` §13-15, link sin cambiar). **Error de cómputo corregido**: `run_fvg_global` a escala 8,5m×429 símbolos = job de ~40h, inviable; matado al 27%, salvado el parcial (dic-2025→feb-2026, 1689 trades). MaGeometry/ADN/OrderBlock corrieron completos vía `run_parallel`. **Resultados subset baseline (8,5 meses)**: (a) **MA Slope Caso 3** 679 trades — RAW PF 1.026/+$33 → **con 2pb slippage PF 0.998/−$3** → 5pb PF 0.96/−$54; 0 intrabar-flips (la negatividad no es artefacto); concentración normal (top10% trades = 26% PnL, NO outliers); abril-2026 solo −$118. **El "edge" previo era fragilidad de ejecución/ruido → FAILED nivel subset.** (b) **Order Block 15m** 3054 trades — RAW PF 0.83/−$439 → 0.71/−$857 con costos, 7/9 meses negativos → **FAILED** (confirma nota 2026-08-23). (c) **FVG parcial fiel** PF 0.87/−$256, LONG y SHORT negativos → **FAILED en historia larga** (coincide con backtest propio PF~1.0). (d) ADN 1D n=4 → **UNTESTABLE**. **Ninguna familia fiel tiene expectancy positiva tras costos realistas.** NO es afirmación sobre el sistema global (36% Nexus sigue UNTESTABLE).
- **Clasificación agregada al artifact**: FACT/EVIDENCE/HYPOTHESIS/FAILED/UNTESTABLE con ledger de todos los hallazgos. "El sistema global no tiene edge" = UNTESTABLE.
- **Alpha Opportunity Map**: casi toda la data exótica (OI, liquidaciones, order book, on-chain) NO tiene historia (colectores recién prendidos — OFI 30 días, liquidaciones 7 días, whale 3 días, **OI cero**). Funding 8 semanas. Lo único con historia + testable causalmente ya que aporta info **estructuralmente distinta**: **cross-sectional / fuerza relativa** (relación de cada símbolo con los otros 428 — nada de lo probado lo usa).
- **H8 candidata (formalizada, PENDIENTE de aprobación)**: rank cross-sectional de fuerza relativa ajustada por volatilidad (retorno k-barras menos mediana del universo, /ATR%) → spread de retorno forward monótono top-vs-bottom decil, OOS temporal, estable entre mitades, tras costos. Controles anti-leakage: ranking causal, universo elegible causal (historia mínima + liquidez), sin survivorship, sin grid search (k y horizonte predefinidos). Test simple = deciles + forward returns, comparado contra BTC / equal-weight / MA Slope Caso 3 / momentum per-símbolo. Primera pregunta: "¿existe señal cross-sectional OOS significativa?" — si no → H8 FAILED. Scripts: `scratchpad/pass04b.py`.
- **Limpieza**: 2 monitor-loops zombie (`until grep`) quedaron corriendo 17h/5h esperando logs de runs ya muertos — matados. Cuidado a futuro con dejar `until grep ... sleep; done` en background sin condición de salida real.

- **Pase 06 — H8 (cross-sectional relative strength) = FAILED** (artifact §15-16, `scratchpad/h8_xs.py` + `h8_results.json`). Test de existencia de señal, todo pre-declarado (rebal 4h/24h, lookback 24/72/168h, fwd 4/24/72h, elegibilidad causal + liquidez, deciles equal-weight, splits discovery/validation/final-OOS intocable, 4 variantes de score). **Hallazgo decisivo**: "relative strength" = retorno − mediana del universo es **rank-invariante** → spread L/S idéntico a momentum absoluto en **54/54 celdas**. La única variante que reordena (ajuste por vol, `vadj_rel` = H8) rinde **peor** que momentum crudo en OOS (7/15 vs 11/15 celdas positivas en los 3 splits). Lo que queda es momentum cross-sectional per-símbolo (short del decil de perdedores 3d): spread +1.2-2.3%/rebalanceo bruto en el mejor caso (lb72/fw72) pero (1) sin monotonía entre deciles OOS — solo D1 underperforma, (2) concentración severa — top-5 símbolos = 100-286% del PnL L/S, (3) spike en (72h,72h) no meseta — lb24/fw72 negativo OOS, lb168 flip de signo en validation, (4) el ajuste por vol degrada. **Falla criterios 1,4,5,7,8. No se rescata con filtros.** Respuesta a "¿fuente nueva o redescubrimos momentum?": redescubrimos momentum, en forma que no pasa la barra. Alpha Opportunity Map: con H8 caído y OI/liquidaciones/order-book/on-chain sin historia, vías restantes = precio cross-exchange (nunca tocado) o empezar a capturar Open Interest ahora y volver en 1-2 meses.

- **H9 (cross-exchange price dislocation) — AUDITORÍA DE DATOS (pre-ejecución, regla 11 del usuario)**. Tabla `klines_multi_exchange` en `binance_vision_clean.db`: **bybit** 96 syms (88 overlap Binance, incluye 8 majors), nombres canónicos, 2025-12-01→2026-07-26, BTC/ETH 100% completos en Abr-Jul, sync excelente (median |px diff| bybit-vs-binance 0.5-1.2bp majors / 3-4bp mid-caps, p95≤10bp, mismos open_time UTC-15m — **sin riesgo de artifact de timestamp**). **bitget** 95 syms (80 overlap Binance) pero **set DISJUNTO de bybit** (alts chicas, sin majors) — sirve como check de 2do exchange independiente. **okx** 75 overlap pero empieza 2026-05-24 → **sin período discovery, inservible para el split de 3**. Todo termina 2026-07-26 → **Final OOS truncado a Jun1-Jul26 (~8 semanas)**. Resolución: **solo 15m**. **Veredicto de testabilidad**: mecanismo 1 (lead/lag microestructural, "quién lidera intra-ventana") = **UNTESTABLE** (requiere quotes/trades sub-segundo sincronizados, no existen). Mecanismos 2 (dislocación→convergencia) y 3 (continuación) = **TESTABLES a 15m** con bybit (88 syms) + bitget como robustez, OOS truncado. Params pre-declarados en `scratchpad/h9_cross_exchange.py`: spread s=ln(px_other)-ln(px_binance), z-score causal trailing-96-barras, buckets |z| fijos [0-1)(1-2)(2-3)(3+), horizontes forward 1/4/16 barras, liq floor $1M/15m, controles A(binance-only mom)/B(other-only mom)/C(cross-exch)/D(placebo shuffle), splits discovery/validation/OOS-truncado. Pendiente green-light del usuario sobre correr mecanismos 2+3 vs declarar todo H9 UNTESTABLE. Ninguna estrategia tocada.

- **H9 (cross-exchange) — EJECUTADO, mecanismos 2+3 = FAILED** (`scratchpad/h9_cross_exchange.py` + `h9_results.json`, artifact §17-18). bybit↔binance 88 símbolos, 1.15M barras joined, params pre-declarados (spread ln(bybit/binance), z causal 96 barras, buckets |z| fijos, horizontes 1/4/16, liq floor $1M/15m, splits discovery/validation/OOS-truncado-8sem, placebo +25 barras). **H9.1 (lead/lag)** = UNTESTABLE confirmado (resolución 15m). **H9.2 (convergencia)** = efecto REAL, monotónico en |z| en los 3 splits, presente en 37-40/40 símbolos líquidos, top5≈30-50% — PERO magnitud +1.6bp (común) / +3bp (moderado) / +5.5bp (extremo, n≈300) en 1h vs round-trip costo 8bp fee-only (16bp spread 2-patas). **~1 orden de magnitud bajo el costo → FAILED como alpha ejecutable**, documentado como señal estadística real no monetizable. Solo 1 par de exchanges (bitget: 1 símbolo pasa liquidez; okx: sin discovery). **H9.3 (continuación)** = partial corr de d(t) con binance-fwd controlando por binance-momentum = ~+0.01 en las 9 celdas (despreciable, r²≈0.0001); el **placebo (+25 barras) reproduce la señal de los buckets comunes** (+0.1 a +2.3bp igual que la real) → movimiento de mercado común, no info cross-exchange. Bucket extremo difiere del placebo pero n≈cientos e inestable de signo entre splits. **H9 = FAILED**. Respuesta: ni info incremental tradeable ni redescubrimiento de momentum — un micro-efecto de reversión de spread genuino pero sub-costo + continuación despreciable que es beta común. **Alpha Opportunity Map tras H8+H9**: agotado OHLCV/cross-sectional/cross-exchange; sin historia OI/liq/orderbook/onchain; quedan vol-regime-filter (prior débil, las familias que filtraría ya fallaron) y funding-reframe (8sem). Única vía con prior fuerte = Open Interest, bloqueada solo por datos → empezar a capturar ahora, volver en 1-2 meses. Ninguna estrategia tocada.

- **Post-H9 — cambio de fase. DOS tracks en paralelo (usuario: no más hipótesis débiles sobre OHLCV).**
- **TRACK A — colector histórico de Open Interest (DATA COLLECTION, no estrategia).** Nuevo `agent/open_interest_collector.py` (patrón de `funding_rates.py`: REST baja freq + circuit breaker Binance compartido). Fuente: `GET /futures/data/openInterestHist` period=5m (Binance solo guarda ~30d → hay que correrlo continuo desde YA). Backfill inicial 30d paginado + régimen permanente cada 5min. Universo `config.WATCHLIST_TIER1`. Guarda SOLO crudo (open_interest base + open_interest_value USDT); derivados (ΔOI/%ΔOI/OI-vol) se calculan al leer, causal, nunca se persisten. `kline_cache.py`: agregada tabla `open_interest` (CREATE TABLE IF NOT EXISTS, aditivo) + métodos `bulk_upsert_open_interest`/`get_latest_open_interest`/`get_open_interest_history(before_ms=)`/`open_interest_gaps`/`prune_old_open_interest` — todos dormidos, nadie los llama. `docker-compose.yml`: servicio `oi-collector` (reusa imagen verge-market-ws, bind-mount ./agent/data para escribir el mismo klines.db que lee el research). Doc completa: `agent/OPEN_INTEREST_COLLECTOR.md` (endpoint/freq/rate-limits/retención/gaps/normalización/anti-lookahead/timestamps/cómo correrlo/checkpoint 4-6 sem). Syntax + smoke test OK (DB throwaway). **NO se corrió contra Binance desde este entorno** (IP compartida con el agente live — ban risk); lo lanza el usuario. **verge_agent.py NO tocado.**
- **TRACK B — Alpha Research Post-Mortem** (artifact nuevo `verge-postmortem.html`, link separado del research log). Análisis transversal de los 7 pases. **5 patrones demostrados**: (1) cuando hay relación medible es diminuta (techo AUC ≤0.54 / efecto ≤2bp en TODOS los tests); (2) lo que funciona IS se degrada a cero OOS (no es "cero correlación siempre"); (3) el edge residual está concentrado (H8 top-5 = 100-286% del PnL); (4) todo deriva de OHLCV de Binance o transformaciones rank-idénticas (demostrado math en H8); (5) donde el dato es distinto, la historia es muy corta o el efecto es real-pero-diminuto — nunca hubo test justo de fuente nueva + magnitud grande + historia. **Tabla NO-HAY-EDGE vs NO-PODEMOS-MEDIR**: 10 FAILED, 1 SEÑAL-REAL-NO-MONETIZABLE (H9.2), 1 EVIDENCE (funding mitigador), 4 bloques UNTESTABLE. **Cuello de botella (por evidencia)**: A (falta de información) para lo probado + G (falta de historia en fuentes nuevas) para lo no probado; C (costos) y B (resolución) secundarios, aguas abajo de A; F (metodología) NO es el problema. **7 requisitos derivados** para la próxima fuente. **Ranking de fuentes** → conclusión: **la próxima investigación debería explorar Open Interest** (única sin testear que cumple los 7 requisitos; la evidencia descarta OFI/cross-exchange/order-book/opciones/on-chain). Basis testable YA como interino opcional. NO dice "Verge no tiene edge" — dice "el conjunto OHLCV está agotado y las fuentes nuevas no tienen historia (lo que TRACK A arregla)".

## 2026-09-04
- **STEP 0.5 — Alpha/Data Inventory Audit v2** (artifact `verge-postmortem.html` §8, link separado). Inventario exhaustivo de TODO lo local. Estado por fuente: **A/EXHAUSTED** = OHLCV perp (429 sym, 260d) + derivados + logs del scanner viejo (`AnalysisLogs` abr-jun con RSI + order-flow hardcodeado, `AlertHistories` 4 días — sin info nueva). **B/FAILED** = cross-sectional momentum (H8) · precio cross-exchange (H9) · basis (H10 = funding) · OFI libro-en-reposo (`orderbook_ofi`, ahora 30d rolling, AUC 0.46) · funding como edge. **C/PARTIAL** = Open Interest (`open_interest`, en acumulación, gate H11 congelado) · OFI si se re-encuadra (prior bajo). **D/UNTESTABLE** = liquidaciones (7d) · whale on-chain (3d) · Nexus/SCAR histórico · opciones (sin fuente). Postgres: BacktestResults/TradeOrders/StrategyCalibrations/TemporalOptimizationResults vacías. SCAR db: 84 signals, dead.
- **HALLAZGO — corrección al Post-Mortem v1**: el v1 clasificó "taker buy/sell pressure" como "necesita collector + 1-2 meses". **Está mal**: las columnas `taker_buy_volume`, `count`, `quote_volume` YA están en el archivo de `data.binance.vision` que se usó para el OHLCV — el downloader (`download_binance_vision_daily_all_v2.py`) sólo guardó cols 0-5. Verificado bajando 1 archivo (BTCUSDT 15m tiene 12 cols, col 9 = taker_buy_base). Se recupera con **un backfill seguro** (mismo CDN, mismos 429 sym, 260d, sin `fapi`, sin ban) → testeable con los 3 splits temporales completos.
- **E/NEVER INVESTIGATED que merece atención**: **taker buy/sell order flow (CVD)** — realized aggressor flow por barra. Genuinamente distinto: OHLCV = dónde fue el precio; OFI = liquidez en reposo; funding = costo de posicionamiento; cross-exchange = dislocación de venue; taker-flow = **quién cruzó el spread**. Mecanismo estándar (CVD/order-flow imbalance). Cumple los 8 criterios de "merece investigarse" salvo amplitud/costos (lo que el test decide).
- **NEXT ACTION = INVESTIGATE taker buy/sell order flow.** Razones: única fuente no-OHLCV con ortogonalidad real + profundidad temporal completa disponible ya; costo = un backfill seguro (no semanas); corre en paralelo a OI (cero retraso a H11); el v1 la había descartado por error. **Prior de PASS honestamente bajo** (order flow en perps líquidos rara vez sobrevive costos; OFI-libro ya falló) — pero barato, ortogonal, no bloquea nada. Si FAILED → se cierra order-flow y se espera a OI, sin rescates. Próximo paso: pre-declarar metodología (mismo rigor que H9/H10/H11) antes de bajar/mirar datos. **NO ejecutar H11 antes del gate (≥70d OI, ≥20 sym, ≥90% cobertura). Protocolo H11 congelado, sin cambios.**
- **H12 (taker order flow / CVD) — PROTOCOLO CONGELADO + Data QA**. `agent/H12_TAKER_FLOW_TEST.md` (preregistration completo, 13 secciones): 5 familias congeladas A(Taker Imbalance) B(CVD) C(flow acceleration) D(price/flow divergence) E(extreme flow); ventanas 1/4/12/24h, horizontes forward 1/4/12h, z-lookback 96 barras, buckets |z| fijos; controles A-E con partial-corr incremental residualizando contra [trailing return, range, funding]; splits discovery(dic-mar)/validation(abr-may)/OOS(jun-17ago intocable); universo perp líquido ≥$1M/15m y ≥95% cobertura taker, top-5 <60%; placebo taker +25 barras + bloques no solapados por autocorrelación; costos 0/2/5bp; **gate económico ≥15-20bp** (1-3bp = FAILED); PASS = los 10 criterios; sin grid-search/variantes post-hoc/rescates. Tabla nueva `taker_flow` en binance_vision_clean.db (aditiva). **Data QA sample OK** (4 símbolos × 3 días = 1152 barras): open_time coincide 100% con klines_clean, volumen coincide 100% (≤1bp), taker_buy+taker_sell==volume exacto, 0 duplicados, sin lookahead (última barra cierra 23:59:59.999), count>0, timezone UTC ambos, taker_buy_frac varía 0.26-0.70. **PENDIENTE: aprobación del usuario para el backfill completo** (429 sym, 260d, solo `data.binance.vision`, no fapi). Ninguna estrategia/producción tocada. H11 sigue congelado, sin ejecutar antes del gate.
- **H12 — backfill de `taker_flow` HECHO + auditoría → DETENIDO antes del alpha test (regla 5 del usuario)**. Backfill: `agent/download_taker_flow.py --backfill`, 429/429 símbolos, **9.328.125 barras 15m**, 424 archivos ausentes (daily de agosto para símbolos no listados aún / monthly de listados post-dic). **Integridad PERFECTA**: 5.706.639 barras comparadas contra `klines_clean` en la intersección de open_time → **100.000% válidas** (volume coincide ≤1bp Y 0≤taker_buy≤volume), **0 discrepancias**. Spacing 100% exacto 15m. Cobertura temporal del universo: disc 99.3%, val 100%, oos 100%. **324/429 símbolos tienen cobertura ≥95%** de la ventana completa (los ~105 restantes son listados post-dic-2025 — no es un problema de datos). **BLOQUEO: el filtro de liquidez del §6 congelado**. Si se interpreta como "quote_volume mediano LIFETIME del símbolo ≥ \$1M/15m" → **solo 12 símbolos** (mega-caps), lo que hace **imposible** el criterio §9.5 (≥20 símbolos con efecto del mismo signo) → forzaría un UNTESTABLE no-guiado-por-datos. Pero §6 dice "dólar-volumen mediano **trailing-7d** < \$1M/15m" — "trailing-7d" ES el rolling per-observación (exactamente como H9/H10/H11 lo hicieron: universo amplio + máscara de elegibilidad por barra). Mi `download_taker_flow.py --audit` implementó mal el filtro como lifetime-median. **Reportado al usuario para confirmar la interpretación antes de correr alpha** — no es cambio de protocolo, es leer el §6 como está escrito (trailing-7d) = universo 324 (cov≥95%) + máscara de liquidez $1M/15m trailing-7d por barra dentro del test, idéntico a H9/H10/H11. Script del test `agent/backtest/h12_taker_flow.py` ya escrito (params congelados, syntax OK). NADA de alpha mirado. OI collector sigue sano (ciclo 124, 0 fallos). H11 congelado.
- **H12 — EJECUTADO COMPLETO = FAILED** (artifact §22, `agent/backtest/h12_results.json`). Fix pre-alpha: `final_universe()` corregido a solo cov>=95% (324 símbolos), liquidez $1M/15m trailing-7d aplicada por barra dentro de `run()` (`F["liq"]=rolling(672).median()`, ya existía correcta, sólo el helper de universo tenía el bug). Implementation QA: máscara rolling causal confirmada, sin lookahead (fwd_h solo como outcome vía shift(-h), nunca predictor), splits/fechas sin cambios, OOS corrido en la misma pasada sin selección post-hoc. Corrida completa: 324 símbolos, 4.777.283 filas. **Resultado por la grilla A-H**: (A) señales continuas (TI por barra, CVD, aceleración) diminutas 0-4bp, hit rate 0.48-0.51 (moneda); eventos extremos (|z|>3) muestran oscilaciones grandes ±25-56bp. (B) **0/27 combinaciones señal×horizonte alcanzan el piso |partial corr|>=0.03 en los 3 splits mismo signo** — la mayor partial corr OOS de TODO el experimento es -0.022 (CVD 1h, horizonte 1h), por debajo del piso. (C) magnitud >=15-20bp solo en eventos extremos raros. (D) las continuas nunca superan ~4bp vs costo 8-16bp — no sobreviven porque nunca hubo efecto. (E) **placebo (+25 barras) reproduce 73% de la magnitud del único efecto grande en OOS (E_extreme z>=3, real -37.13bp vs placebo -27.10bp, mismo signo)** → régimen/vol común, no order-flow causal. (F) 28-60 símbolos por celda, sin sesgo direccional sistemático (hit rate medio 0.482 sobre 441 celdas); métrica de concentración top5 degenerada numéricamente cuando el agregado≈0 (no es lectura válida). (G) **ninguna señal mantiene signo entre Discovery/Validation/OOS** — todas flipean. (H) mecanismo a priori plausible pero sin operar de forma estable en los datos. **VEREDICTO ÚNICO: H12 = FAILED.** Se cierra la línea taker order-flow/CVD, sin H12.1/H12.2. El backfill (`taker_flow`, 9.3M filas, integridad 100%) queda como dato disponible para el futuro. H11/OI sigue acumulando en paralelo, congelado hasta su gate (>=70d, >=20 símbolos, >=90% cobertura). Ninguna estrategia/producción tocada.
## 2026-09-03
- Resumen: cerré H10 (basis) = FAILED tras bajar spot histórico; construí el procedimiento de verificación del OI collector; **preparé H11 (Open Interest) completo y congelado**. Ninguna estrategia/producción tocada. Detalle:
- **PASO 1 — verificación del OI collector: NO se pudo ejecutar, Docker Desktop caído en el host** (daemon no responde; todos los contenedores Verge abajo). `agent/verify_oi_collector.py` reporta NO OPERATIVO correctamente (0 filas, sin contenedor). Nada que arreglar en el pipeline. El usuario tiene que: iniciar Docker → `docker compose up -d oi-collector` → esperar ~10-20 min → `python agent/verify_oi_collector.py` (exit 0 sólo si pasan los 10 chequeos).
- **OI collector — LEVANTADO Y VERIFICADO (proceso host)**. Docker volvió; se intentó el servicio `oi-collector` pero falla en Windows: (1) imagen `verge-market-ws` es stale y no tenía el archivo → se le puso bind mount `./agent:/app`; (2) con el código montado, `sqlite3 disk I/O error` en `PRAGMA journal_mode=WAL` — **WAL no funciona sobre bind mount de Docker Desktop** (misma razón por la que market-ws/backtest usan volumen nombrado + DB separada). → **el collector corre como PROCESO DEL HOST** (`cd agent && python -u open_interest_collector.py`), escribiendo el `klines.db` nativo del host (mismo que lee el research; un solo writer, coordinado por WAL con funding_rates/agente). Servicio de compose desactivado + documentado el porqué. Bug menor arreglado: mezclaba `logging` (flushea) con `print` (block-buffered a archivo redirigido) → no se veía el progreso; ahora `run_oi_capture(agent_log=logger.info)`.
- **Backfill inicial OK**: 30 símbolos TIER1, **224.216 filas** (~26-30 días de OI 5m c/u) en ~5,5 min. Ciclos permanentes cada 5 min: 30/30 actualizados, 0 breaker, 0 fallidos.
- **`verify_oi_collector.py` — TODOS los chequeos ✅** (tras arreglar 2 métricas mal diseñadas): process_health (host proc + log fresco), rows_growing, **no_forming_bucket=0**, **no_duplicates=0**, **spacing 100% exacto 5m** (BULLAUSDT 8642 filas, max gap 5min), latency mediana **469s (~8min)** del bucket más nuevo por símbolo (normal para period=5m), gaps: **0 huecos del collector en los 30 símbolos líquidos** — 4 símbolos (BNCUSDT/MARAUSDT/SNOWUSDT/TSLLUSDT) son source-sparse (Binance devuelve OI esparso para perps finos/nuevos; H11 los filtra con OI_COVERAGE_MIN), restart/idempotencia: 2× upsert del mismo lote → 0 duplicados. **Collector OPERATIVO.**
- **OI collector — persistencia ante reinicios de PC** (usuario reinicia 2-3×/semana). Agregado `agent/oi_collector_run.bat` (lanzador con `pythonw` sin ventana + rotación de log a 20MB) + `agent/register_oi_collector_task.ps1` (registra la tarea de Windows `VergeOICollector`: trigger AtLogOn+30s, auto-restart cada 5min ×999, una sola instancia, sin límite de tiempo, sin admin, sin password guardado). Clave de resiliencia: cada arranque re-corre el backfill de 30 días (idempotente por PK) → **cualquier hueco < 30 días se rellena solo**; 2-3 reinicios/semana no cuestan historia si el colector vuelve en <30 días. Doc actualizada en `OPEN_INTEREST_COLLECTOR.md`. El usuario: `powershell -ExecutionPolicy Bypass -File agent
egister_oi_collector_task.ps1` → `Start-ScheduledTask -TaskName VergeOICollector` → (a los 10-20 min) `python agenterify_oi_collector.py` → después puede frenar la tarea background del harness.
- Nota: el proceso corre como background del harness — para el run de semanas conviene Task Scheduler / AgentProcessManager. `checkpoint`: correr `python agent/h11_oi.py --gate` en ~4-6 semanas; cuando pase (≥70d, ≥20 símbolos ≥90% cobertura) → `python agent/h11_oi.py` ejecuta el test congelado.
- **PASO 2 — H11 (Open Interest Information Test) METODOLOGÍA CONGELADA**: `agent/H11_OPEN_INTEREST_TEST.md` (spec completa) + `scratchpad/h11_oi.py` (script, syntax OK, gate --gate reporta UNTESTABLE correcto sin datos). **Gate del §0 (se evalúa primero, no se corre el test sin esto)**: ≥70 días de historia de OI + ≥20 símbolos con ≥90% cobertura → si no, UNTESTABLE y se dice cuánto falta. Params congelados: resolución 15m (OI 5m→15m cierre de bucket), z-lookback 96, Δ-lookbacks 1/4/16, buckets |z| fijos, horizontes fwd 4/16/48 barras, liq floor $1M/15m, costos fees+slip 0/2/5bp, placebo OI+25 barras, splits 50/25/25 (OOS intocable, fechas calculadas de min/max y escritas antes de ejecutar). 8 señales: nivel-z / ΔOI / %ΔOI / ΔOI-por-volumen / aceleración / precio×ΔOI (4 cuadrantes) / divergencia precio-OI / ΔOI×funding. Controles A(OHLCV)/B(momentum)/C(funding)/D(A+B+C)/**E=partial corr residualizando contra [retorno trailing, rango/vol, funding] = el gate incremental** (si colapsa al controlar funding como el basis → FAILED). **PASS necesita los 8**: causal (|pcorr|≥0.03, mismo signo 3 splits) · incremental · estable temporal · amplitud ≥15-20bp/rebalanceo (1-3bp = FAILED aunque sea significativo) · ≥20 símbolos ≥100 eventos/bucket · sobrevive costos · top-5 <60% · mecanismo económico. Verdict PASS→diseñar estrategia (paso aparte) / FAILED→cerrar OI como fuente de alpha / UNTESTABLE→decir qué falta. Sin cuarta opción. Mientras OI acumula, no se abren investigaciones nuevas sobre OHLCV.
- **PASO 1 — procedimiento de verificación del OI collector**: `agent/verify_oi_collector.py`. Chequea 10 cosas (NO alcanza con "Docker Up"): container running + health + restart count, scan de logs (ciclos, errores HTTP, 429/418, breaker skips, crashes), filas creciendo (2 lecturas + `updated_at` reciente), **ningún bucket en formación** (`timestamp > now - 300000`), sin duplicados (PK + verificación), spacing exacto de 5m ≥95% sobre el símbolo top, latencia de captura (mediana `updated_at - timestamp/1000` < 20min), gaps + cobertura por símbolo (flag <95%), cobertura de universo ≥20 símbolos, y `--restart-test` (docker restart → reanuda idempotente sin duplicar ni re-fechar). Exit 0 solo si TODO pasa; críticos = container_health/rows_growing/no_forming_bucket/no_duplicates/spacing_ok. Dry-run contra la tabla vacía OK (reporta NO OPERATIVO correctamente). El usuario levanta `docker compose up -d oi-collector` y corre esto tras ~10-20 min.
- **PASO 2 — H10 (BASIS) auditoría de datos = UNTESTABLE con los datos actuales**. Todas las klines históricas locales son **USDⓈ-M FUTURES** (`download_binance_vision_daily_all_v2.py` línea 22: `data.binance.vision/data/futures/um/...`). `klines_multi_exchange` = perps de bybit/bitget/okx. `live_prices` = perps. **NO hay klines SPOT en ningún lado** → basis clásico (spot − perp) no es construible. El único proxy de basis que tenemos (funding rate, que Binance deriva del premium perp-vs-spot-index) ya se testeó → EVIDENCE/mitigador, no edge. H10 NO permanentemente muerto: el spot histórico se puede bajar de `data.binance.vision/data/spot/daily/klines/...` (archivo CDN público, NO es el vector de ban de `fapi.binance.com` — el proyecto ya usa data.binance.vision como el método seguro). Es una tarea de DATA ACQUISITION (tipo Track A), no research. Pendiente decisión del usuario: bajar spot y hacer H10 testable, o dejarlo UNTESTABLE y esperar a OI.

- **Track A — backfill de klines SPOT** (`agent/download_spot_klines.py`, solo `data.binance.vision/data/spot/...`, NUNCA fapi.binance.com). Probe: de los 429 símbolos futures, **240 tienen par spot con nombre exacto**; 189 futures-only (1000x renombrados, stock-tokens AAPLUSDT/etc, alts nuevos). Historia spot monthly disponible 2025-11 → 2026-07 para toda la muestra → cubre la ventana común con el panel de futures. Backfill 15m (dec-2025→17-ago-2026) a tabla NUEVA `spot_klines` en binance_vision_clean.db (aditiva, no toca `klines_clean`). Corriendo en background.
- **H10 (BASIS) — metodología PRE-DECLARADA** (`scratchpad/h10_basis.py`, antes de ver resultados): basis(t)=ln(perp_close/spot_close). Variantes: level / Δbasis(1h) / z-score causal (96 barras). Buckets |z| fijos [0-1)(1-2)(2-3)(3+). Horizontes forward 4/16/48 barras (1h/4h/12h). Liq floor perp $1M/15m. Splits discovery(dic→mar)/validation(abr→may)/**OOS final(jun→17-ago) intocable**. Controles A(perp momentum solo)/B(funding solo)/C(basis)/D=incremental (partial corr de basis con retorno perp forward controlando por [perp momentum, funding]). Placebo = spot desplazado +25 barras. También mide si el basis-z predice mean-reversion del basis mismo. PASS solo si: graduado/monotónico + consistente en 3 splits + partial corr incremental >0 en OOS + supera placebo + ≥20 símbolos no-top5 + sobrevive costos + magnitud > piso de costo. Gate: si el efecto vive entero en horizonte 1 → potencialmente no monetizable (= H9.2). Sin grid search, sin elegir variante tras ver OOS.

- **H10 (BASIS) — EJECUTADO = FAILED** (`scratchpad/h10_basis.py` + `h10_results.json`, artifact §20). Backfill spot OK: 240 símbolos, 5.8M velas 15m (archivos SPOT de Binance Vision están en **microsegundos** — normalizado a ms). 37 símbolos ≥95% cobertura, ~200 con ~54% (listados spot recientes). Alineación timestamp 100% en majors. **Resultado**: (1) test incremental — la correlación parcial de basis con retorno perp forward es ~0 en discovery, pequeña-negativa en validation, **+0.04 a +0.08 en OOS PERO se invierte a −0.05 al controlar por FUNDING** → basis y funding son la misma información (el funding de Binance ES el premium perp-vs-spot); H10 = funding re-derivado de klines, no fuente nueva. (2) señal direccional del nivel = degenerada (basis casi siempre negativo → sign(basis)≈constante → mide deriva de mercado, misma clase que rank-invarianza de H8). (3) el basis SÍ mean-revierte (monotónico en |z|, consistente 3 splits, hit rate creciente) — pero 1-4 bp buckets comunes vs 8-16 bp round-trip = **SEÑAL REAL NO MONETIZABLE, igual que H9.2**. (4) placebo (spot +25 barras) no se supera limpio en discovery/validation. **H10 = FAILED** — no incremental (es funding), no estable, no sobrevive costos. El backfill de spot (`spot_klines`) queda como dato testeable para futuras hipótesis. Track A (OI collector) sigue acumulando, sin usar.

## 2026-09-01
- Nota meta: la sesión de investigación cuantitativa (Pases 01–03) quedó registrada abajo bajo `## 2026-08-31` porque arrancó guiándose por la fecha de los artifacts; el reloj real del sistema es 2026-09-01. Los pases 01/02/03 (dossier de contexto, H1 router de régimen = FAILED, auditoría del motor de backtest para H7) son todos de esta sesión — ver detalle completo en la sección de abajo.
- **Pase 03 cerrado**: auditoría de `engine.py` + matriz de cobertura + test de reproducibilidad de MA Slope Caso 3 (reproduce OK, PnL dentro del 13%). Hallazgo clave: el motor solo cubre con fidelidad 4 familias (~59% del libro); las familias Nexus (~36%, las de más plata real) no tienen camino histórico. Artifact `verge-research-log.html` §11.
- **Pase 04 — GAP ANALYSIS + subset baseline** (artifact §12–13, link sin cambiar). Gap analysis por familia (8 preguntas c/u) con decisión A/B/C: **PDH Sweep, Death Cross, Arrow Reversal = A (reconstruir, fidelidad ~90-95%, esfuerzo bajo — son OHLCV puro en `verge_agent.py` o analyzer monkeypatcheable; ojo: PDH/DC minadas sobre su propia ventana → holdout obligatorio)**. **LSE = B (~75% fidelidad — tiene máquina de estados por símbolo + cooldowns + gate de batch, medio-alto esfuerzo)**. **Standard/Scalping Clone/MA Cross/MA Clone = C para panel fiel / B para spot-check acotado** (dependen de `/nexus15/analyze` — llamable pero: Nexus15FeatureEngine cambió de versión en el período, Bridge/superscore sin registro histórico ~9%, SCAR sin historia ~20-36%; escala completa = 10M llamadas HTTP ≈ 40-80h prohibitivo). **SCAR = C UNTESTABLE** (proxies usan snapshots de OI + ratio volumen 24h nunca archivados). Conclusión formal: reconstruibles ≥75% suben cobertura a ~66%; **nunca se podrá afirmar limpio sobre el edge GLOBAL solo con backtest**. Agregada al artifact la clasificación FACT/EVIDENCE/HYPOTHESIS/FAILED/UNTESTABLE (ledger de todos los hallazgos). Subset baseline (4 familias fieles, 8,5 meses, raw vs realistic con slippage 0/0.02/0.05% + funding real + MAE/MFE + intrabar conservador TP-y-SL-en-misma-vela→SL) **corriendo en background** (`scratchpad/pass04_baseline.py` → `pass04_report.json`/`pass04_dataset.jsonl`). Sin optimizar nada. Pase 05: publicar baseline + ejecutar decisión A (runners PDH/DC/Arrow).

## 2026-08-31
- **Dossier de contexto del sistema** publicado como artifact (`verge-dossier.html`) — onboarding completo para alguien externo: arquitectura, agente 24/7, fuentes de datos, defensa anti-ban, creación de estrategias, estado real. No toca código.
- **Misión de investigación cuantitativa — Pase 01 (auditoría)**, publicado como artifact `verge-research-log.html`. Todo contra datos locales (Postgres `verge-db` + `binance_vision_clean.db`/`klines.db`), cero llamadas a Binance (IP compartida con el agente vivo). Hallazgos: (1) MA Slope Caso 3 se degradó a PF 0.34/−$28.80 en 30 días — ninguna estrategia tiene edge standalone estable, el rendimiento rota; los que ganan ahora son Level Sweep 15m SL3 RR3 (PF 1.51), Death Cross (1.46), FVG-15m (1.17). (2) Confirmadas 4 vías muertas: grid-search OHLCV (0/41.369 configs pasan robustez), ML ranking sobre candidatos (−$177/mes OOS), meta-labeling level_sweep (AUC 0.52), breakeven-desde-apertura (PF 0.02). (3) Mina MAE/MFE: edge de salida delgado — solo 4.7% de perdedores llegan a ≥70% del TP; ganadores no se cortan cortos (MFE/TP≈1.03); SL bien dimensionado. (4) Backlog de 6 hipótesis rankeado; H1 en curso = router de régimen sobre las familias hoy positivas (asignación de cupos por régimen, no filtro de entrada).
- Nota lateral (bug, no tocado): 4 filas `StrategyProfile` idénticas `ma_pullback 240m LONG (evolutivo)` activas en paralelo — limpiar antes de cualquier A/B.
- **Pase 02 — H1 (router de régimen) = FAILED**, documentado en el artifact `verge-research-log.html` (mismo link, actualizado). Método: régimen BTC causal (retorno 72h, umbrales fijados de antemano), walk-forward expandido de 5 bloques, controles A–E incl. router aleatorio. Resultados: router pierde en los 3 folds, PF OOS 0.76, **peor que un router aleatorio** en 2 de 3 folds, 0% meses positivos; cero persistencia de liderazgo H1→H2 (el líder de cada régimen desaparece o cambia de signo). Bloqueo de datos: las familias no se solapan temporalmente (level_sweep/death_cross/band_touch/pdh_sweep tienen <20 días de datos vivos c/u). El único efecto del router es operar menos de un libro de EV negativo — no es alpha. Libro completo pierde vs BTC buy&hold (+19.8%) en el tramo OOS.
- **Diagnóstico señales no-OHLCV** (`pass02b.py`, scratchpad): join causal de OFI (`orderbook_ofi`, ts en segundos no ms) y funding (`funding_rates`) a los 4763 trades. OFI: AUC 0.46 = sin separación (REJECT, confirma el A/B viejo con muestra grande). Funding×dirección: AUC 0.539 IS pero colapsa a PF 0.98 OOS — mitigador de pérdidas, no edge (REJECT como edge).
- **Conclusión estructural (2 pases)**: no hay edge robusto explotable en los datos locales actuales — grid-search (0/41369), ML ranking, meta-labeling (AUC 0.52), router de régimen, exits, OFI, funding, todos negativos/nulos. Próxima vía elegida por evidencia: **H7 = backtest de 8 meses de TODAS las familias con el motor real** (`agent/backtest/engine.py` sobre `binance_vision_clean.db`) para tener familias que se solapen en el tiempo y recién ahí poder testear régimen/portfolio con walk-forward de verdad. H8 = reparar/evaluar OFI en serio. Scripts en scratchpad de la sesión: `pass02.py`, `pass02b.py`.
- **Pase 03 — auditoría de H7 (motor de backtest)**, en el artifact `verge-research-log.html` §11. Hallazgos: (a) `engine.py` reusa código de producción de verdad, sin lookahead, pooling correcto; datos `binance_vision_clean.db` = klines_5m + klines_clean(15m), 429 símbolos, 2025-12-01→2026-08-17. (b) **Gaps de fidelidad**: slippage NO se modela, funding NO se aplica al PnL (solo veto), MAE/MFE NO se emiten (falta walk post-trade), TP-antes-de-SL en misma vela optimista, btc_filter desactivado, perfiles hardcodeados en scripts `verify_*` pueden diferir de la DB. (c) **Cobertura de familias — hallazgo duro**: solo 4 familias tienen camino fiel (FVG, MaGeometry, ADN, OrderBlock ≈ 59% del libro). Las familias **Nexus/SCAR/LSE** (scalping 867, ma_cross 469, lse 129, arrow 20 ≈ 36% del libro, las de más plata real) **no tienen camino histórico** — requieren armar pipeline histórico de Nexus-15/LSE. Level Sweep/Band Touch/MA Pullback solo vía `strategy_lab.py` (sim a mano, no `_run_generic`) → marcar como aproximados. PDH Sweep / Death Cross sin camino. (d) Matriz de cobertura del libro real: solo scalping y ma_cross cubren jun+jul+ago; el resto vive 1 mes — misma razón por la que el walk-forward de H1 fue imposible. **Alcance de H7 revisado** al panel fiel de 4 familias. Test de reproducibilidad de MA Slope Caso 3 (motor vs libro real, ventana 2026-07-11→08-17, 429 símbolos): **reproduce OK** — P&L mensual coincide en signo y magnitud (jul motor +72.27 vs real +75.69; ago −17.06 vs −11.96; total motor +55.21 vs real +63.73, dentro del 13%), trades de julio 55 vs 58. Agosto sobre-genera (42 vs 15) por competencia de cupos en el libro real (backtest da los 3 cupos a una familia sola) — diferencia estructural, no bug. WR diverge por etiquetado (motor: win=TP exacto; libro: PnL>0). Conclusión: confiable para comparación relativa entre familias y PnL direccional, con nº de trades inflado y PnL como techo optimista (sin slippage/funding). Pase 04: correr el panel de 8 meses (4 familias fieles) con MAE/MFE + sensibilidad de slippage + funding, dataset persistido, sin optimizar.

## 2026-08-23
- **Order Block re-validado con el motor REAL** (`backtest/engine.py::run_order_block`, nuevo — reusa `risk_manager.py`/`setup_validator.py` reales en vez del script suelto de ayer): resultado se cae, da **negativo** (-$5.89/mes 15m, -$8.05/mes 1h, n~530/139, no estable) contra el +$92.26/mes optimista de `orderblock_backtest.py`. Se probó y descartó la hipótesis de `maxTradeDurationCandles` como causa (16/96/300 velas, todos negativos). Conclusión: **no productivizar Order Block por ahora** — el número de ayer no sobrevive el stack real de vetos (`validate_pre_trade`) + `MinRR` del perfil. Se agregó igual el flag `order_block_mode` en `risk_manager.py`/`setup_validator.py` y `_build_order_block_candidate` en `verge_agent.py` (patrón de inyección directa completo, por si se retoma), pero sin `StrategyProfile` creado — queda solo como capacidad del motor.
- **Bug real encontrado en mi propio proceso de validación de FVG**: comparé el motor contra producción usando parámetros de perfil INVENTADOS (`MinRR=1.5`, `maxTradeDurationCandles=16`) en vez de los reales de "FVG - 15m" (`MinRR=3`, `maxTradeDurationCandles=60`, ver DB) — y encima con un universo de solo 40 símbolos por capitalización en vez de los ~429 reales del watchlist dinámico. Ambos corregidos (universo real vía `engine.available_symbols()` + `run_parallel`, params reales de la DB) — resultado de esa corrida pendiente al cierre de esta sesión.
- **Limpieza**: ~24 procesos `bash.exe` zombies quedaron corriendo horas desde una sesión anterior (loops `until grep ... sleep 30; done` esperando logs de corridas ya muertas/nunca lanzadas) — identificados y matados.

## 2026-08-22
- **Herramienta nueva "Order Block + BOS + Liquidez"** (concepto SMC/ICT distinto de FVG, investigado en internet a pedido explícito del usuario): última vela opuesta antes de un quiebre de estructura (BOS), TP en el próximo nivel de liquidez real, filtro de confluencia con POC/HVN de volumen. Backtestada contra `orderblock_backtest.py`: SHORT-only + filtro POC es la única config estable (`stable_between_halves=True`), $92.26/mes, n=3272, WR 29.4%. LONG no pasó el filtro de estabilidad, se muestra igual (informativo) marcado `validated_stable=false`.
- **Llevada a producción completa, no solo backtest**: módulo FastAPI nuevo `python-service/orderblock/` (detector + analyzer + endpoints `/analyze` y `/scan`, mismo patrón que `fvg/`), capa .NET nueva `Verge.Trading.OrderBlock` (AppService + PythonOrderBlockService + DTOs, mismo patrón que Fvg) expuesta en `/api/app/order-block/*`, y página Angular nueva `angular/src/app/order-block/` (análisis por símbolo + scan Top-5) registrada en `/order-block`.
- Para que quedara visible en la app real (no solo verificado por script suelto) hubo que: rebuildear la imagen Docker `verge-python-ai` (sin volumen montado, un restart simple no alcanza) y reiniciar el proceso `Verge.HttpApi.Host.exe` (quedó ~1 min caído). Verificado end-to-end con curl real contra los 3 saltos (Angular→.NET→python-service→Binance) — encontró una señal SHORT real en BTCUSDT en vivo.

## 2026-08-19
- **Bug de reproducibilidad grave encontrado en `validate_pre_trade`**: el veto de agotamiento diario (`daily_pump_exhaustion`/`daily_dump_exhaustion`, `setup_validator.py`) pegaba SIEMPRE a la API en vivo de Binance pidiendo el cambio de 24h de AHORA MISMO, sin importar qué fecha histórica simulaba el backtest — contaminaba todo resultado que pasara por `validate_pre_trade` (grid search, laboratorio evolutivo) con el estado del mercado real al momento de correr el script. Confirmado: el mismo backtest exacto daba $104.73/mes una corrida y -$4.84/mes otra. Fix: `candidate["historical_daily_change_pct"]` como override — `strategy_lab.py` ahora calcula el cambio real histórico desde las velas, producción real no setea ese campo y sigue como antes. Sumado a que `MODE_FLAG_BY_ENTRY_TYPE` del lab nunca se había actualizado con `rsi_extreme_mode`/`ma_pullback_mode` (agregados a producción esta noche) — sin el flag cayían en el veto `disabled_signal_for_tier`, dependiente del watchlist en vivo (no determinístico). Con ambos fixes, 4 de 5 patrones dan resultados idénticos entre corridas (queda `ma_cross` pendiente, no tiene flag real todavía). El "techo" de $104.73/mes de anoche era falso — el real es $65.84/mes.
- **Bug real encontrado en producción (no backtest) auditando trades reales**: de 3371 trades cerrados en toda la historia, solo 1 cerró por TP (`tp_hit`) — 2496 por SL, 630 por timeout. 140 trades llegaron a ≥70% del camino al TP y terminaron en pérdida total (-$227 en 45 días). Implementé un lock a breakeven en `SimulationMarkPriceWorker.cs` (mueve el SL a breakeven solo una vez que el trade llega a 75% de progreso hacia TP) — **se desplegó sin backtestear primero por error, corregido por el usuario, y desactivado de nuevo** hasta validarlo candle-a-candle. Backtesteado después contra el motor ya arreglado: ayuda a 2 de 4 patrones (Band Touch +21.90, MA Pullback +19.30), perjudica a RSI Extreme, no alcanza a sacar a Level Sweep de negativo — no es un fix universal, falta decidir si se reactiva selectivamente.
- **Historial `klines_clean` estaba desactualizado desde el 31/jul** (un mes de atraso) — bloqueaba cualquier backtest de fechas recientes. Sincronizado con `data_sync.py` (ya existía, nunca se había corrido para esto) hasta el 17/ago, 700k velas nuevas.
- Encontrado también: campo `UnrealizedPnl` en `SimulatedTrades` queda en 0 para posiciones abiertas a pesar de que el precio se mueve — bug de cálculo/persistencia separado, no arreglado todavía (hubo que calcular el PnL flotante real a mano con EntryPrice/MarkPrice para verificar los números que el usuario veía en el dashboard).
- **Bug real de fidelidad en `strategy_lab.py::atr_series`**: usaba ATR suavizado de Wilder (memoria de toda la historia previa) mientras producción real (`_run_level_sweep_1h_scan` y las demás en `verge_agent.py`) usa un promedio simple de las últimas 14 TR — SL/TP distintos, encontrado verificando señal por señal contra trades reales de la cuenta del usuario (APRUSDT tenía el TP desalineado 32%). Arreglado para matchear producción exacto. Verificación directa (11 posiciones reales vs detección offline) también encontró: 5 símbolos sin cobertura en la base histórica, 2 casos donde el motor no detecta una señal que producción sí abrió (producción evalúa velas de 60m a medio formar, no cerradas — diferencia estructural, no bug de cálculo, pendiente decidir si se corrige el comportamiento real).
- **5 estrategias reales desplegadas y activadas** (3 Level Sweep + 2 Band Touch, todas SHORT) con los números recalibrados post-fix — laboratorio evolutivo reiniciado de cero (archivos viejos contaminados archivados, no borrados) para que la UI deje de mostrar los `$/mes` falsos de antes del fix.
- **Bug real en el log del agente** (`verge_agent.py`): la rotación de `agent.log` a medianoche fallaba (archivo bloqueado por otro proceso) y el handler reintentaba en CADA línea de log siguiente en vez de esperar — miles de errores en cascada por hora. Arreglado con cooldown de 5 min entre reintentos.
- **Laboratorio evolutivo corrido ~2 días, techo real ~$125/mes (`ma_pullback` 240m)** — confirmado que es un límite estructural: el "laboratorio" es un buscador de parámetros dentro de 5 plantillas fijas, no puede encontrar nada fuera de lo que esas plantillas ya expresan.
- **`ml_lab.py` (nuevo, primer intento real de modelo entrenado)**: LightGBM sobre ~14 features crudas (RSI, distancias a medias, ATR%, %B Bollinger, momentum, volumen relativo, hora/día) con split cronológico train/test real (nunca mezclado), en vez de reglas escritas a mano. Dataset: 2.536.957 muestras (430 símbolos, stride 1h). **Resultado honesto: AUC test 0.53 (~azar, 0.50=random)** — el modelo no encontró un edge real con estas features. Al umbral de confianza más alto solo daba 2-0 señales; al más bajo, 154 señales con WR 51.3%/R+1.05 pero es una muestra demasiado chica para confiar (puede ser ruido). Conclusión: el espacio de features técnicas clásicas es débil, no un problema del método de búsqueda — próximo paso pendiente es alimentar el modelo con los detectores más sofisticados ya existentes (Nexus15/SCAR/LSE) en vez de indicadores genéricos.
- **Encontrado también: `whale_events` tiene el mismo bug de timestamp corrupto (epoch 1970) que `orderbook_ofi`** — ninguna de las dos tablas se puede alinear con precio real todavía, descartadas de los experimentos de hoy. Bug de datos separado, no arreglado.
- **`ml_lab_v2.py` y `ml_lab_ranked.py` (nuevos)**: v2 agrega fuerza relativa vs BTC, micro-momentum de 1-3 velas, distancia a máx/mín de 24h y volumen como z-score al set de features de v1. `ml_lab_ranked.py` fusiona el modelo v1 ya entrenado con el mecanismo real de FVG (competencia entre TODO el universo por tick, top-3 por probabilidad, no umbral fijo) — corrido SOLO sobre el tramo out-of-sample real. **Resultado de `ml_lab_ranked`: solo 2 trades en 78 días, ambos perdedores, -$8.64/mes** — confirma que el AUC 0.53 de v1 es demasiado débil para que la competencia entre símbolos ayude (casi nada supera el umbral mínimo de confianza). Nota técnica: la primera versión de `ml_lab_ranked.py` predecía símbolo por símbolo (miles de llamadas al modelo por tick) y hubiera tardado horas — reescrita para predecir en lote (una llamada por tick), terminó en ~3 min. **Resultado de `ml_lab_v2`: AUC test 0.5269, prácticamente igual a v1 (0.5298)** — las features nuevas (fuerza relativa vs BTC, micro-momentum, distancia a extremos de 24h) no mejoraron nada. Conclusión de las 3 corridas de esta noche (v1, v2, rankeado): con velas de 15m + indicadores derivados de precio/volumen, no hay señal explotable real, en ninguna de las 3 formas de buscarla. Para avanzar de verdad haría falta primero arreglar el bug de timestamps de `orderbook_ofi`/`whale_events` (dato real, sin usar).
- **Corrección: el "bug de timestamps" de `orderbook_ofi`/`whale_events` reportado antes era mío, no del proyecto** — `kline_cache.py::upsert_ofi/insert_whale_event` guardan `int(time.time())` en SEGUNDOS, y mi verificación rápida dividía por 1000 de más asumiendo milisegundos (por eso daba 1970). Con la unidad correcta: `orderbook_ofi` tiene **1 mes real de historia (22/jul-21/ago), 2.5M filas, 462 símbolos** — dato de microestructura real, nunca usado como feature de entrada hasta ahora. `whale_events` solo 3 días, 31 símbolos (insuficiente todavía, pero no corrupto). **Resultado de `ml_lab_v3` (con OFI real): AUC test 0.5489 — el mejor de los tres (v1=0.53, v2=0.527), y OFI salió como la feature MÁS importante del modelo.** Es una mejora real, aunque todavía chica (dataset de solo 1 mes por la ventana de cobertura OFI, 254k muestras vs 2.5M de v1/v2). A umbral de confianza 0.55 el modelo no llega a predecir tan alto con este dataset chico (0 señales) — **Encontrado con detalle de percentiles: hay señal real y monotónica** — top 1% por probabilidad del modelo: WR 36.03% (vs base 22.87%), avg R +0.441; top 5%: WR 30.23%, avg R +0.209; degrada limpio hasta top 30% ≈ base rate. El modelo nunca predice por encima de 0.52 (dataset chico), por eso un umbral fijo no servía — hay que rankear por percentil, no por umbral absoluto. **Primera corrida de `ml_lab_v3_ranked.py`: 20 trades en solo 8 días out-of-sample, WR 0%, -$211/mes.** Dos problemas reales: (1) bug metodológico encontrado en el propio script — calculaba el percentil 95 tick-a-tick sobre el pool chico de candidatos disponibles en ese instante, no contra la distribución global de probabilidad (no es lo mismo, corregido a un umbral fijo real p95=0.3415 sacado del análisis offline completo); (2) la ventana out-of-sample real es de solo 8 días (por el split 70/30 dentro del único mes de cobertura OFI) — 20 trades es una muestra demasiado chica para confiar en el resultado en cualquier dirección. Recorrida con el umbral fijo corregido: **mismo resultado, 21 trades, 0% WR, -$176.95/mes** — confirma que no fue el bug del percentil tick-a-tick. Investigando la causa real: el análisis offline por percentil (top 1%: WR 36%) se midió sobre una ventana de 6.3 días (10-16 ago), pero la simulación rankeada walk-forward cubrió una ventana distinta y más tardía dentro del mismo mes (8 días) — **la señal encontrada en una ventana no se repite en la ventana siguiente**, la prueba más clara de que es ruido de muestra chica alineándose por casualidad, no un edge real todavía. Conclusión de la noche: el OFI es un dato genuinamente nuevo y prometedor en el análisis offline superficial, pero con solo 1 mes de historia real no alcanza para confirmar que sea tradeable — hace falta que se siga acumulando (la captura ya corre sola en background) antes de volver a intentar esto. Usuario autorizó explícitamente seguir toda la noche sin pedir permiso, incluso después de este resultado negativo.
- **`meta_label.py` (nuevo)**: técnica de meta-labeling (Lopez de Prado) nunca probada acá — filtro sobre las señales que ya genera Level Sweep 15m SL3/RR2, en vez de que el modelo invente señales desde una base débil. **Resultado real (358k señales RAW, 41 días out-of-sample, motor real con vetos+3 cupos): la estrategia base, que promedia +$62.13/mes en los 260 días completos, dio -$1267.41/mes específicamente en estos últimos 41 días** — confirma con datos nuevos (no solo intuición) que hay fuerte no-estacionariedad: el rendimiento de esta estrategia varía muchísimo según la ventana de tiempo. El filtro del meta-modelo SÍ ayuda direccionalmente (corta la pérdida ~83%, de -$1267 a -$220/mes con el umbral de mediana) pero no la vuelve positiva, y el AUC del meta-modelo es igual de débil que los anteriores (0.52).
- **Conclusión de la noche completa (5 técnicas distintas probadas: parámetros fijos exhaustivos, ML desde cero v1/v2/v3, ranking competitivo, meta-labeling)**: todas convergen en el mismo techo de información (AUC 0.52-0.55, apenas mejor que azar) usando indicadores derivados de precio/volumen a 15m. La causa más probable no es falta de método sino que ese nivel de datos genuinamente no contiene una señal fuerte explotable en este mercado — se necesitarían datos de microestructura con más historia (OFI ya tiene 1 mes, necesita más) o una fuente de información distinta a la que ya se probó. No se encontró ninguna estrategia nueva que supere las 5 ya activas. Sistema quedó estable: 5 estrategias activas sin cambios, ningún experimento se desplegó a producción.
- **Pedido explícito del usuario a la mañana siguiente: desplegar las 3 mejores del laboratorio evolutivo aunque no pasen el filtro de estabilidad** — MA Pullback 240m LONG SL3x/RR4x, 3 variantes casi idénticas (solo cambia `slope_min_pct`: 1.0/0.6/1.5), $125.94/$124.28/$121.48 mes teóricos, n=50-51 trades en 260 días, `passes_bar=False` en las 3 (no estables entre mitades). Desplegadas y activadas vía `lab_control_server.py` (que se había caído solo, reiniciado antes de desplegar). Total: 8 estrategias activas en producción.
- **Usuario pidió borrar esas 3 y buscar una estable — 0 encontradas** en todo `lab_tested_all.jsonl` (`passes_bar=True` en ningún registro de la corrida de anoche). Peor: **re-verificando las 5 estrategias Level Sweep/Band Touch desplegadas anoche con los mismos parámetros exactos, los números NO reproducen** (Level Sweep 15m SL3/RR2: $62.13/mes anoche → $17.01/mes ahora, n=482→541; Level Sweep 15m SL3/RR3: $57.53→-$30.81/mes) y ninguna pasa `passes_bar`. Confirma que sigue habiendo una fuente de no-determinismo en el motor sin encontrar todavía, a pesar de los fixes de anoche (API en vivo, ATR, MODE_FLAG). Las 3 nuevas de MA Pullback fueron borradas de producción. **Causa real encontrada y arreglada: los 5 scripts `grid_search_*.py` tenían `DAYS=243` hardcodeado**, stale desde antes de sincronizar `klines_clean` anoche (ahora ~260 días reales) — dividir la ganancia entre menos días de los reales inflaba el $/mes de todo lo que salió de esos scripts (no era no-determinismo del motor: dos corridas idénticas con los mismos parámetros dieron el mismo resultado, $17.01/mes, dejando claro que el motor en sí es reproducible). Arreglado en los 5 scripts (ahora calculan los días reales desde la DB en vez de un número fijo). **Las 5 estrategias Level Sweep/Band Touch desplegadas anoche fueron desactivadas** (`IsActive=false`, no borradas) tras confirmar que ninguna sostiene su número real ni pasa el filtro de estabilidad. Estado final: 0 estrategias personalizadas activas en producción — vuelto a las estrategias originales del sistema.
- **Hallazgo real nuevo (usuario pidió investigar un concepto mejor que FVG, no otro parámetro)**: investigado en internet qué usan hoy las herramientas serias de trading (Order Block + Market Structure Shift/BOS, familia ICT/SMC igual que FVG pero geométricamente distinto — no es un gap de 3 velas, es la última vela contraria antes de un impulso que rompe estructura). Construido `python-service/orderblock/detector.py` (detección real de swings confirmados + BOS + Order Block, sin look-ahead) y `agent/backtest/orderblock_backtest.py` (backtest honesto, SL estructural del propio OB + TP de liquidez real ya validado en Level Sweep). **Resultado: $80.89/mes combinado (LONG+SHORT), ambas mitades positivas pero no estable en conjunto — separado por lado, SHORT solo da $15.64/mes y SÍ es estable (ambas mitades positivas y proporcionadas), LONG da $50.15/mes pero inestable (mismo patrón de siempre: la segunda mitad domina).** Primera vez en toda la sesión que un concepto nuevo (no parámetro reciclado) da señal real. **Afinado con el filtro POC/HVN (ya construido para Level Sweep) sobre el lado SHORT: salto grande, $92.26/mes, 3272 trades, WR 29.4%, ESTABLE de verdad (mitades +$431.45 y +$356.52, 83% de proporción) — el mejor resultado de toda la sesión, con muestra grande y concepto real.** LONG+POC y BOTH-sides+POC se cortaron (corridas anormalmente lentas/con CPU casi nula por momentos, posible cuello de botella de I/O — no investigado a fondo, se priorizó no seguir gastando tiempo dado que LONG solo ya era conocido inestable sin el filtro). **Config final: Order Block + BOS + TP liquidez + filtro POC/HVN, SHORT-only — $92.26/mes, 3272 trades, WR 29.4%, estable — el mejor resultado real de toda la sesión.** **Interfaz completa construida y probada de punta a punta contra datos reales en vivo**: `python-service/orderblock/` (detector.py, analyzer.py, schemas.py, router en `__init__.py`), mismo patrón exacto que `fvg/` — endpoints `/orderblock/analyze` y `/orderblock/scan`, registrados en `main.py`. `only_validated=true` (default) filtra a solo Order Blocks bearish/SHORT (la única dirección con backtest estable). Probado contra BTCUSDT real: encontró una zona bearish APPROACHING, score 80, SL/TP calculados correctamente. Falta rebuildear el contenedor `verge-python-ai` (sin volumen montado, no tiene hot-reload) para que estos endpoints queden disponibles en la app real — pendiente de confirmación del usuario antes de hacerlo.
- **Investigación a fondo de por qué FVG gana de verdad** (pedido del usuario, en vez de seguir clonando "compite contra el universo" que ya se probó y falló): leído `python-service/fvg/analyzer.py::_liquidity_target` completo — el TP de FVG nunca es un múltiplo fijo de ATR (como TODO lo probado hasta ahora en el lab/ML), es el nivel de liquidez real (swing high/low del impulso que formó el gap), con detección de impulso viejo desproporcionado (recorta a la mitad si el extremo de toda la ventana es mucho más lejos que el alcance de la pata reciente) y un haircut del 10% siempre. `level_sweep_liquidity_tp.py` (nuevo) prueba esa misma lógica de TP aplicada a Level Sweep (conceptualmente parecido: los dos "barren un nivel") en vez de su TP genérico ATR×RR — **Resultado SHORT-only: 743 trades, WR 48.05% (vs 35.12% con TP fijo ATR×RR), $21.08/mes, y las DOS mitades del período dan positivo** ($148.56/$31.46) — no pasa el filtro estricto de estabilidad (segunda mitad no retiene el 40% de la primera) pero es la primera vez en toda la semana que un cambio deja las dos mitades del lado positivo en vez de una compensando a la otra. Probado LONG también: mucho peor (-$42.98/mes vs +$21.08/mes SHORT-only, mismo patrón ya visto con FVG y otras estrategias del proyecto — LONG casi siempre pierde acá). Vuelto a SHORT-only. **Barrido de 7 combinaciones completado — encontrada la primera estrategia genuinamente estable de toda la sesión**: con `disproportion_ratio=1.2` (más conservador que el 1.5 real de FVG), Level Sweep 15m SHORT + TP liquidez da n=858, WR=47.3%, $9.79/mes, **`stable_between_halves=True`** (mitades parecidas: $45.67 y $37.95, no una compensando a la otra) — no pasa el umbral mínimo de $/mes del filtro estricto pero es la primera vez en toda la semana que algo resulta genuinamente estable en el tiempo, no una casualidad de ventana. El resto de las variantes probadas (haircut 0.7/0.8, lookback 20/60, disprop 2.0) todas metieron una mitad negativa — la combinación original (parámetros iguales a los reales de FVG) y esta variante conservadora son las únicas dos que no. Probados también 60m y 240m con la misma config estable: ambos peores y no estables (60m: -$9.93/mes; 240m: +$16.58/mes pero con una mitad muy negativa) — 15m sigue siendo el único caso genuinamente estable. Conclusión de esta línea: encontrado un mecanismo real (TP por liquidez real en vez de ATR×RR fijo) que SÍ logra estabilidad genuina, aunque modesta en $/mes ($9.79). Probado en Band Touch (`band_touch_liquidity_tp.py`, nuevo): en 15m da peor que su baseline (n=1361, -$17.82/mes, las DOS mitades negativas) — el mecanismo de TP por liquidez real no se transfiere automáticamente a cualquier patrón, funciona específicamente para Level Sweep (probablemente porque ambos comparten la lógica de "barrer un nivel", pero Band Touch es un patrón de reversión a la media, conceptualmente distinto). 60m tampoco funciona (n=375, $1.58/mes, sigue inestable) — confirmado que el mecanismo es específico de Level Sweep, no generaliza. **Cierre de esta línea**: único hallazgo real de la sesión es Level Sweep 15m SHORT + TP de liquidez real, $9.79/mes estable — modesto pero genuino. Esperando decisión del usuario sobre activarlo o dejarlo solo documentado.
- **Pieza de FVG que faltaba probar, encontrada tras crítica válida del usuario**: solo se había copiado el TP de liquidez de FVG, nunca el filtro de perfil de volumen (POC/HVN, `python-service/fvg/volume_profile.py`) — FVG solo entra si la zona está cerca de un nodo real de alto volumen negociado, no solo un máximo/mínimo geométrico. `level_sweep_poc_filter.py` (nuevo) agrega ese mismo filtro (perfil de volumen de 200 velas, HVN≥70% del POC, distancia máxima 0.5%) a la config ya estable de Level Sweep. **Resultado: empeora, no ayuda** — n=1050, $5.74/mes, y rompe la estabilidad que ya tenía (mitad 2 muy negativa, -$291.55) — la pieza de perfil de volumen de FVG no se transfiere a Level Sweep, probablemente porque el mecanismo real de FVG (gap de 3 velas = desequilibrio de order flow genuino) es estructuralmente distinto de "barrer un nivel de precio". Sigue siendo la mejor config encontrada la original sin filtro de volumen: Level Sweep 15m SHORT + TP liquidez, $9.79/mes, estable. Sin más piezas nuevas de FVG para probar sin repetir lo ya intentado.

## 2026-08-18
- **Bug de fondo encontrado en TODO el laboratorio (`strategy_lab.py`)**: `backtest_strategy()` corría cada símbolo con capital infinito (sin límite de posiciones simultáneas), inflando por 10x+ todos los `$/mes` reportados — real producción solo permite 3 posiciones de $150 a la vez por estrategia (`config.MAX_OPEN_POSITIONS`). Sumado a que `validate_pre_trade()` (vetos reales) tampoco se llamaba nunca. Arreglado el motor central: cada backtest ahora aplica el límite real de 3 cupos (algoritmo greedy igual a `_capital_sim`) + los vetos reales de producción antes de reportar cualquier número. Los 4 activos re-testeados con esto: Level Sweep 1H -$20/mes, Level Sweep 60m SL2/RR1.5 +$8.65/mes, Level Sweep 15m SL3/RR3 +$57.53/mes, Band Touch 15m +$2.64/mes — muy lejos de los $300-900/mes prometidos originalmente sin el límite.
- **MA Slope Caso 3 recalibrado con éxito** (82-88% de fidelidad vs 74 trades/+$64.78 reales) tras encontrar y arreglar 3 bugs reales auditando un trade real (NVDAUSDT): día calculado en UTC en vez de hora Argentina (server usa `datetime.now()` local, UTC-3), `MaxTradeDurationCandles` no seteado en el test (default 16 en vez de 192 real), y el cierre por timeout solo se forzaba si el trade estaba perdiendo — un trade real cerró por timeout en ganancia (`ExitReason='timeout'`), confirmando que debe cerrar siempre, gane o pierda.
- **FVG-15m sigue sin calibrar** (-$111 backtest vs +$150 real en el mismo mes) — se descartaron 3 hipótesis (features de Nexus15 vacías, redundancia de símbolos, duración de trades) sin encontrar la causa. Pendiente para otra sesión.
- **Infraestructura de auto-arranque**: 3 tareas programadas de Windows (`Verge-Agent-AutoStart`, `Verge-LabControl-AutoStart`) para que el agente en vivo y el laboratorio sobrevivan un reinicio de PC sin intervención manual. Nuevo `lab_control_server.py` (puerto 8011, corre en el host — un contenedor Docker no puede arrancar procesos del host) expone `/control/start|stop|status`; botón "Arrancar/Detener laboratorio" agregado al frontend `/strategy-lab` que habla directo con este servicio.

## 2026-08-17
- **Backtest fiel de FVG-15m (`run_fvg_global`) se colgó 6 horas sin terminar — root cause real encontrado y arreglado en `engine.py`**: `HistoricalFetcher.set_active_symbol` cacheaba solo 1 símbolo a la vez (`_active_by_interval`), pero el walk global recorre cientos de símbolos por tick — cada switch de símbolo invalidaba el cache entero y reconstruía desde cero la lista de timestamps (miles de elementos) para CADA símbolo en CADA uno de los ~70.000 ticks. Benchmark confirmó 9ms/llamada alternando símbolos vs 0.002ms quedándose en uno solo (4500x). Fix: cache permanente por `(symbol, interval)` (`_all_by_interval`), nunca se invalida — no cambia ningún resultado, solo evita recalcular lo mismo. Reduce el warmup a ~1 vez por símbolo (~0.65s c/u, fijo) en vez de repetirlo por tick.
- **Segunda optimización real, sin perder fidelidad de salida**: separado el chequeo de TP/SL de posiciones abiertas (barato, sigue cada 5 min exacto — ahí vive la fidelidad real de cuándo cierra un trade) del escaneo de candidatos NUEVOS (caro, llama al analyzer real con pandas) — la zona FVG de un símbolo no puede cambiar dentro de la misma vela de 15m, así que escanearla cada 5 min era 3x trabajo redundante. Nuevo parámetro `scan_step_ticks` (default 1 = idéntico a antes) permite escanear candidatos nuevos cada 15 min reales sin tocar cuándo cierran los trades abiertos.
- **Resiliencia agregada a `run_fvg_global`**: todo el loop principal envuelto en try/except (loguea y sigue, mismo patrón que el fix de `strategy_lab.py` del día anterior) + `checkpoint_path` opcional que vuelca trades acumulados a disco cada N ticks — para que una corrida larga que se cae no vuelva a perder todo el cómputo como pasó con las 6 horas iniciales.
- **Confirmado con benchmark real (no bug)**: una prueba intermedia dio "0 trades" en varios rangos de fecha porque usaba fechas hardcodeadas (~enero 2025) fuera de la cobertura real de la DB (dic 2025 - jul 2026) — error propio de testing, no del motor. Repetido con fechas reales, en verificación.
- **Confirmado (no implementado, limitación real de arquitectura)**: "MA Cross Momentum" no se puede backtestear fielmente ni siquiera con vetos reducidos — depende de features de Nexus-15 en vivo (`ma50_history`, `volume_surge_bullish`, "Ley de Nico"/velas de cemento) que no tienen wrapper offline en `engine.py` (a diferencia de FVG/AdnCompression/MaGeometry, que sí lo tienen). Reimplementarlo requeriría clonar el motor de Nexus-15 completo, fuera de alcance de una sesión.
- Agregado patrón **"MA Pullback"** al generador de `strategy_lab.py` (ver entrada 2026-08-16) — corriendo en background, acumulando resultados.

## 2026-08-16
- **Encontrado el bug real detrás de "no veo progreso del laboratorio"**: `Dockerfile.backtest` hace `COPY agent/ /app/` en build time, así que `agent/backtest/*.jsonl`/`lab_progress.log` quedaban horneados en la imagen — la API en :8010 siempre servía una foto congelada del último `docker compose build`, sin importar cuánto corriera `strategy_lab.py` en el host. El usuario tenía razón en desconfiar.
- **Arreglado**: bind mount de `./agent/backtest` en `docker-compose.yml` (son archivos de texto chicos, no la DB de 3.5GB que motivó el volumen nombrado); `strategy_lab.py` ahora escribe `lab_current.json` en cada iteración (heartbeat + estrategia actual + período cubierto); nuevo endpoint `/lab/current` en `api.py`; `isRunning` de `/lab/status` dejó de depender de psutil (nunca veía el proceso del host desde dentro del container) y ahora usa la edad del heartbeat.
- **UI `/strategy-lab`**: barra de progreso en vivo con la variante que se está probando ahora mismo, y botón "Ver Top 10" con modal mostrando $/mes, PnL total, período y WR de las 10 mejores. Verificado extremo a extremo por curl (host→bind mount→API); `tsc --noEmit` sin errores; no se pudo verificar visualmente en browser por falta de credenciales de login.
- **Segundo bug real encontrado por el usuario al mirar el Top 10 recién arreglado**: el generador randomizaba `ma_pair`/`rsi_thresh` SIEMPRE, aunque `detect_signal()` solo los usa para `ma_cross`/`rsi_extreme` — level_sweep se probó 25 veces con distinto ma_pair/rsi_thresh "de mentira" (mismo backtest exacto), desperdiciando ~93% del cómputo en redundancia y llenando el Top 10 de filas idénticas. Arreglado en dos capas: `random_strategy()` ahora solo randomiza el parámetro relevante por tipo de entrada (más eficiente hacia adelante), y `/lab/strategies/top`+`/found` dedupean por firma canónica de parámetros relevantes (limpia también el histórico ya acumulado con el generador viejo).
- **Root-cause encontrado del "se cuelga solo" que el usuario venía reportando hace días**: `write_current()` (agregada hoy mismo) usaba `os.replace()` para el heartbeat atómico, pero en Windows esa syscall tira `PermissionError` si otro proceso (el container Docker, por el bind mount nuevo) tiene el archivo abierto en simultáneo — y como no estaba en un try/except, tumbaba TODO el loop principal sin dejar rastro visible salvo en `lab_stdout.log`. Arreglado: la escritura del heartbeat es best-effort (falla silenciosa, no mata el proceso) y además se envolvió el loop principal completo en try/except (loguea y sigue con la próxima estrategia) para que ningún error futuro de cualquier tipo vuelva a matar el proceso sin supervisión.
- **Canasta del laboratorio ampliada de 150→422 símbolos** (todos los que tienen ≥3000 velas de 15m en la DB) por pedido del usuario — el límite de 150 no tenía justificación rigurosa, quedó de la primera corrida overnight. Re-correr las 6 estrategias reales del "SL cap" (sin tope vs tope -$5, margen $150) contra el universo completo reveló que 2 de las 4 que estaban capadas en producción (Level Sweep 1H, Level Sweep 60m SL1.5/RR3) NO se sostenían igual que con la muestra de 150 símbolos.
- **2 cambios reales aplicados en producción** (StrategyProfiles en Postgres) según ese resultado: se sacó `capSlLoss` a "Level Sweep 1H" (con tope era inestable entre mitades — 16% de retención vs 61% sin tope). "Level Sweep 60m SL1.5/RR3" y "Death Cross" quedan sin tocar por pedido explícito del usuario (no tienen ninguna configuración estable, pero no le molesta dejarlas así por ahora).
- **Nuevo patrón "MA Pullback" agregado al generador del lab** (5to `entry_type`, junto a ma_cross/level_sweep/band_touch/rsi_extreme) — pedido del usuario tras notar que el lab solo encontraba SHORT (confirmado real: el período histórico cargado fue mayormente bajista para las alts, no un bug). Replica la lógica descrita en el perfil real "MA Cross Momentum"/"MA Clone" (que generaron $220+ en producción antes de romperse): MA7 con pendiente mínima define tendencia, entra cuando el precio re-ingresa a la zona MA25-MA99 desde afuera (pullback de continuación, no reversión), con confirmación de volumen opcional. 100% calculable desde velas crudas (a diferencia del MA Cross Momentum real, que depende de Nexus-15 — sin wrapper offline en `engine.py`, confirmado al intentar backtestearlo fiel). Objetivo: tener cobertura de estrategias tipo tendencia/LONG lista para cuando el mercado gire alcista, no solo las 4 SHORT actuales.
- **UI: sistema de favoritos (⭐) compartido entre Dashboard e Historial** — nuevo `FavoriteStrategiesService` (localStorage, sin campo en backend) con estrellita clickeable en los tabs del dashboard y los chips de filtro del historial; favoritos ordenan primero en ambos lados. En Historial, los trades abiertos (status 0) ahora se fijan siempre arriba de la lista (con borde verde distintivo) y vuelven a su posición cronológica normal al cerrar. Se sacó "Standard Scalping" de los tabs del dashboard y de los chips/lista del historial por pedido explícito (ya no aporta nada activo). `tsc --noEmit` sin errores.

## 2026-08-15 (cont.)
- **Encontrado y arreglado: el laboratorio de estrategias (`strategy_lab.py`) estaba muerto desde el 12/8** sin que nadie lo notara (sin traceback visible en su log, corte silencioso) — ya había encontrado 434 candidatas en 22.365 pruebas antes de caerse. Reiniciado. Diagnóstico honesto pedido por el usuario ("¿qué falta probar?"): la búsqueda aleatoria de variantes ya llegó a retornos decrecientes (mayoría de hallazgos son reformulaciones del mismo puñado de ideas); lo que realmente falta es dejar acumular datos reales de las 6 estrategias en vivo, aplicarles el circuit breaker de curva de equity (ya existe, nunca se los pusimos), y probar confluencia entre señales de distintas estrategias — ninguna de las 3 implementada todavía, pendiente de decisión del usuario.

## 2026-08-15
- **Batería de pruebas offline (sin tocar producción) sobre las 6 estrategias del laboratorio, pedidas por el usuario**: (1) tope de SL -$10 a margen $150 y -$15 a margen $300 (confirmó que -$5/$150 y -$10/$300 siguen siendo el mejor par, no se cambió nada); (2) TP recortado 11% manteniendo el SL igual — empeora en 4 de 6 estrategias (peor caso: -14.8% en Level Sweep 60m SL1.5/RR3), no se aplica. Ninguna de las dos ideas se implementó, quedaron solo como pruebas descartadas con evidencia real.
- Sin cambios de código todavía hoy — quedó pendiente la decisión del usuario sobre aplicar en vivo el margen $300 + tope de SL -$10 (probado el 08-14, mejora 5/6 estrategias del laboratorio). Agente y laboratorio siguen corriendo sin tocar.

## 2026-08-14
- **Diagnosticado y arreglado: pérdidas grandes en "Level Sweep 60m SL1.5/RR3"** (ej. ACEUSDT -$18.79, CYSUSDT abierto -$14.33) — confirmado con DB que el SL se ejecutó exacto en su precio programado (`ExitReason: sl_hit`, no era un bug de ejecución). Causa real: margen fijo de $150 combinado con SL a 1.5xATR hace que el riesgo en dólares varíe mucho según la volatilidad de cada símbolo.
- **Implementado el fix**: tope de pérdida en dólares sobre el SL (TP siempre calculado sobre la distancia ORIGINAL del SL, nunca se toca) vía `_capped_sl_tp_dist` en `verge_agent.py`, opt-in por perfil (`capSlLoss` en `PatternParamsJson`). Validado re-simulando vela por vela (no editando resultados post-hoc) en las 6 estrategias del laboratorio: mejora real en 4 (Level Sweep 1H +6%, 60m SL1.5/RR3 +19%, 15m SL3/RR3 +19%, Band Touch +9.6%), empeora en 2 (60m SL2/RR1.5, Death Cross) — activado el tope SOLO en las 4 que mejoran.
- **Probado con margen $300 (posición duplicada) y tope -$5 vs -$10**: confirmada la intuición del usuario — a $300 el tope de -$5 queda desproporcionadamente ajustado (empeora en 4/6), pero -$10 (el doble, compensando el margen duplicado) gana en 5/6, mismo patrón. Pendiente decisión de aplicarlo en vivo.

## 2026-08-13
- **Confirmado con plata real que los 3 fixes del día anterior funcionaron**: "Level Sweep 15m SL3/RR3", "Level Sweep 60m SL2/RR1.5" y "PDH Sweep" ya abren trades tras arreglar el bug de `TpMultiplier`/`SlMultiplier` mal cargados y reactivar PDH Sweep. "Level Sweep 60m SL1.5/RR3" (sin tocar desde que se creó) lleva +$79.02 en 4 trades cerrados (2 ganadores grandes ~$35-44, 2 perdedores chicos ~$0.4) — la asimetría R:R=3:1 funcionando como se diseñó, aunque la muestra todavía es chica para sacar conclusiones de win rate real.

## 2026-08-12
- **"Laboratorio de estrategias" (agent/backtest/strategy_lab.py)**: generador/backtester autónomo que corre en loop infinito (background, separado del agente en vivo), prueba combinaciones random de entrada (cruce de MAs, barrido de nivel, toque de banda de Bollinger, RSI extremo) sobre datos históricos reales con sizing/comisiones correctos (aprendido de un bug propio: la primera versión medía en "R" mal normalizado, invirtiendo el signo real en dólares) y exige estabilidad entre las dos mitades cronológicas (no solo positivo, para no repetir la decadencia tipo MA Slope Caso 3). De ahí salieron 5 estrategias reales, conectadas al agente (patrón de inyección directa, 3 lugares obligatorios c/u) y activadas: Level Sweep 1H, Level Sweep 60m SL1.5/RR3, Level Sweep 60m SL2/RR1.5, Level Sweep 15m SL3/RR3, Band Touch 15m, más Death Cross (SMA50/200 4h) y PDH Sweep (barrido de máximo del día anterior) de una ronda anterior.
- **UI nueva `/strategy-lab`** (Angular, ícono planeta en sidebar+bottom-nav) mostrando resultados del laboratorio en vivo, con endpoints nuevos en `agent/backtest/api.py` (`/lab/status`, `/lab/strategies/found`, `/lab/strategies/top`). De paso: arreglado `.dockerignore` (`agent/*.txt` bloqueaba el build del contenedor `verge-backtest` al excluir `requirements-backtest.txt`) y registrado en `app.config.ts` los íconos ionicons que faltaban (`planet-outline`, `flask-outline`, etc. — el registro es manual/obligatorio en este proyecto, si falta el ícono sale vacío).
- **3 bugs reales de silenciamiento encontrados y arreglados en las estrategias nuevas**: (1) `MinConfluenceScore` puesto en 50 en vez de 80 al crear los perfiles vetaba el 100% de los candidatos por `low_confluence`/`low_confluence_for_tier3` (mismo tipo de bug ya documentado con FVG el 2026-07-13, ahora vía un campo distinto); (2) 3 variantes de "Level Sweep" con el mismo timeframe/lookback (60m/10 velas) compiten por los mismos símbolos vía la regla global "un trade por símbolo por día" — no son independientes entre sí aunque tengan SL/TP distinto; (3) `TpMultiplier`/`SlMultiplier` cargados mal al crear 2 perfiles (ratio real de 1.0 y 0.75 en vez de 3 y 1.5) los vetaba por `low_rr` aunque el SL/TP custom por ATR estuviera bien calculado — esos dos campos generan un chequeo de RR aparte del cálculo real. PDH Sweep además había quedado inactiva sin querer tras el fix de confluence.

## 2026-08-11
- **"FVG - 15m v3" activada por el usuario con capital real** (no por mí) — quedó corriendo con 3 mecanismos opt-in nuevos apilados: sizing por meta-labeling (`mlEvSizingMode`), circuit breaker de curva de equity (`equityCurveBreaker`, pausa/reduce tamaño según drawdown del propio perfil desde su pico — protege contra devolver ganancias, ej. el caso real de FVG-15m que subió a ~$175 y volvió a ~$130) y veto por funding extremo (`maxAbsFundingPct`, nuevo hallazgo: con funding rate extremo el WR de FVG-15m cae a 6-9%, matemáticamente perdedor con su R:R; validado con split temporal, se sostiene en ambas mitades). Infra de funding/liquidaciones (`funding_rates.py`, `liquidation_tracker.py`, `kline_cache.py`) ya existía del epic `market-data-expansion` pero nunca se había aplicado a FVG.
- **Apagadas 2 estrategias reales confirmadas perdedoras**: "MA Slope Caso 2" (-$114.93/111 trades, geometría de MAs contradictoria) y "MA Slope Caso 3 Pulido" (-$23.10/45 trades — resultó ser Caso 3, la ganadora, con el TP mínimo bajado de 10% a 3%, lo que la rompió).
- **Hallazgo: "MA Slope Caso 3" (53.7% WR, +$55.83/67 trades) tiene un perfil de riesgo mucho más sano que FVG-15m** — peor caída desde el pico $24.17 (vs $79.71 de FVG), peor trade individual -$4.45 (vs hasta -$16 de FVG), sin depender de outliers grandes. Candidata a llevarle el mismo circuit breaker de curva de equity que v3, pendiente de decisión del usuario.

## 2026-08-10
- **v2 "filtros minados" desactivada y descartada**: perdió -$21.90 real en 5 días. Auditoría del motor de backtest reveló un bug de fondo — `backtest/engine.py` evaluaba cada símbolo aislado en su propia línea de tiempo, sin la competencia cross-symbol real (top-5 por `tp_distance_pct` que hace `/fvg/scan` en producción cada ciclo). Se agregó `run_fvg_global` (loop temporal global con el mismo corte top-5) pero igual no logra reproducir fielmente producción porque `config.WATCHLIST` es dinámico (armado por volatilidad en tiempo real) y no se puede reconstruir históricamente símbolo por símbolo — limitación real de la herramienta, no un bug puntual.
- **Hallazgo importante (auditoría de 316 trades reales de "FVG - 15m")**: filtrar señales "dudosas" (gap>2.5%, TP muy lejos, patrón en U repetido) SIEMPRE cortó también a los ganadores grandes que sostienen toda la estrategia (9 de 96 dudosas ganaron +$115.74 solas) — 4 intentos de filtro binario (Gap Chico, v2, Pulido, Pulido V2) fallaron todos en vivo por la misma razón. Confirmado con un modelo de clasificación (meta-labeling, 1657 trades combinados de todas las variantes): optimizar por win-rate apunta en la dirección CONTRARIA al dinero en esta estrategia (el 20% de señales que el modelo cree "mejores" perdió -$49.67; el 20% que cree "peores" ganó +$84.19) — la ganancia vive en pocos outliers grandes, no en acertar seguido.
- **"FVG - 15m v3 (anti-racha)" creada (inactiva)**: en vez de filtrar, usa un modelo de regresión (GradientBoosting, `agent/models/fvg_ev_model.joblib`) que predice el PnL esperado en dólares (no gana/pierde) y ajusta el tamaño de la apuesta entre 20%-100% del margen normal según ese EV — nunca excluye una señal. Validado out-of-sample con walk-forward real: top-30% de EV predicho ganó +$70.97, bottom-30% perdió -$56.64 (dirección correcta). Cableado como opt-in (`mlEvSizingMode` en `PatternParamsJson`) en `risk_manager.py`/`verge_agent.py` sin tocar ninguna otra estrategia. Falta reiniciar el agente y activar para probarlo con capital real.

## 2026-08-09
- Continuación de la sesión del 08/8 (quedó fechada como 08-08 por confundir la fecha real del sistema en ese momento — es la misma sesión, ver esa entrada abajo para el detalle completo de "FVG - 15m v2 (filtros minados)").
- Auditados los 63 trades reales de v2 contra el original en el mismo período exacto (6-8/8): ambos perfiles perdieron parejo (v2: -$21.90/WR 7.9%, original: -$11.31/WR 7.1%), confirmando que fue una mala racha de mercado para FVG-15m en general, no un problema del filtro nuevo. Sin decisión tomada aún sobre si dejarla correr más días o pausarla.

## 2026-08-08
- **"FVG - 15m Gap Chico" (filtro maxGapPct=1.0) desactivada**: confirmado con datos reales que el filtro descartaba justo a los mejores ganadores (COTIUSDT gap=2.29%, BTWUSDT gap=1.25%) — no era un bug, el filtro estaba mal calibrado desde el diseño.
- **Minería real sobre los 268 trades de "FVG - 15m" original** (único perfil FVG que gana de verdad, +$121.29; todas las variantes previas dan negativo): encontrados 3 filtros (`maxGapPct>2.5`, `maxTpDistancePct>25`, `maxUShapeCount>=9` a excluir) validados con split temporal (mejora en ambas mitades cronológicas, no solo el agregado). Implementados como opt-in en `verge_agent.py::_build_fvg_candidate` y creado el clon "FVG - 15m v2 (filtros minados)" (activado por el usuario).
- **Primeros ~3 días en vivo de v2: -$21.90/63 trades, WR 7.9%** — mala racha, pero confirmado que el FVG-15m ORIGINAL tuvo el mismo período igual de malo (-$11.31/28 trades, WR 7.1%), consistente con el diseño asimétrico ya documentado el 22/7 (pocos ganadores grandes compensan muchas pérdidas chicas). Sin bug de implementación (0 violaciones de filtro verificadas). Pendiente: más días de muestra antes de decidir si el filtro realmente ayuda.

## 2026-08-05
- **WhaleTracker (BTC on-chain, `agent/whale_tracker.py`) diagnosticado, no arreglado**: el loop de reconexión a `blockchain.info` reintenta cada 5s sin importar el motivo del corte, ignorando el header `retry-after` (visto: pedía ~16.5hs de espera tras un 429) — no afecta trading real (solo pierde esa señal secundaria), queda pendiente para otra sesión.
- **Backtest FVG-15m/8 meses/426 símbolos se colgó dos veces** (la primera por `BrokenProcessPool`, la segunda por chocar con el mismo bug de rotación de log de Windows que ya afecta al agente en vivo, al compartir `agent.log`) — corregido apuntando a un log propio, corrida de 6 meses con el filtro `maxGapPct=1.0` (clon "FVG - 15m Gap Chico") sí terminó: WR 17.3%, **-$542.42** (vs +$153.01 de Pump Reaper en 8 meses).
- **Hallazgo importante confirmado con datos reales de producción**: el filtro `maxGapPct=1.0` (pensado para cortar pérdidas) también habría descartado 2 de los mejores trades ganadores recientes de FVG-15m real (COTIUSDT +$24.39 con gap=2.29%, BTWUSDT +$34.15 con gap=1.25%) — no es un bug de replicación, el filtro corta ganadores grandes junto con las pérdidas. Pendiente decidir si vale la pena activarlo con ese trade-off a la vista.
- Continuación de la sesión del 27/7 (que se extendió hasta acá por la fecha real del sistema): fix de `MinRR=4`+tope estructural que dejaba sin abrir a FVG/MA Slope/Arrow Reversal/ADN Compression, dockerización del motor de backtest (`verge-backtest`, volumen nombrado por I/O lento de bind mount en Windows), y fix de `CloseTradeAsync` para instrumentos TradFi (AAPLUSDT/HK1810USDT/etc.) que no resuelven precio y dejaban el trade imposible de cerrar. Detalle completo en la entrada `## 2026-07-27` de arriba (quedó ahí por error de fecha).
- Aclarado con el usuario: "Total Trades" del Historial (1719) vs `COUNT(*)` crudo de `SimulatedTrades` (3289) no es un bug — `GetPerformanceStatsAsync` filtra a propósito trades abiertos, de estrategias pausadas, y con `ExclusionTag` seteado (confirmado replicando el filtro exacto en SQL: 1721, coincide).

## 2026-07-27
- Continuación pasada la medianoche de la sesión del 26/7 (módulo de backtesting): selector "Todos/Top 40" símbolos, fix de progreso granular (`multiprocessing.Manager` Queue en vez de progreso por lote), feedback visual del selector, `symbols_used`/`symbols_count` en el resultado. Logs sueltos del servidor de backtest (`agent/backtest_api_log*.txt`) sacados de git y agregados a `.gitignore` (se reescriben todo el tiempo mientras el server corre, no aportan valor versionado).
- Reportado por el usuario y sin resolver todavía: el backend .NET (`Verge.HttpApi.Host.exe`) crasheó solo (exit code -529697949, excepción no manejada) — falta el stack trace real para diagnosticar (el log en disco quedó congelado desde marzo).
- **Bug real encontrado y arreglado: `MinRR=4` (subido en una sesión previa) + el veto `MIN-RR-VETO` nuevo en `risk_manager.py` dejaron sin abrir trades durante ~2 días a FVG/MA Slope/Arrow Reversal/ADN Compression** (R:R real quedaba en ~3.75:1, justo debajo del piso). Bajado `MinRR` a 3.0 en los 14 perfiles afectados (DB). Además `ma_slope_mode` no estaba excluido del tope estructural de TP (`_apply_structural_tp_cap`, sí lo estaban `fvg_mode`/`arrow_peak_mode`) — el cap le pisaba el TP ya calibrado a MA Slope, tirando su R:R real a 0.5-2:1 y auto-vetándose sistemáticamente; corregido excluyéndolo también. Confirmado con datos de log que ambos fixes funcionan (MA Slope Caso 3/FVG volvieron a abrir).
- **`agent/backtest/api.py` (motor de backtest) migrado a Docker** como servicio `backtest` en `docker-compose.yml` — antes corría suelto a mano (`python -m backtest.api`), no sobrevivía a cerrar la terminal. Requirió copiar `python-service/fvg` y `python-service/adn_compression` a la imagen (el motor los reusa via import directo) y usar un **volumen nombrado** (`verge_backtest_data`) en vez de bind mount para la DB histórica de 3.5GB+ — un bind mount desde NTFS via la capa de virtualización de Docker Desktop en Windows resultaba en queries de SQLite que nunca volvían (confirmado, >60s colgado); con volumen nombrado responde en segundos. Agregado `.dockerignore` en la raíz (no existía — los builds mandaban 4GB+ de contexto).
- **Bug real en `SimulatedTradeAppService.CloseTradeAsync`**: instrumentos "TradFi Perps" de MA Slope Caso 3 (AAPLUSDT, GOOGLUSDT, HK1810USDT, etc.) nunca resuelven precio vía Binance/Bybit/OKX (no son cripto) — tiraba `UserFriendlyException` sin controlar, dejando esos trades imposibles de cerrar para siempre. Arreglado usando `MarkPrice`/`EntryPrice` como respaldo (mismo patrón que el guard de desviación ya existente) en vez de tirar. Pendiente: recompilar y reiniciar el backend (no tiene hot-reload) para que tome el cambio.

## 2026-07-26
- **Datos históricos limpios completados**: `agent/data/binance_vision_clean.db` ahora cubre dic 2025 - jul 2026 (428 símbolos, incluye julio completo día por día que antes faltaba en 403/428 símbolos — bug de cobertura silencioso que había invalidado comparaciones previas). Scripts de descarga paralela (`agent/download_binance_vision_daily_all_v2.py`, 64 workers HTTP, no toca la API en vivo) reemplazan al secuencial original (~10x más rápido).
- **Motor de backtest genérico arrancado** (`agent/backtest/engine.py`) — reusa `verge_agent.py`/`risk_manager.py` REALES (instanciados con `__new__`, fetcher histórico inyectado) en vez de reimplementar la lógica a mano por estrategia, que había causado bugs reales (backtests previos de MA Slope Caso 3 y FVG se salteaban el tope estructural de TP de `risk_manager.py`). Primer alcance: StrategyType=MaGeometry. Optimizado con bisect (evita O(n²)) y evaluación cada 15min con vela parcial (igual que producción, que lee la vela en formación, no solo cerradas) — validado 1:1 contra un trade real (NVDAUSDT) con match casi exacto en MA7/25/50/99.
- **Pendiente sin cerrar (RESUELTO más tarde en la misma sesión, ver abajo)**: frecuencia de señales del backtest de MA Slope Caso 3 sigue por debajo de la real (auditado: 51 trades reales en 11-25/jul vs bastante menos en el backtest, incluso tras el fix de vela parcial) — causa exacta aún sin confirmar al 100%.
- **Causa real encontrada y arreglada (validado 83%/78% de fidelidad en dos ventanas de 1 semana)**: el `_apply_structural_tp_cap` de `risk_manager.py` rechazaba trades que en la realidad SI abrieron, porque mi backtest siempre tenía el historial completo (400 velas de 15m) disponible mientras que la caché en vivo de producción rara vez llega a tenerlo listo en el momento exacto de la consulta — se replicó ese fail-open (la propia función real ya lo contempla: `if not klines or len(klines) < 60: return tp_price`). Con eso + evaluación cada 5min (no 15) + zombie_timeout portado, la fidelidad de detección subió de 3/24 a 20/24 (semana 1) y 25/32→30/32 contando ausencias por falta de datos (semana 2, 17-24/jul).
- **Módulo de backtest completado (OpenSpec `complete-backtest-module`, archivado)**: conectados StrategyType=AdnCompression (validado 1:1 contra EDUUSDT real: SL 0.0311048 vs 0.0311056) además de MaGeometry/FVG (11+2=13 de 19 perfiles ahora testeables); sincronización de datos on-demand (`agent/backtest/data_sync.py`, botón "Actualizar datos" en la UI, detecta y descarga solo huecos reales); persistencia de corridas en tabla SQLite local `backtest_runs` (NO en Postgres/.NET, a propósito, para no depender de reiniciar el backend de VS del usuario cada vez); paralelización por símbolo (`ProcessPoolExecutor`, ~3.1x más rápido en la prueba de 1 mes: 899s→287s) — encontrado y arreglado en el camino un bug real de no-determinismo (desempate entre señales con el mismo `open_time` dependía de qué proceso terminaba primero, cambiando qué trade se quedaba con el cupo de capital limitado; arreglado ordenando siempre por `(open_time, symbol)`). Reenganche de progreso en la UI vía `localStorage` + endpoint `/backtest/jobs/active` (antes, editar el componente Angular con un backtest corriendo perdía la barra de progreso al recargar la página, aunque el job seguía vivo del lado del servidor). `StrategyType=Generic` (motor de scoring Nexus-15/LSE completo) queda explícitamente fuera de alcance para otro change.
- **Confirmado con evidencia (no reinicio, no bug de este trabajo)**: el agente en vivo (proceso corriendo desde las 00:05 sin reiniciarse en todo el día, verificado contra el log) dejó de abrir trades de MaGeometry/FVG/AdnCompression desde el 25/7 — las señales SÍ se siguen generando (confirmado en el log, decenas por ciclo), el ciclo del agente está tardando 800-2400s en vez de los 300s esperados ("performance bottleneck detected", reportado por el propio agente) — pendiente investigar esa lentitud aparte.
- **Selector de universo de símbolos + 3 fixes de UI del backtesting**: agregado `TOP_40_SYMBOLS` (mismos pares grandes/líquidos del backtest de FVG large-cap) + endpoint `/backtest/symbols/top40`, con botones "Todos (400+)" / "Top 40" en la UI. Arreglados en el camino: progreso de `run_parallel` quedaba en 0% hasta que el primer lote entero terminaba (ahora granular por símbolo vía `multiprocessing.Manager` Queue), el selector no mostraba visualmente cuál estaba elegido, y el resultado no decía qué símbolos se usaron (agregado `symbols_used`/`symbols_count`).
- **Backend .NET (`Verge.HttpApi.Host.exe`) crasheó solo esta noche** (exit code -529697949, excepción no manejada) — reportado por el usuario, pendiente diagnosticar (falta el stack trace real; el log en disco está congelado desde marzo, no sirve).

## 2026-07-22
- **Auditoría cuantitativa de FVG-15m (163 trades) cruzando `AgentDecisionJson` contra `RealizedPnl` real vía SQL/Python**: encontrada la fórmula real de por qué gana — los SHORT abiertos contra una tendencia previa fuertemente alcista (`slope_ema50_deg` > 3° al entrar, agotamiento clásico) tienen 29.2% win rate y +$46.40, contra 13.6%/-$7.52 cuando la pendiente es plana. Ese 14.7% de los trades genera el 65% de toda la ganancia de la estrategia. Los LONG (72% del volumen) no muestran ese mismo corte limpio — diluyen el resultado sin aportar edge claro. Gap size y tier del símbolo se probaron y NO correlacionan limpio (descartados como explicación).

## 2026-07-21
- **Primer día real de FVG-15m contra Binance Testnet** (con `BroadcastToBinance` recién habilitado) — encontrado y arreglado bug real: símbolos que existen en `exchangeInfo` de mainnet pero NO en testnet (BLUAIUSDT, CLOUSDT) abrían simulación interna pero el mirror a Binance fallaba en silencio ("Invalid symbol", código -1121). Root cause real: el campo que distingue "operable" no es "está listado" — es `status` (`PENDING_TRADING` en testnet vs `TRADING` en mainnet, 129 símbolos de testnet están en ese estado ahora mismo). Fix proactivo en `agent/position_manager.py`: chequea `status=="TRADING"` del `exchangeInfo` del entorno actual ANTES de abrir la simulación interna (no solo antes del mirror) — antes era reactivo (lista negra aprendida recién tras el primer rechazo real).
- **Alerta de Telegram agregada** cuando el mirror a Binance falla (apertura o cierre) — reusa el bot de `tools/ip_alert/`, mismo bot/chat_id.
- **Optimización de latencia del mirror a Binance**: leverage 1x cacheado por símbolo (antes se reseteaba en cada entrada, ~0.7s evitables), precisión de TODOS los símbolos cacheada de una sola llamada a `exchangeInfo` (antes una por símbolo nuevo, ~1s la primera vez), precalentado en un hilo aparte al arrancar el agente en vez de en el primer trade real.
- **Auditoría honesta de FVG-15m** (usuario preocupado por 163 trades/14% win rate/pico $106→$71): confirmado que `RealizedPnl` YA descuenta fees (`TradingSimulationService.cs`, no es un número inflado). La caída fue un solo día (21/jul: 22 trades, 0 ganadores, -$31.69) — estadísticamente esperable con el diseño asimétrico de la estrategia (ganador prom. $12.25 vs perdedor prom. -$1.50, ratio 8:1), no un bug nuevo. Descartada la hipótesis de agente duplicado (solo 1 proceso corriendo).

## 2026-07-20
- **Fix de regresión real que yo mismo introduje**: `loadStats()` (nuevo endpoint de stats totales de Historial) quedó enganchado a `recomputeAll()`, que el handler `tradeUpdate$` de SignalR dispara UNA VEZ POR SEGUNDO POR CADA POSICIÓN ABIERTA (mark price tickea cada 1s en `SimulationMarkPriceWorker.cs`) — causó una ráfaga de ~20 pedidos/seg al mismo endpoint, cancelándose entre sí (`OperationCanceledException` en cascada, Historial mostrando $0 en todas las estrategias). Arreglado: `loadStats()` ahora solo se llama desde `loadData()` y `setStrategyFilter()`, nunca desde el patch de `tradeUpdate$`.
- **Verificación honesta de FVG-15m** (estrategia que el usuario notó rentable recién al arreglar el bug de totales): sin tocar Binance ni asumir nada, se comprobó vía DB que todos los cierres de la semana pegan exacto en TP o SL preseteado (0% de desvío) — descarta que haya vuelto el bug viejo del "TP fantasma a precio imposible". 107 trades/semana, +$99.49, 15.9% win rate, racha máxima de 15 pérdidas seguidas, drawdown máximo -$28.59 — datos reales, no estimados.
- **Nueva pieza: alerta de cambio de IP pública** en `tools/ip_alert/` (Telegram + WhatsApp vía CallMeBot + alarma sonora local con ventana que no se apaga sola) — necesaria porque el usuario whitelisteó su IP casera en la API key de Binance Testnet y una IP dinámica que cambia deja al agente sin poder operar hasta actualizarla a mano. Probado end-to-end con un cambio de IP simulado, los 3 canales confirmados funcionando.
- **Reemplazado el hardcodeo de "qué estrategia opera contra Binance real"**: antes era un check fijo por id/nombre ("MA Cross Momentum") en dos puntos de `agent/verge_agent.py`. Ahora es un campo `BroadcastToBinance` en `StrategyProfile` (migración de EF ya aplicada a Postgres) con switch en el editor de `/strategies` — cualquier estrategia se puede espejar a Binance (Testnet o Mainnet, según cómo esté corriendo el agente) sin tocar código. Pendiente: el usuario prende el switch en FVG-15m y reinicia backend + agente para probar el primer trade real en Testnet.

## 2026-07-19 (cont.)
- **Bug real en producción encontrado y arreglado, causó una pérdida real (BULLAUSDT -$14.43)**: `python-service/nexus15/arrow_peak_analyzer.py` contaba días de sangrado usando la vela diaria de Binance que incluye el día EN FORMACIÓN — confirmó "1 día de sangrado" con 3 horas de datos parciales que, al cerrar el día completo, terminaron en VERDE (revirtió). Fix: descartar la última vela diaria si no pasaron 24h desde que abrió. Deploy: rebuild + restart de `market-data` (el contenedor sin volumen montado).
- **Fix de R:R en el clon `arrow_peak_v2`**: el TP graduado podía dejar una relación riesgo/beneficio pésima (caso real RAVEUSDT: 11% de riesgo por 2.75% de ganancia) porque el SL queda fijo y solo se achicaba el TP. Agregado `ARROW_PEAK_V2_MIN_RR` — si el graduado da peor R:R que el mínimo, cae al TP completo del original.
- Dos hipótesis más del usuario descartadas con datos reales (MA25 como gatillo, sacar la exigencia de "vela roja más grande que la anterior") — ambas empeoran el resultado (PF cae de 1.39 a 0.97 y 1.02 respectivamente).
- Decisión explícita del usuario: no agregar cierre por tiempo/mecanismos de salida dinámica para el problema de trades largos bloqueando capital — la regla de SL/TP fijo al abrir (decidida 2026-07-15) se mantiene sagrada; la respuesta es mejorar la calidad de entrada, no parchear la salida.

## 2026-07-19
- **Primer caso real en vivo de `arrow_peak` vs `arrow_peak_v2` corriendo en paralelo**: BULLAUSDT (fuera de la franja floja, TP idéntico en ambos, los dos con SL) y RAVEUSDT (dentro de la franja floja 25-50%, TP del clon real y notablemente más cerca — 2.75% vs 17.73% del original) — confirmado con logs reales que el mecanismo funciona tal cual se diseñó.
- **Dos hipótesis más del usuario, testeadas y descartadas con datos reales** (mismo método honesto, 681 símbolos): (1) usar MA25 en vez de MA99 como gatillo de entrada más temprana — PF cae de 1.39 a 0.97 (pérdida neta), descartado. (2) sacar la exigencia de "vela roja más grande que la verde anterior", dejando solo "toca la MA99 en rojo" — PF cae de 1.39 a 1.02 y el PnL total cae de +386% a +28%, descartado.
- **Sospecha del usuario de que el fix de anoche "rompió" Arrow Reversal original** (dos trades perdedores/en contra justo después del cambio) — verificado con `git diff` que `_build_arrow_peak_candidate` (el original) no tiene una sola línea tocada desde antes de la sesión; ambos triggers (BULLAUSDT 1d/+55.3%, RAVEUSDT 2d/+36.5%) fueron válidos por las reglas del propio patrón, no bugs. Con ~62% de precisión histórica, dos pérdidas seguidas es variancia normal, no causa-efecto.
- Encontrado y corregido en el camino un error propio real: usé el caché SQLite del agente (`kline_cache.py`, desactualizado ~1.5h por falta de refresco) para verificar en vivo si el precio tocaba la MA99, dando un número de distancia incorrecto al usuario. El sistema real (`shared_kline_cache.py`, TTL 15s) nunca usó ese dato viejo para decidir — el error fue solo en mi verificación manual, no en el trading real.

## 2026-07-18
- **Backtest real de Arrow Reversal (Arrow Peak)** — `agent/arrow_peak_backtest.py`, portando la lógica de `arrow_peak_analyzer.py` contra klines cacheados (1D reconstruido agregando 1h por día calendario, ya que casi no hay velas 1D cacheadas). 185 trades reales / 681 símbolos: 62.2% precisión, PF 1.39, robusto en los 3 regímenes de BTC (no depende de que esté bajista, contra la sospecha inicial del usuario).
- **Verificada con datos reales la hipótesis del usuario** sobre reversión post-pico: 0% de recuperación del precio del pico en 1 día (0/348 casos históricos), solo 25.6% la recupera dentro de 10 días — confirma el mecanismo de fondo detrás de Arrow Reversal.
- **Hallazgo real para calibrar el TP**: la relación entre magnitud del pump previo y calidad del trade NO es lineal — forma de "U" (franjas 20-25% y 50%+ rinden bien, la franja intermedia 25-50% rinde mal y encima tarda más). Candidato a clonar Arrow Reversal con TP graduado por esta franja, sin tocar el original (mismo patrón que MA Slope Caso 3 15m).
- Chequeada la hipótesis de confluencia multi-timeframe de medias móviles (`agent/arrow_ma_confluence_check.py`): señal real pero moderada (PF 1.71 vs 1.14 comprimido/no comprimido), correlación lineal débil — sugestivo, no confirmado con fuerza.
- Commit + push del epic #156 completo (secciones 1-4 en producción, sección 5 con resultado honesto de "no sirve todavía", TODO documentado en `openspec/changes/market-data-expansion/tasks.md`).
- **Clon `arrow_peak_v2` (TP graduado, sección 7)**: construido y activado en paralelo al original — mismo detector, TP al 50% de retroceso en la franja floja (25-50% de subida previa) en vez del 100%. Excepción puntual al anti-churn diario para que original y clon puedan abrir posiciones simultáneas en el mismo símbolo (comparación real, mismo evento). Bug real propio encontrado y arreglado: me olvidé de crear la fila `StrategyProfile` en Postgres para el clon (existe solo para el original, "Arrow Reversal") — sin eso, el clon operaba de verdad pero era invisible en Historial/Estrategias (sin nombre, sin color, sin filtro), aunque la lógica de trading nunca fue falsa. Clonada la fila vía SQL directo (mismo patrón que MA Slope Caso 3 15m), `AllowedSources='arrow_peak_v2'` — se recoge solo, sin reiniciar el agente (los perfiles se refrescan de la DB cada ciclo).
- Agregado botón de debug "🏹⚡ ARROW PEAK V2 COMPARE" en Nexus-15 (llama directo a `localhost:8002/arrow-peak-v2-preview`, nuevo endpoint en el HTTP del agente) para comparar TP original vs. graduado sin esperar un trigger real.

## 2026-07-17
- **Epic #156 (market-data-expansion) — secciones 1 y 2 completas y en producción**: order book imbalance (OFI, `agent/orderbook_ws.py`) y funding rate (`agent/funding_rates.py`) capturando en vivo, con veto opt-in de funding en `setup_validator.py`. Bug real encontrado y arreglado en el camino: el payload real de Binance Futures para partial depth usa `b`/`a` y trae `s` (símbolo), no `bids`/`asks` como muestra la doc pública (esa es la de spot) — detectado porque los mensajes llegaban pero nunca se escribían.
- **Hallazgo de arquitectura importante**: el contenedor Docker `market-ws` y el proceso real `verge_agent.py` (host) usan bases SQLite completamente separadas — el backtest engine solo lee la del host. La captura de OFI/funding se movió a correr TAMBIÉN dentro de `verge_agent.py` (no solo en Docker) para que `backtest_engine.py` tenga acceso real. Requirió reiniciar el agente en vivo 3 veces esta sesión (con 26 posiciones abiertas, todas resincronizadas sin problema).
- **Sección 3 (liquidaciones) bloqueada por Binance, no por diseño**: ni el WS `forceOrder` ni el REST `allForceOrders` responden (el REST está dado de baja oficialmente). Sección 4 (on-chain) bloqueada por decisión de proveedor/presupuesto del usuario.
- **Sección 5 (entrenar XGBoost Nexus-15) en curso**: existía `train_nexus15.py` completo pero nunca se había generado el dataset real — construido `python-service/nexus15/build_training_dataset.py` (reusa la misma `Nexus15FeatureEngine` que corre en vivo, para evitar mismatch train/serve) corriendo en background sobre ~723 símbolos.

## 2026-07-16
- **Bug real encontrado y arreglado: el sistema "inventaba" TP/SL/ClosePrice en trades reales** (3 casos con plata real de por medio: ONUSDT +$149.81 falso, LITUSDT -$106.44 falso, UBUSDT +$54.22 falso). Causa raíz: `ResolveCurrentPriceAsync` (`SimulatedTradeAppService.cs`) es una cascada de 7 fuentes de precio no relacionadas (WS interno → python-service → tickers → Binance Futures → **Binance Spot** → Bybit → OKX) sin validar ninguna contra las demás — para símbolos sintéticos/tokenizados (ONUSDT, ASMLUSDT, GOOGLUSDT) puede traer el precio de un instrumento completamente distinto. Se usa tanto para abrir como para cerrar trades. Fix aplicado solo en el cierre: se compara el precio resuelto contra `trade.MarkPrice` (ya validado por el fix de outliers del 12/07) y si difiere >30% se descarta, usando MarkPrice en su lugar. **La apertura queda con el mismo riesgo, sin arreglar** — no hay un precio anterior contra el cual validar ahí. Los 3 trades corruptos se borraron (soft-delete) y se revirtió el balance exacto que habían afectado.
- **Eliminada la Cosecha Inteligente / Diamond Hands Trailing** de `_manage_open_positions` (`verge_agent.py`), a pedido explícito del usuario: quiere SL/TP fijo al abrir, sin nada que lo mueva después (como un bracket order real de exchange). BTC Macro Exit y zombie_timeout quedaron intactos (son otra cosa). Esto contradice la nota vieja de este mismo CLAUDE.md sobre "arquitectura pendiente de decisión" — ya no aplica, está decidido y sacado.
- **Circuit breaker: arreglado el "thundering herd" de HALF_OPEN** (`circuit_breaker.py`) — antes, is_available() dejaba pasar a CUALQUIER caller durante la prueba de recuperación; si dos pegaban a la vez contra un exchange todavía baneado, cada 418 reabría la cuarentena completa por separado (visto en vivo: 5 reaperturas de ~6h en <1s el 14/07). Ahora `is_available()` reserva un único cupo de prueba con un flag (`_half_open_probe_in_flight`), liberado en `record_success`/`record_failure`.
- **Construido un motor de backtesting** (`agent/backtest_engine.py` para MaGeometry, `agent/fvg_backtest.py` para FVG) que reusa la lógica REAL de detección contra klines históricos ya cacheados en `kline_cache.py` (sqlite local, ~2 meses acumulados solos como subproducto del escaneo en vivo) — sin lookahead bias, precalculando las medias una sola vez por símbolo (O(n), la primera versión era O(n²) y no escalaba a 400 símbolos). Corrido contra ~700 símbolos: **MA Slope Caso 1 y 2 pierden plata en conjunto** (profit factor 0.82 y 0.59), **FVG en las 3 temporalidades está en el punto de equilibrio** (0.95-1.00), **solo MA Slope Caso 3 tiene margen real** (1.13-1.31) — contradice la creencia inicial de que FVG generaba más porque "en vivo" se había visto con una muestra de días, no de miles de trades. Confirmado además por datos reales del dashboard (Historial) el mismo día: Caso 3 73% precisión vs. FVG 14-20%.
- **Creado el perfil "MA Slope Caso 3 (15m)"** (clon exacto de Caso 3 con timeframe cambiado a 15m, vía SQL directo clonando la fila completa) para aprovechar la misma geometría probada con mayor frecuencia de señales — corriendo en paralelo al original, sin reemplazarlo.

## 2026-07-15
- **Nota meta (corrige la de más abajo)**: la fecha real de hoy es 2026-07-15 (system clock), no 2026-07-14 — todas las entradas de la sección de abajo (baneo Binance, logs 24h, dashboard, etc.) en realidad pasaron hoy; quedaron mal fechadas porque me guié por los timestamps de `agent/logs/agent.log` (reloj del entorno del agente) en vez de la fecha real de la sesión.
- **Bug real encontrado y arreglado en el hook global de Stop** (`~/.claude/hooks/progress-log-stop.sh`, fuera de este repo): `git rev-parse --is-inside-work-tree` da `true` parado en cualquier subdirectorio del work tree, pero el chequeo de "¿ya existe la sección de hoy?" usaba una ruta relativa (`.claude/PROGRESS_LOG.md`) que solo resuelve bien parado exactamente en la raíz del repo. Si el proceso del hook terminaba en un subdirectorio (CWD de sesión con backslashes de Windows que el `[ -d ]` de bash no reconoce, cayendo al CWD heredado), nunca encontraba el archivo y bloqueaba el Stop siempre, sin importar que el log ya estuviera al día — causó bloqueos repetidos toda la sesión. Fix: pararse en la raíz real vía `git rev-parse --show-toplevel` antes de chequear.
- **Discutido con el usuario (no implementado todavía)**: propuesta de clonar el perfil "MA Slope Caso 3" (90% aciertos según el usuario, pero muestra chica) a un timeframe más rápido (15m/5m) — el motor `MaGeometry` ya soporta `timeframe` por perfil vía `patternParamsJson` (`verge_agent.py:3399`), así que sería solo un perfil nuevo en DB, sin tocar código. Pendiente de confirmación del usuario para crear el perfil.

## 2026-07-14
- **Auditoría de `logs.txt` (1h en vivo) + causa raíz real del baneo 418 encontrada y arreglada**: `_fetch_binance_futures_klines_direct` (MA Slope, ~400 símbolos/ciclo) y `_do_fetch_binance_direct` (klines 5m) le pegaban directo a `fapi.binance.com` sin nunca avisarle al circuit breaker compartido — un 418 se resolvía con fallback pero el ciclo siguiente reintentaba igual, sin backoff, alimentando el ban en vez de dejarlo asentarse. Conectados ambos al mismo breaker que ya protege bitget/pyth (cuarentena de 4h en 418), con fallback a multi-exchange preservado para no perder datos frescos mientras dura el ban. De paso, único bug real encontrado en el log (aparte del ban): FVG generaba un candidato SHORT en LRCUSDT en cada ciclo con SL estructural de ~78% (zona agotada/vieja) que siempre terminaba bloqueado en RiskManager por TP inválido — agregado `FVG_MAX_SL_DISTANCE_PCT` (15%) en `_build_fvg_candidate` para descartarlo en origen.
- **Sistema de logs de 24h para el agente Python**: no existía ningún archivo persistente, solo stdout vía SignalR al panel `/agent` — por eso había que copiar/pegar el DOM a mano (con mojibake de encoding) para revisar algo de más de 1h atrás. Agregado `TimedRotatingFileHandler` en `verge_agent.py` → `agent/logs/agent.log` (rota a medianoche, backupCount=1, UTF-8 real), aditivo, no toca la terminal en vivo. De paso, arreglado el lag real del navegador en `/agent` tras >1h abierto: el buffer vivo hacía `shift()` de a una línea sobre un array de hasta 10k (O(n) por línea) sin `trackBy` en el `*ngFor` — bajado el cap a 2000, recorte en lotes con un solo `splice`, y `trackBy` agregado. Falta reiniciar el agente Python (backoff + log file) y rebuildear Angular (fix de lag) para que tomen efecto.
- **Verificado post-reinicio**: usuario reinició agente Python + frontend. `agent/logs/agent.log` se generó correctamente desde el arranque (21:28:28), encoding UTF-8 real (sin mojibake), 0 baneos 418 y 0 casos del bug LRCUSDT/FVG en los primeros ~9 min — señal temprana de que ambos fixes funcionan, aunque falta más tiempo corrido para confirmar del todo que el ban no reaparece.
- **Revisión de ~19h de logs reales**: baneo de Binance mejoró de ~1651/h a solo 3 episodios reales, PERO encontrado un residual: a las 10:10 hubo 5 baneos casi simultáneos (<1s) porque el breaker deja que varios call-sites prueben Binance a la vez durante `HALF_OPEN`, re-baneando la cuarentena entera cada vez en vez de servializar una sola prueba — no arreglado todavía, pendiente de decisión con el usuario. Más importante: el fix de LRCUSDT/FVG de esta misma sesión **no alcanzó** al principio — el mismo candidato roto siguió apareciendo 5 veces más después del reinicio porque mi filtro comparaba el SL contra el `current_price` que manda python-service en el snapshot del scan, que para LRCUSDT queda desalineado del precio real por horas (no es un bug de unidades — el SL real ES ~78%, un bug de logging aparte en `risk_manager.py` lo mostraba como "0.78%" por un `*100` faltante, lo cual confundió el diagnóstico inicial). Fix real: `_build_fvg_candidate` ahora revalida con un precio fresco propio (`self.fetcher.get_current_price`) antes de aceptar el candidato, no confía ciegamente en el snapshot. Falta reiniciar el agente para que tome efecto.
- Nota meta: las primeras entradas de esta sesión se escribieron por error bajo el encabezado `## 2026-07-13` (fecha de la sección ya existente en el archivo) en vez de crear `## 2026-07-14` (fecha real del día) — corregido moviéndolas acá. Esto había hecho que el hook de Stop bloqueara repetidamente creyendo que el día de hoy nunca quedó anotado (no era un bug del hook, era la fecha mal puesta).
- **Dashboard: quitado Libro de Órdenes + Trades del sidebar** (pedido explícito del usuario, "no se usan, ocupan espacio") — columna del gráfico ensanchada de `col-xl-8` a `col-xl-10` para ocupar el espacio libre (la tabla de posiciones abiertas, en la misma columna, gana el mismo ancho de yapa), panel de trading movido a `col-xl-2` propio. De paso, sacado el footer del panel de trading (`Precio de liq./Exposición/Costo/Máx/Nivel de comisión`) — eran valores mayormente hardcodeados/sin usar.
- **Dashboard: tab "Standard Scalping" movida adentro del carril con scroll** (antes fija/pinned junto a "Historial de trades", por pedido del usuario tras ver la captura del layout nuevo) — ahora scrollea junto con el resto de las estrategias dinámicas; "Historial de trades" sigue fija a la derecha.

## 2026-07-13
- **Bug encontrado y arreglado: FVG dejó de abrir CUALQUIER trade (15m/5m/1m) tras agregar `_run_fvg_scan`/`fvg_mode`**: el usuario reportó que FVG pasó de +25 USDT/10 trades a cero trades desde el fix de TP estructural del 2026-07-12. No era el fix de TP siendo "demasiado estricto" — era un omission simple. `_is_direct_injection_candidate()` en `agent/setup_validator.py` (la lista que exime a MA Slope/Arrow Peak/Total Sweep/Golden U-Turn de los vetos `range_too_small` y `disabled_signal_for_tier`, pensados para señales Nexus genéricas) nunca se actualizó para incluir `fvg_mode` cuando se agregó esa inyección directa. Resultado: el 100% de los candidatos `FVG-INJECT` en el log caían en uno de esos dos vetos — `range_too_small` porque `_build_fvg_candidate` nunca setea `estimated_range_pct` (default 0), o `disabled_signal_for_tier` porque el score de FVG (80.0 exacto, `minConfluenceScore`) cae justo en la banda 60-80% bloqueada para Tier 3. Fix: agregada `candidate.get("fvg_mode")` a `_is_direct_injection_candidate`. Falta reiniciar el agente Python para que tome efecto (no tiene hot-reload).
- **Deploy de todos los fixes del 2026-07-12** (Arrow Peak TP estructural, FVG TP estructural + cap de MA + filtro de tendencia, tope universal de TP, horario fijo a Buenos Aires): usuario reinició backend .NET y el agente Python; yo rebuildeé y reinicié el contenedor `market-data` (`verge-python-ai`), que no tiene volumen montado y se hubiera quedado con el código viejo con un simple restart. Confirmado en vivo por logs que el filtro de tendencia ya está funcionando en producción (`219 con zonas válidas | 5 bloqueados solo por contra-tendencia`).
- **Encontrado (no arreglado)**: al reiniciar todo en simultáneo, los servicios salieron a pedir datos de los ~400 símbolos del watchlist en frío al mismo tiempo — 577 errores 418 de Binance en 5 minutos. No es un problema nuevo introducido hoy, es el reinicio simultáneo golpeando la falta de caché calentado. Debería asentarse solo; si se repite cada reinicio, hay que escalonar el arranque o apoyarse más en market-ws en vez de Binance directo.

## 2026-07-12
- **ADN Compression** (estrategia nueva `StrategyType=AdnCompression`): detector de compresión MA25/50/99 + cruces de MA7 ("ADN") + ignición + pullback, en python-service (`adn_compression/`), radar en `/adn-compression`, wireado al agente (`_run_adn_compression_scan`) solo en fase `PULLBACK_TO_MA7`, LONG-only. 2 perfiles creados en DB (Micro 5m / Macro 1D).
- **FVG**: reemplazado el scan estático (`BINANCE_FUTURES_PAIRS`) por ranking real de volatilidad (`/market/tickers`) — mismo fix aplicado a Strike15m, Staircase, Arrow Peak manual, SCAR y Explosion Scanner vía `VolatileSymbolsService` compartido. Arreglado bug de `analyze_cascade` que mostraba gaps viejos/lejanos como si fueran setups válidos (ahora exige zona accionable). Cascada ahora se ancla a la temporalidad elegida en el dropdown (15m/5m/1m) en vez de forzar siempre 15m. Top-5 ahora respeta esa misma temporalidad en vez de mezclar 1m/5m/15m.
- **Bug grave encontrado y arreglado en `market_ws_server.py`**: `on_open` nunca llamaba a `exc.subscribe_fn()` — bybit/okx/bitget conectaban el socket pero nunca pedían streams, así que el caché de precios en vivo llevaba ~59 días congelado sin que nadie lo notara (el health check solo mira si el socket abrió, no si llegan datos). Agregado el llamado faltante + logging real (antes eran `except: pass` silenciosos).
- **Caché compartida nueva** (`shared_kline_cache.py`, TTL 15s) entre FVG/ADN/Arrow Peak/Strike15m/Staircase para no pedir las mismas velas 5 veces en la misma ventana — esto + el fix de market-ws fue en respuesta a un bloqueo real 418 de Binance por exceso de requests en la sesión.
- **Meta**: armado este mismo sistema de PROGRESS_LOG.md + hook global `Stop` (`~/.claude/hooks/progress-log-stop.sh`) para que futuras sesiones (en cualquier proyecto) recuerden el contexto reciente automáticamente. Probado en vivo (disparó de verdad al final de un turno real, no solo en tests). Agregado `.claude/.progress_log_marker` al `.gitignore` (bookkeeping interno, no versionar).
- Nota: durante las pruebas del hook, un `git checkout --` de test borró por accidente las constantes de ADN Compression en `agent/config.py` — detectado y restaurado en el momento, verificado que compila.
- **Estrategia nueva `StrategyType=FVG`**: agregado `sort_by=range` a `/fvg/scan` (python-service) — en vez de rankear por `confluence_score`, prioriza el mayor recorrido real hasta el TP ("la mejor entrada armada, no la de mejor score"). Wireado al agente (`_run_fvg_scan`/`_build_fvg_candidate`), SL/TP estructurales del propio gap, soporta LONG y SHORT según la zona. 3 perfiles creados en DB (1m/5m/15m). Verificado con datos reales: LABUSDT salió primero con 73.7% de rango a TP en sort_by=range, vs. ~2-10% en el ranking por score de siempre.
- **Dashboard**: rediseñada la barra de tabs de estrategias (`dashboard.component.html/scss`) — el contenedor tenía `overflow-hidden`, así que las estrategias nuevas (ADN/FVG) quedaban recortadas e invisibles aunque existían. Ahora Standard Scalping/Historial quedan fijos y el resto vive en un carril con scroll horizontal propio (nunca se recorta), con degradé de borde, ícono + color por familia (usando `profile.color`/`strategyType` que ya estaban en la DB) y badge de conteo que brilla en verde cuando hay posiciones abiertas.
- **Bug grave en `SimulationMarkPriceWorker.cs`** (preexistente, no de hoy): usaba el precio crudo de cada tick sin ninguna validación, para decidir TP/SL Y para grabar el ClosePrice — un solo print corrupto/flash de baja liquidez quedaba grabado como real para siempre (ratchet monotónico). Encontrado por un trade "ganador" sospechoso de RKLBUSDT (avance a TP de 188%); al buscar el patrón aparecieron 10 casos así (ONUSDT llegó a 728,413%). Fix: filtro de outlier (máx 15%/tick vs último precio válido del trade) + clamp del cierre real al TpPrice/SlPrice exacto (nunca al tick crudo) + tope de 100% en el ratchet. Corregidos con la fórmula exacta de `TradingSimulationService`: 10 trades cerrados (ClosePrice/ExitFee/RealizedPnl/ROI recalculados, VirtualBalance ajustado +$7.61 neto) + 61 posiciones abiertas (solo reseteo de MaxFavorablePrice/MaxTpProgressPct, sin plata de por medio). 42 casos de SL "peor de lo esperado" (100-200%) se dejaron sin tocar — es slippage real y consistente, no dato corrupto.
- **Efecto secundario mío del fix anterior**: mi script de corrección usó Decimals de Python con 50 dígitos de precisión al escribir en Postgres — `System.Decimal` de .NET solo aguanta 28-29, así que el Historial empezó a tirar `OverflowException` al leer esas filas. Encontrado que esto en realidad es un problema preexistente y mucho más grande (901 filas en toda la tabla, desde el 15/6, probablemente originado en el `backfill_tp_sl_progress.sql` de hace días) — no solo mis 10 filas. Arreglado con un `ROUND(..., 12)` sobre todas las columnas numéricas de `SimulatedTrades` y `TraderProfiles.VirtualBalance` (0 filas afectadas ahora), más redondeo defensivo agregado en los métodos de `TradingSimulationService.cs` para que no se vuelva a acumular desde el lado de C#. Lección para scripts SQL directos a futuro: siempre `ROUND` a <=12 decimales antes de escribir en columnas `numeric` que después lee un `decimal` de .NET.
- **Bug real encontrado: ni ADN Compression ni FVG abrieron NUNCA un trade real** desde que se crearon esta noche, a pesar de generar candidatos válidos (visto en los logs, `[ADN-COMPRESSION-INJECT]`/`[FVG-INJECT]` con símbolos y scores reales). Causa: existía un bypass especial en `verge_agent.py` para que MaGeometry (`source=ma_pattern:{id}`) no pasara por el matching genérico de `AllowedSources` — nunca agregué el mismo bypass para `adn_compression:{id}` ni `fvg:{id}`, así que cada perfil rechazaba sus propios candidatos (comparaba el string completo `"fvg:<uuid>"` contra el valor guardado `"fvg"`, nunca coincidía). Agregados los mismos 2 bloques de bypass que ya tenía MaGeometry. Falta reiniciar el agente Python para que tome el fix.

## 2026-07-12
- **Bug de TP fantasioso en Arrow Reversal (Arrow Peak)**: el TP se calculaba como un múltiplo RR fijo (`tpMultiplier` del perfil, 4x) sobre la distancia al SL — pero el SL de este setup es estructural (techo del pico previo + 1% buffer), no un % fijo, así que su distancia varía según qué tan grande fue el pump. Cuando el pump previo era grande, la distancia al SL terminaba siendo grande también, y el RR×4 la proyectaba hacia abajo generando TPs irreales (ej. MONUSDT: SL a 15.4% → TP a 61.7%, cuando el precio nunca viaja tanto en una reversión de 1-3 días). Esto probablemente explica por qué la estrategia "nunca ganaba nada" — el TP casi nunca era alcanzable. Confirmado con el usuario mostrando un trade real donde el TP caía muy por debajo de cualquier soporte visible en el gráfico.
  - Fix (a pedido explícito del usuario, quien pidió el approach más simple): el TP ahora apunta al **origen real de la flecha** (open de la primera vela del pump detectado, `arrow_start_price`, nuevo campo en `ArrowPeakItem`/`arrow_peak_analyzer.py`), con un buffer de 2% (`ARROW_PEAK_TP_BUFFER_PCT`) para cerrar un poco antes de completar la flecha entera. Reemplaza el RR×SL solo para Arrow Peak — otros modos (Golden U-Turn, MA Slope) no se tocaron. `custom_tp_price` (antes código muerto, nunca leído) ahora se usa de verdad para este caso específico en `risk_manager.py`. Si no hay origen de flecha válido (ej. detección vieja sin el campo), cae al piso mínimo de 10% de siempre.
  - Falta reiniciar el python-service y el agente para que tome el fix; sin reiniciar, los trades de Arrow Reversal van a seguir usando el TP viejo (RR×SL).
- **Mismo día, bug análogo (más consecuente) encontrado y arreglado en FVG**: verificado con datos reales de Binance (BEATUSDT, VELVETUSDT) que el TP de FVG apuntaba al máximo/mínimo absoluto de toda la ventana de 200 velas — cuando ese extremo era la punta de un impulso ANTERIOR mucho más grande y ya agotado (no el impulso que formó el gap), el trade avanzaba 70-85% del camino y se daba vuelta antes de llegar, devolviendo casi toda la ganancia (BEAT: entró +23% de $150 en vez de los $40-50 que llegó a tener desplegados). Fix en `python-service/fvg/analyzer.py::_liquidity_target`: compara el alcance de una ventana reciente (la pata actual) contra el de la ventana completa — si el extremo lejano es desproporcionado (>1.5x), el objetivo pasa a ser la mitad del camino restante en vez del extremo entero; si no hay desproporción (medida AB=CD / liquidity-sweep normal), se usa entero. Se aplica además un haircut final del 10% siempre (nunca apuntar al 100% del nivel calculado — práctica estándar de TA, el precio suele reaccionar antes del nivel "obvio"). Validado con datos sintéticos (sin pegarle a Binance, todavía baneados por IP hasta ~19:51 UTC por las verificaciones de este mismo día).
  - Complementario: encontrado que el bypass de riesgo de FVG reusaba `ma_slope_mode` de MaGeometry, cuyo `min_tp_pct` es solo un PISO (nunca podía acotar el TP hacia abajo, solo estirarlo) — aunque se arreglara el cálculo de arriba, un RR×SL grande del perfil podía igual mandar el TP más lejos que el nivel estructural ya corregido. Se le dio a FVG su propio modo aislado (`fvg_mode`, con `custom_tp_price` como objetivo directo) en `risk_manager.py`, sin tocar el `ma_slope_mode` real de MaGeometry/ADN.
  - Ideas confirmadas con research (measured move/AB=CD, liquidity sweep/stop hunt, TP-antes-del-nivel-obvio) — ver memoria `verge_tp_calibration`. Falta reiniciar python-service + agente para que tome el fix.
- **El mismo día, subido de escala a TODAS las estrategias** (usuario confirmó haber visto el mismo síntoma — TP apuntando a la punta de un impulso viejo/agotado, nunca alcanzable — en varias estrategias, no solo FVG). Agregado `RiskManager._apply_structural_tp_cap()` en `agent/risk_manager.py`, llamado al final de `_calculate_position_nexus_style` (para TODO lo que no sea `fvg_mode`/`arrow_peak_mode`, que ya tienen su propio TP estructural con su propio haircut — evita descontar doble) y de `_calculate_position_lse`. Mismo razonamiento que el fix de FVG (ventana reciente vs. ventana de ~4 días en 15m, recorte a la mitad si desproporcionado, haircut del 10% siempre), pero como tope de SEGURIDAD sobre lo que sea que la estrategia ya haya calculado (RR×SL, floor de Golden U-Turn, etc.) — nunca puede agrandar el TP, solo achicarlo (`min`/`max` contra el valor original), y si no hay datos suficientes no toca nada (fail-open, nunca bloquea un trade por esto). Usa `fetcher.get_klines_for_nexus` (caché-first, mismo mecanismo que ya evita pegarle de más a Binance) — no agrega llamadas nuevas a Binance en el camino feliz. Validado con 3 casos sintéticos (LONG desproporcionado, LONG proporcional, SHORT desproporcionado — el espejo para SHORT funciona bien). Falta reiniciar el agente para que tome efecto en trades reales.
- **Encontrado el problema estructural real detrás de "el TP nunca se alcanza" (mismo día, tras verificar BEATUSDT ya cerrado)**: `_manage_open_positions()` (Cosecha Inteligente / BTC Macro Exit / trailing stop) corre dentro del ciclo principal del agente Python, `LOOP_INTERVAL_SECONDS=300` — revisa las posiciones abiertas una vez cada 5 minutos. El backend C# (`SimulationMarkPriceWorker.cs`) tickea el mark price cada 1s y sí capturó el pico real de BEAT ($2.9093, verificado también contra Bybit). El agente Python, mirando el precio una vez cada 5 min, nunca vio ese pico — su propio `max_profit_price` quedó en $2.599 (visible en `ExitAuditJson`), así que la Cosecha Inteligente (que protege el 75% del máximo alcanzado y ya existe en el código) nunca tuvo la info correcta para activarse a tiempo. Conclusión: el cálculo del TP (ya arreglado hoy) no alcanza por sí solo — la lógica de protección de ganancia corre 300x más lento que el precio real. No implementado todavía — es un cambio de arquitectura (mover la lógica al lado C# de tick rápido, o desacoplar el monitoreo de posiciones del ciclo de 5 min), pendiente de decidir con el usuario.
- De paso, confirmado que el "BTC Dump (salida preventiva)" de BEAT fue un cierre 100% interno (llamada a la API de Verge, no a Binance) — existe un mirror real a Binance en `position_manager.py::BinanceDirectClient`, pero está gateado por `BINANCE_REAL_TRADING` (default false, no encontrado activado en el repo) y limitado únicamente al perfil "MA Cross Momentum".
- **Caso AGLDUSDT (SHORT de FVG-1m) investigado a fondo**: entrada ($0.1616 @ 05:19:11 UTC) verificada 100% real contra Bybit — no hubo corrupción de dato. El trade es de ANTES del fix de hoy (`AgentDecisionJson` muestra `ma_slope_mode: true`, el nombre viejo, confirma que el agente sigue sin reiniciar). El TP (7.54% de distancia) sí era un objetivo desconectado de la estructura real: el precio venía en tendencia alcista clara, la baja que gatilló el SHORT era solo una corrección hacia la MA25/50, no una reversión.
  - **Fix nuevo en `_liquidity_target`**: si un SHORT se arma con el precio ya por encima de sus propias MA25/50 (o un LONG por debajo), es una corrección dentro de la tendencia de fondo — el objetivo pasa a ser la media más próxima en vez de perseguir un swing lejano (validado con caso sintético: entrada en tendencia alcista con un mínimo viejo e irrelevante en la ventana, el viejo cálculo iba a buscar ese mínimo, el nuevo cap lo lleva correctamente cerca de la MA25). Si el precio ya está del lado de la tendencia (SHORT por debajo de sus MAs, LONG por arriba), no se aplica — se asume movimiento a favor de tendencia, no corrección.
  - **Bug de horario también encontrado y arreglado** (no relacionado con el timestamp guardado en base, que es correcto): el gráfico del dashboard (`lightweight-charts`) formateaba los ticks del eje X con el timezone del navegador/SO por default, sin fijarlo a Argentina — igual que `history.component.ts`, que usaba `toLocaleTimeString` sin `timeZone` explícito. Ambos ahora fuerzan `America/Argentina/Buenos_Aires` explícitamente, para que no dependan de cómo esté configurado el navegador/SO donde corra el dashboard. La comparación "es hoy" en el historial también se corrigió para no romperse cerca de la medianoche por el mismo motivo.
- **Auditoría completa de los 40 trades de FVG-1m de hoy** (agente en background, contra Bybit, sin usar Binance): 4 ganados, 34 perdidos por SL, 2 por otra razón. De los 34 perdidos, **19 (56%) fueron entradas contra-tendencia** (short con precio ya arriba de sus propias EMA25/50, o al revés) — de esos, 7 nunca tuvieron ni un segundo a favor (ningún ajuste de TP los salva, la entrada nunca debió abrirse), los otros 12 sí tuvieron algo de margen antes de darse vuelta. Un caso (STORJUSDT) con el mismo setup ganó — el patrón no es 100% perdedor, es ~95%. Entradas de los 40 trades verificadas 100% legítimas contra Bybit. Hallazgo aparte: BANKUSDT y REDUSDT tienen el `MaxAdversePrice` corrupto (9-15% fuera del rango real) aunque el `ClosePrice`/plata perdida es correcta — mismo tipo de bug de tick corrupto de antes (RKLBUSDT/UB), esta vez del lado del MAE, no del cierre. No investigado todavía.
- **Filtro de sesgo de tendencia agregado a FVG** (la pieza que faltaba, según la auditoría de arriba): FVG detecta el hueco de 3 velas pero nunca chequeaba la tendencia de fondo — la metodología ICT/SMC de la que sale este patrón siempre lo usa con un filtro direccional (gaps alcistas solo en tendencia alcista, bajistas solo en bajista), y acá no existía. Agregado `_is_trend_aligned()`/`_compute_mas()` en `python-service/fvg/analyzer.py`: rechaza (no lo deja ser candidato) un short con precio ya arriba de EMA25 y EMA50, o un long por debajo de ambas — mismo criterio ya usado hoy para el cap de TP, reusado acá para RECHAZAR la entrada en vez de solo acotar el objetivo. Aplicado en `_scan_symbol` (candidatos del scan) y `analyze_cascade` (bias de la cascada) — los dos puntos reales por los que el agente termina abriendo un trade. Nuevo campo `trend_aligned` expuesto en `FvgZone`. Validado con 3 casos sintéticos (short contra-tendencia rechazado, short a favor de tendencia aceptado, long contra-tendencia rechazado). Habría bloqueado los 19 casos de hoy (18 pérdidas + 1 ganancia de STORJUSDT) — no toca los otros 15 perdidos, que no muestran patrón sistémico (pérdidas normales de mercado).
  - **Visibilidad agregada** (pedido explícito del usuario: que el filtro no sea una caja negra que pueda dejar el sistema sin candidatos sin que se note): `/fvg/scan` ahora devuelve `trend_blocked_count`/`actionable_count` — cuántos símbolos tenían un gap accionable pero se descartó solo por ir contra tendencia, vs. cuántos pasaron. Logueado en python-service Y en el agente (`[FVG-STRATEGY] N candidatos a favor de tendencia | M bloqueados`). El filtro corre sobre los 400 símbolos del watchlist completo y zona por zona (no solo sobre un top-5 ya elegido), así que un símbolo con más de un gap detectado puede seguir aportando un candidato aunque su mejor gap sea contra-tendencia. Con los datos de hoy como referencia: 19 de 400 bloqueados, no un problema de volumen.

## 2026-07-24
- **Bug real confirmado en vivo: `MAX_POSITION_DURATION_HOURS=48` (timer global en `config.py`) cortaba CUALQUIER trade a las 48hs clavadas, gane o pierda** — violaba la regla explícita de "SL/TP fijo, sin salidas por tiempo". Pasó con FVG-15m Pulido: NIGHTUSDT llegó a 98.95% del TP (a centavos de +$22-24) y el timer lo cerró igual a las 48hs en +$17, perdiendo ganancia ya generada. Subido a 30 días (red de seguridad real, no salida encubierta). Falta reiniciar el agente.
- Confirmado en el código que el cierre por timeout (o cualquier otro motivo) ya dispara el mirror a Binance si el perfil tiene `BroadcastToBinance=true` — no hacía falta nada nuevo ahí, ya estaba resuelto por el mismo path que usa TP/SL normal.
- **FVG - 15m Pulido** y **MA Slope Caso 3 Pulido** creados como clones 100% vía config (`PatternParamsJson`, sin tocar código ni afectar los originales): el primero con filtro de agotamiento (exige pendiente de EMA50 pronunciada antes de la entrada — auditoría real: 15% de los trades de FVG-15m generaban 65% de la ganancia); el segundo con `tpMinPct` bajado de 10.0 a 3.0 (el diseño original de MA Slope busca capturar solo el primer ~2%, no un 10% que casi nunca se alcanza a tiempo).
- **Historial**: el "Progreso a TP" de un trade abierto solo mostraba el valor en vivo, nunca el pico histórico (`MaxTpProgressPct`, que sí se guardaba bien en tiempo real) — un trade podía haber llegado a 98.95% del TP y la UI mostraba 70% sin dar pistas de que estuvo mucho más cerca. Ahora se muestra "actual (máx. X%)" mientras sigue abierto.
- **`market_ws_server.py` (contenedor Docker `verge-market-ws`, no el agente)**: encontrados y arreglados 3 bugs reales tras un corte de luz — (1) crash total por encoding cp1252/emojis al redirigir stdout; (2) `ping_payload="ping"` definido en `exchange_registry.py` pero nunca usado, por eso OKX/Bitget se caían cada 30-90s (agregado hilo de keepalive real); (3) la URL de OKX (`/public`) ya no sirve el canal de velas desde hace tiempo — confirmado en vivo contra el servidor real, corregido a `/business`. Verificado post-fix: OKX pasó de `written=0` siempre a `written=704/707` sin cortes.
- Agregado campo `BroadcastToBinance` end-to-end (Domain/DTO/AppService/Angular/agente) para reemplazar el hardcodeo por id/nombre que solo dejaba operar contra Binance a "MA Cross Momentum" — ahora es un switch por estrategia en `/strategies`. De paso, encontrado y arreglado que Binance Testnet lista símbolos (`BLUAIUSDT`, `CLOUSDT`) con `status=PENDING_TRADING` que igual rechaza al operar — chequeo proactivo + lista negra persistida por entorno antes de intentar la orden.

## 2026-07-25
- **FVG - 15m Pulido pasó a Short-only**: auditoría real de 39 trades mostró Short (13 trades) +$43.73/38.5% WR vs Long (26 trades) -$16.27/7.7% WR — el filtro de agotamiento "espejo" para Long no funciona (probado con varios umbrales de pendiente, ninguno salva el Long de ningún timeframe de FVG). `AllowLong=false` en DB. Clonado también "FVG - 15m Pulido V2" (idéntico, Short-only) para próximos cambios a definir.
- **Investigación de patrón LONG real en todo Verge** (no solo FVG): encontrado que MA Slope Caso 1 (Long-only, 61 trades/5 días) es la mejor generadora de Long de todo el sistema, y que filtrar por pendiente de EMA50 < -5° (caída previa real antes del giro) casi duplica su resultado: 37.7%→47.4% WR, +$20.54→+$49.32. Patrón confirmado real pero específico de MA Slope, no transferible a FVG.
- **Armado `agent/fvg_short_backtest.py`**: backtest de FVG-15m-Pulido-Short contra klines históricos cacheados (`agent/data/klines.db`, hasta 76 días por símbolo), reusando el motor REAL de detección (`python-service/fvg/analyzer.py`) vía monkeypatch de `_fetch_klines` en vez de reimplementar la lógica — sin look-ahead. Prueba piloto con 3 símbolos: 95 trades, 28.4% WR. Corrida más grande en progreso al cierre de la sesión.

## 2026-07-26
- **Backtest a mano de MA Slope Caso 3 tuvo un bug real**: se saltó `RiskManager._apply_structural_tp_cap()`, que SÍ aplica a `ma_slope_mode` (solo FVG/Arrow Peak están exceptuados) — el resultado manual (+$134.96/137 trades) quedó invalidado. Motivó construir un motor de backtest genérico reutilizable (`agent/backtest/engine.py`) que reusa `verge_agent.py`/`risk_manager.py` REALES (vía un `HistoricalFetcher` que sirve klines históricos en vez de pegarle a Binance en vivo) en lugar de reimplementar cada estrategia a mano — evita esta clase de bug a futuro. Primer alcance: `StrategyType=MaGeometry`.
- **`agent/data/binance_vision_clean.db` ampliada a cobertura completa dic-2025→jul-2026** para los 428 símbolos vigentes (antes solo llegaba a jun-2026 para la mayoría, y varios símbolos nunca tenían dic-2025) — descarga vía `data.binance.vision` (archivo público, sin riesgo de ban) paralelizada con `ThreadPoolExecutor` (64 workers) en `agent/download_binance_vision_daily_all_v2.py`, bajó de horas a minutos.
- **Hallazgo real corriendo el motor nuevo**: con el tope estructural de TP + veto MinRR≥4 aplicados de verdad, MA Slope Caso 3 rechaza la enorme mayoría de sus candidatos (R:R real cae muy por debajo de 4:1 tras el recorte de TP) — pendiente confirmar el número final de PnL sobre los 428 símbolos completos (corrida en curso al cierre de sesión, optimizando antes un cuello de botella de performance: `verge_agent.py` configura logging a archivo con rotación al importarse, generaba miles de writes a disco por corrida y frenaba el backtest a ~2min/símbolo).
- Plan completo del módulo de backtesting dinámico (motor + API FastAPI + tabla `BacktestRun` + UI Angular con barra de progreso, reemplazando el mock existente en `angular/src/app/backtesting/`) guardado en `C:\Users\Nicolas\.claude\plans\foamy-watching-ladybug.md`, aprobado por el usuario — implementación en curso.

## 2026-08-02
- **Bug real encontrado y arreglado: `MinRR=4` + tope estructural mal aplicado a `ma_slope_mode` tenían "MA Slope Caso 3" y "FVG - 15m" completamente bloqueadas (0 trades, 2 días) sin loguear el motivo real** — bajado `MinRR` a 3 en los perfiles afectados y excluido `ma_slope_mode` del cap genérico en `risk_manager.py` (mismo criterio que ya tenían `fvg_mode`/`arrow_peak_mode`). Confirmado con datos reales: +$72.93 generados en las 2 estrategias desde el fix (antes: $0). También arreglado `CloseTradeAsync` en el backend .NET — instrumentos "TradFi Perps" (AAPLUSDT, HK1810USDT, etc.) nunca resuelven precio vía Binance/Bybit/OKX y dejaban el trade imposible de cerrar para siempre; ahora cae a `MarkPrice` en vez de tirar excepción.
- **`verge-backtest` containerizado en Docker** (antes corría suelto, se caía si se cerraba la terminal) — volumen NOMBRADO (no bind mount) para la DB de 3.5GB, evita la lentitud extrema de I/O de Docker Desktop en Windows con archivos grandes.
- **Epic `market-data-expansion` (#156): corrido el A/B pendiente hace 2 semanas** (OFI/funding/liquidaciones como filtro sobre MA Slope Caso 3, 15 días de historia real acumulada) — resultado real: ningún filtro mejora (OFI empeora PF 1.13→0.90, funding/liquidaciones no tuvieron ni un solo evento que coincidiera con un candidato en la ventana). Pausada "FVG - 15m Pulido V2" (convergía con su gemela a ~5.8% WR perdedor).
- **3 estrategias nuevas probadas desde cero** (RSI+Bollinger mean reversion, Bollinger Squeeze breakout, combinación FVG+geometría Caso 3) contra la misma ventana histórica — ninguna superó a MA Slope Caso 3 (+$76.74) ni FVG-15m (+$83.35) reales del usuario; resultados: +$10.08/PF 1.03, -$45.07/PF 0.89, -$165.42/PF 0.58 respectivamente. Estrategia #4 (soporte/resistencia con volumen) en curso al cierre de esta entrada.

## 2026-08-03
- **"Pump Reaper" creado y activado en producción**: estrategia nueva minada desde cero (RSI>65 + precio arriba de MA7/25/50/99 + subiendo + volumen + confirmación de giro → SHORT del blow-off top en memecoins), validada con minería cross-symbol + robustez temporal + out-of-sample + motor real de backtest. Conectada como inyección directa (`meme_short_top_mode` en `risk_manager.py`/`setup_validator.py`, detector en `verge_agent.py`), `StrategyProfile` real creado y activado por el usuario. **Bug real encontrado y corregido el mismo día**: el backtest daba 3 cupos de capital a CADA símbolo por separado en vez de 3 cupos TOTALES compartidos — sobreestimaba el resultado ~4.8x (+$740 vs el real +$153 en 8 meses). Mismo patrón probado también en una canasta diversa de altcoins no-meme (+$81/8 meses con filtro dinámico de volatilidad ATR≤1.5%, sin lista hardcodeada) — queda solo como script, no productivizado.
- **Investigado por qué FVG-15m (original) pierde muchos trades seguidos**: confirmado con datos reales de producción (no backtest) que el tamaño del gap (`gap_pct`) predice el tamaño de la pérdida — trades con gap≤1% dieron +$130.70 (196 trades), trades con gap>1% dieron -$37.79 (67 trades), sobre un total real de +$92.90. El filtro `maxGapPct` ya existe en el código (usado en los clones "Pulido") pero nunca se activó en el perfil original — pendiente crear el clon con el filtro.

## 2026-08-30
- **Redactado un dossier de contexto completo del sistema** (para onboardear a alguien que va a extender Verge) y publicado como Artifact HTML público: arquitectura + procesos/puertos, el loop 24/7 del agente, fuentes de datos, las 5 capas anti-ban (WS/cache-first/rate limiter/circuit breaker/binance.vision), cómo se crean estrategias (fila StrategyProfile: Manera A solo-config vs Manera B inyección directa con flag en risk_manager.py + setup_validator.py), ciclo de vida de un trade, control/dashboard, backtesting (motor que reusa código real + lecciones de pooling), estado honesto (solo MA Slope Caso 3 con edge, XGBoost sin entrenar, widget ballenas falso, epic #156 sin mejora), trampas conocidas y puntos de entrada.
- Sin cambios de código — solo documentación. Fuente: código + PROGRESS_LOG + memorias de sesión.

## 2026-09-13 (Round 43)
- **Trail-1 despriorizado explícitamente a "overlay opcional parqueado"** (el usuario aclaró que no es una estrategia y no quiere más rondas demostrándolo). Parte 1: el switch global (antes `TrailStop:Enabled` en `appsettings.json`, requería editar archivo + reiniciar) pasó a ser un **ABP Setting** (`Verge.TrailStop.Enabled`), administrable en caliente desde una pestaña nueva en Configuración de Verge (`SettingTabsService`), sin migraciones nuevas (`AbpSettings` ya existía). Nuevo `VergeGlobalSettingsAppService` acotado a este único switch. Ambos toggles (global + por perfil) quedan en `false`. Build 0 errores, 17/17 tests OK, `tsc --noEmit` 0 errores. Ver `TRAIL1_UI_SETTINGS_ROUND43.md`.
- **Objetivo real de la ronda (encontrar 1 estrategia nueva desde los 3.364 trades reales) terminó en `NO HAY ESTRATEGIA VALIDADA`**: solo alcanzó un pase exploratorio, no la reconstrucción forense completa. Hallazgo real de datos (no una estrategia): la corrupción de `ONUSDT`/`BBUSDT` ya documentada en R37 sigue sin limpiarse en `SimulatedTrades` — infla el PnL agregado del perfil "Nexus" de $3,549 real a $363,260 falso (70x). Con datos limpios, "Nexus" concentra ~70% del PnL real total (260/3.340 trades) pero con solo $13.65/trade promedio — no investigado a fondo, queda como punto de partida para la próxima ronda dedicada a esto. Ver `NEW_STRATEGY_DISCOVERY_ROUND43.md`.

## 2026-09-13 (Round 44)
- **Forense de "Nexus" (270% del PnL "real limpio" según R43) = FAILED, y la causa es más grave de lo esperado**: el perfil `Nexus` tiene `CreationTime`=2026-09-06, pero sus 264 trades van de 2026-05-16 a 2026-09-04 (¡anteriores a que el perfil exista!). Los 264 tienen `ExtraProperties={"reconstructed":"trades.csv 2026-09-05"}`, `AgentDecisionJson`/`Ma7DistancePctAtEntry`/`MaxAdversePrice`/`MaxFavorablePrice` NULL en el 100%, 260/260 cierran por `tp_hit` (cero SL, estadísticamente imposible en producción real), y hay 15 grupos de trades duplicados casi idénticos (mismo símbolo/precio, aperturas con segundos de diferencia). Todo coincide exactamente con el incidente YA documentado en memoria (`verge_backtest_gate`): reset de Docker Desktop borró `SimulatedTrades`+`StrategyProfiles` sin backup, reconstruidos desde `agent/data/trades.csv` con PnL explícitamente "no confiable".
- **Hallazgo más grave: no es solo Nexus.** De los ~446 trades cerrados que existen hoy en la tabla, **442 (99.1%) llevan la misma etiqueta `reconstructed`** — los 18 perfiles restantes con historial están en la misma situación. **Solo 4 trades en todo el sistema son genuinamente post-incidente** (2 de "MA Slope Caso 3", 1 de "FVG-15m", 1 de "Band Touch 15m", todos del 2026-09-13, hoy). El objetivo de R43 ("minar 3.364 operaciones reales") estaba construido sobre una premisa falsa: esa población es en un 99% el mismo import legacy no confiable, no operativa real.
- **No se pudo avanzar a Portfolio Engine/TRAIN-VAL-OOS** porque no hay estrategia real que formalizar — el fallo ocurre en el gate de integridad de datos (Paso 1), antes de reconstruir contexto de mercado. Sin inventar parámetros para salvarlo.
- **Conclusión operativa para el futuro**: hace falta dejar correr el sistema en vivo semanas (no días) para acumular volumen real antes de poder repetir este tipo de minería forense con datos confiables. Deliverable completo: `NEXUS_FORENSIC_ROUND44.md`.

## 2026-09-13 (Round 45)
- **Corrección a R44**: el "100% tp_hit, cero SL" de Nexus reportado en R44 era un bug propio (filtré `Status=1`, que en este esquema significa específicamente "cerrado por TP", no "cerrado" en general — 0=abierto, 1=tp_hit, 2=sl_hit/timeout). Nexus real: 1,919 trades (260 TP/1,641 SL/18 timeout), PnL neto **−$1,536.37** (perdedor). El resto de la evidencia de R44 (perfil creado después de sus propios trades, 99%+ de `SimulatedTrades` con `ExtraProperties={"reconstructed":...}`) sigue siendo válida.
- **Minería transversal de los ~19 perfiles** (sin distinguir por nombre): 3,296 trades limpios → 2,611 con contexto causal reconstruido (retornos/MA/ATR/volumen/OI/funding/forma de vela). 40,075 combinaciones evaluadas TRAIN/VAL/OOS; 5 candidatas sobrevivieron los 3 splits (todas variantes de "momentum 3 velas + vela de convicción", LONG). Un segundo split independiente (mitad1/mitad2) dejó 2 vivas. Corridas por `portfolio_engine.py` real (capital 450, 3 slots, fees/funding reales por trade): ambas dan OOS positivo (+$11/+$11.1, ~$8.4-8.5/mes) — pasan el gate por la letra, pero **"Momentum-Wick LONG" no sobrevive quitar el mejor trade de OOS** (+$11→−$3.4, 1 trade explica todo) y **"Volume-Body LONG" sobrevive apenas pero es económicamente nulo** (~$2.7-8.5/mes sobre $450, muy por debajo del piso histórico de $150/mes del proyecto). **Veredicto: NO HAY ESTRATEGIA VALIDADA.** Con 40k combinaciones probadas, encontrar 1-2 "positivas en 3 splits" es lo esperable por puro azar (multiple comparisons), no señal real. Deliverable: `CROSS_PROFILE_MINING_ROUND45.md`. Scripts: `agent/backtest/r45_cross_profile_mining.py`, `r45c_mine_fast.py`, `r45d_second_split_and_portfolio.py`.

## 2026-09-13 (Round 46)
- **Descubrimiento guiado por 45 rondas de evidencia** (no combinatoria ciega): se construyó una matriz "familia→por qué falló→qué implica" a partir de la memoria del proyecto (funding carry murió por fees de 2 patas, retail/smart-money murió por placebo idéntico, régimen BTC peor que aleatorio, etc.) para acotar la búsqueda a mecanismos con movimientos de cientos de bp y control de placebo explícito.
- **Reencuadre event-driven**: se enriqueció el dataset transversal de R45 (2,156 trades) con MFE/MAE del path real + features de transición (aceleración 3v/10v relativa, compresión previa, sorpresa de volumen). Bucketizando por TAMAÑO del movimiento (no solo win/loss): 56% de las pérdidas totales tuvieron ≥100bp de favorable antes de revertir (reconfirma el origen de Trail-1, sin tocarlo). Hallazgo real: los "grandes ganadores" (MFE≥300bp) no se distinguen de las "pérdidas con reversión" (MFE≥100bp) por el NIVEL de momentum pre-entrada (similar en ambos) sino por si ese momentum viene ACELERANDO recién (persecución de un pico → revierte) o es sostenido sin aceleración reciente (continuación → corre) — mecanismo interpretable y transversal (5+ perfiles distintos en el bucket de grandes ganadores).
- **3 candidatas formalizadas** (Contexto→Estado→Evento→Entrada→Exit) y validadas con la misma disciplina de R45 (TRAIN/VAL/OOS + 2º split independiente + `portfolio_engine.py` real + remove-best-trade): C1 (LONG) y C3 (control sin filtro de aceleración) = FAILED. **C2 (SHORT, tendencia bajista + aceleración bajista sostenida, sin agotamiento) = CONDITIONAL**: sobrevive TODO (2º split +$52.9/+$54.1 ambas mitades, portfolio-engine OOS +$89.0 con 0 rechazos por concurrencia, remove-best-trade deja +$20.1 sigue positivo), transversal (57 símbolos, 8 perfiles de origen, sin concentración), MFE mediana 266bp/media 574bp. No llega a PASS: tramo VAL negativo (−$19.9, n=13) y volumen total bajo (67 trades/90 días). Trail-1 no se combinó (candidata no confirmada aún). Deliverable: `GUIDED_DISCOVERY_ROUND46.md`. Scripts: `agent/backtest/r46*.py`.
- **Próximo paso**: NO repetir combinatoria — dejar acumular más trades SHORT en tendencia bajista genuinamente en vivo y re-correr la MISMA regla ya congelada de C2 (sin retocar umbrales) como la prueba OOS real que le falta antes de subirla a PASS.

## 2026-09-13 (Round 47)
- **Intento final de convertir C2 (R46) en estrategia económica = FAILED.** Fórmula congelada (SHORT: price_vs_ma50<0 & ret_20<0 & accel_3_10>=mediana_TRAIN), sin retocar umbrales. Población total: 67 trades en 90 días. OOS (20 días, 15 trades): PnL +$89.0 (~$133.5/mes, cerca del objetivo de $150) PERO **no sobrevive quitar los 3 mejores trades** (pasa a −$11.1) y depende de un solo mes calendario (jul −$5.5, ago +$94.5) — gate explícito de la ronda. Mirror LONG (C1, n=408, mucha más frecuencia) y versión SHORT sin filtro de aceleración (más frecuencia, menos calidad) ambas FAILED: no sobreviven el 2º split independiente ni remove-best/top3. Conclusión: el mecanismo de R46 es real e interpretable pero su techo económico está muy por debajo de 450/3 slots — no es un problema de esperar más datos. Scripts: `agent/backtest/r47a_*.py`, `r47b_*.py`.

## 2026-09-13 (Round 48 — ÚLTIMA ronda de discovery, cerrada)
- **2 mecanismos nuevos probados** (D1 "Capitulación-Reversión": extremo de 50 velas + compresión previa + sorpresa de volumen; D2 "Expansión-tras-Compresión": breakout direccional tras compresión) sobre el dataset transversal de R46-R47. Ambos con las 3 condiciones combinadas son demasiado raros (1-11 eventos en 5 meses) para evaluar — FAILED por frecuencia insuficiente. Versión relajada de D1 (solo extremo+volumen, sin compresión, n=60-93) da resultado **decisivamente negativo** en TRAIN/VAL/OOS/ambas mitades del 2º split (SHORT: OOS wr=0%, −$119.1/27 trades) — comprar/vender en un extremo con volumen alto resulta ser "agarrar el cuchillo que cae", no reversión.
- **Veredicto final: `NO HAY EVIDENCIA SUFICIENTE DE UNA NUEVA ESTRATEGIA ECONÓMICAMENTE ÚTIL`.** Discovery TERMINADO según instrucción explícita del usuario — no generar más rondas repitiendo esta evidencia. Deliverable: `FINAL_DISCOVERY_ROUND47_48.md` (cubre R47 y R48). Scripts: `agent/backtest/r47*.py`, `r48_final_discovery.py`.
- **Qué haría falta para reabrir la línea en el futuro**: volumen de trades genuinamente en vivo (no reconstruidos, ver R44) y/o fuentes de datos nuevas nunca probadas (liquidaciones, on-chain, opciones — descartadas en R1-R22 por falta de datos, no por resultado negativo).

## 2026-09-14
- **Análisis de reversión desde el pico (giveback), pedido por el usuario tras un test previo mal planteado**: contrafactual real sobre 2,333 trades muestra que de los 153 que llegaron a ≥$30 flotante, 41.8% terminaron dando vuelta TOTAL a SL (casos extremos: HOMEUSDT +$380→$0, LABUSDT +$267→$0) y otro 49.7% se dejó más de la mitad del pico — ~$16,300 dejados sobre la mesa solo en ese grupo. Confirma que esto es exactamente el problema que Trail-1 (parqueado desde R42, switches OFF) ya resuelve — se le recomendó al usuario activarlo en vez de reinventar la idea.
- **Cap de SL máximo -$5 USDT**: confirmado por el usuario que ya está activo en producción (implementado en otra sesión) y que ya borró ~347 trades históricos que lo excedían por su cuenta. Verificado en DB: quedan 440 trades con pérdida >-$5, incluidos algunos muy recientes (hoy/ayer) — pendiente investigar por qué el cap no frena todas las entradas nuevas. No se tocó la DB en esta sesión (ni borrado ni excluido), a la espera de instrucción explícita del usuario sobre alcance.
